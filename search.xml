<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>使用命令行的艺术1 - Bash Shell相关</title>
    <url>/2025/03/11/2025-3-11-Command%20Line%20Art1-%20Bash%20Shell%E7%9B%B8%E5%85%B3/</url>
    <content><![CDATA[<p>熟练使用命令行是一种常常被忽视，或被认为难以掌握的技能，但实际上，它会提高你作为工程师的灵活性以及生产力。本文旨在了解命令行的Shell语言的一些基础概念和有关使用命令行的Shell的一些快捷操作，额外带一些Linux系统中系统文件说明…</p>
<span id="more"></span>
<h2 id="使用docker拉取ubuntu容器"><a class="markdownIt-Anchor" href="#使用docker拉取ubuntu容器"></a> 使用docker拉取Ubuntu容器</h2>
<p>由于本人的笔记本并非Linux操作系统，为了方便命令行的学习而又不想搭建虚拟机双开系统，我们使用Docker来拉取一个Ubuntu镜像来方便我们学习。</p>
<ul>
<li>拉取Ubuntu镜像</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker pull ubuntu</span><br></pre></td></tr></table></figure>
<ul>
<li>运行一个交互式的 Ubuntu 容器</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -it ubuntu</span><br></pre></td></tr></table></figure>
<p>这将拉起一个新的 Ubuntu 容器并进入它的命令行。<code>-it </code>标志表示以交互模式启动，并连接到该容器的终端。进入容器后，你将获得一个完全隔离的 Linux 系统，可以像平常一样使用 Shell。</p>
<ul>
<li>退出容器<br />
你可以在Linux系统中运行 exit 命令来退出容器。默认情况下，当你退出容器后，容器也会自动停止。如果你想保持容器在后台运行，可以通过以下方式运行容器：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d ubuntu</span><br></pre></td></tr></table></figure>
<ul>
<li>重新进入容器<br />
如果是在Linux系统中运行exit命令后，退出容器后容器自动停止，此时再想打开这个容器的话我们先运行以下命令找到我们的容器ID。</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker ps -a</span><br></pre></td></tr></table></figure>
<p>我的电脑上输出的结果是：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">CONTAINER ID   IMAGE     COMMAND       CREATED          STATUS         PORTS     NAMES</span><br><span class="line">3a8357da8968   ubuntu    &quot;/bin/bash&quot;   2 minutes ago   Exited (0) About a minute ago             tender_elbakyan</span><br></pre></td></tr></table></figure>
<p>然后再根据容器ID启动一个停止的容器：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker start 3a8357da8968</span><br></pre></td></tr></table></figure>
<p>进入一个已经在运行的容器：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker <span class="built_in">exec</span> -it 3a8357da8968 /bin/bash</span><br></pre></td></tr></table></figure>
<ul>
<li>删除容器和镜像<br />
当我们不在需要容器和镜像的时候，可以删除容器和镜像来节省内存。首先根据容器ID删除容器：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker <span class="built_in">rm</span> 3a8357da8968</span><br></pre></td></tr></table></figure>
<p>再根据镜像ID删除镜像。先得到镜像ID：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker images</span><br></pre></td></tr></table></figure>
<p>我的电脑的运行结果是：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">REPOSITORY   TAG       IMAGE ID       CREATED       SIZE</span><br><span class="line">ubuntu       latest    c3d1a3432580   6 weeks ago   101MB</span><br></pre></td></tr></table></figure>
<p>再删除镜像：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker rmi c3d1a3432580</span><br></pre></td></tr></table></figure>
<h2 id="bash-shell"><a class="markdownIt-Anchor" href="#bash-shell"></a> Bash Shell</h2>
<h3 id="linux系统文件中的shell"><a class="markdownIt-Anchor" href="#linux系统文件中的shell"></a> Linux系统文件中的shell</h3>
<p>Shell（壳程序）可以提供使用者操作应用程序的接口。<code>/bin/bash</code>是Linux默认的shell，Linux可用的shell记录在<code>/etc/shells</code>中，我们可以通过以下命令查看Linux可用的shells：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cat</span> /etc/shells</span><br></pre></td></tr></table></figure>
<p>得到的输出是：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line"># /etc/shells: valid login shells</span><br><span class="line">/bin/sh</span><br><span class="line">/usr/bin/sh</span><br><span class="line">/bin/bash</span><br><span class="line">/usr/bin/bash</span><br><span class="line">/bin/rbash</span><br><span class="line">/usr/bin/rbash</span><br><span class="line">/usr/bin/dash</span><br></pre></td></tr></table></figure>
<p>登陆Linux系统时，系统会给予一个shell，这个登陆取得的shell记录在<code>/etc/passwd</code>中，可以通过以下命令查看：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cat</span> /etc/passwd</span><br></pre></td></tr></table></figure>
<p>得到的输出是：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">root@611b47761989:/# cat /etc/passwd</span><br><span class="line">root:x:0:0:root:/root:/bin/bash</span><br><span class="line">daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin</span><br><span class="line">......（下面省略）......</span><br></pre></td></tr></table></figure>
<h3 id="bash-shell的功能"><a class="markdownIt-Anchor" href="#bash-shell的功能"></a> bash shell的功能</h3>
<ul>
<li>记忆使用过的指令（记录的指令在<code>~/.bash_history</code>中）</li>
<li>命令与文件的补全（使用<code>tab</code>键，两次连按<code>tab</code>键会对匹配的命令或文件进行显示，如果只有一个与之匹配的命令或文件，则一次<code>tab</code>键会自动补全）</li>
<li>设置命令别名<br />
比如要现实目录下面的所有文件 （包含隐藏文件） 及所有的文件属性，我们可以运行以下命令：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">ls</span> -al</span><br></pre></td></tr></table></figure>
<p>但我们又是嫌弃这样写太过麻烦，我们可以对以上命令进行别名命名：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">alias</span> lm=<span class="string">&#x27;ls -al&#x27;</span></span><br></pre></td></tr></table></figure>
<p>那么<code>lm</code>的功能就和之前的<code>ls -al</code>功能一致了。</p>
<ul>
<li>程序化脚本（shell script）</li>
<li>万用字符匹配*（正则表达式匹配）</li>
</ul>
<table>
<thead>
<tr>
<th>符号</th>
<th>意义</th>
</tr>
</thead>
<tbody>
<tr>
<td>*</td>
<td>代表“ 0 个到无穷多个”任意字符</td>
</tr>
<tr>
<td>?</td>
<td>代表“一定有一个”任意字符</td>
</tr>
<tr>
<td>[ ]</td>
<td>同样代表“一定有一个在括号内”的字符（非任意字符）。例如<code>[abcd]</code> 代表“一定有一个字符， 可能是 a, b, c, d 这四个任何一个”</td>
</tr>
<tr>
<td>[ - ]</td>
<td>若有减号在中括号内时，代表“在编码顺序内的所有字符”。例如<code>[0-9]</code> 代表 0 到 9 之间的所有数字，因为数字的语系编码是连续的！</td>
</tr>
<tr>
<td>[^ ]</td>
<td>若中括号内的第一个字符为指数符号 （^） ，那表示“反向选择”，例如 <code>[^abc]</code> 代表 一定有一个字符，只要是非 a, b, c 的其他字符就接受的意思。</td>
</tr>
</tbody>
</table>
<ul>
<li>与命令行相关的特殊符号</li>
</ul>
<table>
<thead>
<tr>
<th></th>
<th></th>
</tr>
</thead>
<tbody>
<tr>
<td>#</td>
<td>注解符号：这个最常被使用在 script 当中，视为说明！在后的数据均不执行</td>
</tr>
<tr>
<td>\</td>
<td>跳脱符号：将“特殊字符或万用字符”还原成一般字符</td>
</tr>
<tr>
<td>|</td>
<td>管道（pipe）：分隔两个管线命令的界定；</td>
</tr>
<tr>
<td>;</td>
<td>连续指令下达分隔符号：连续性命令的界定 （注意！与管线命令并不相同）</td>
</tr>
<tr>
<td>~</td>
<td>使用者的主文件夹</td>
</tr>
<tr>
<td>$</td>
<td>取用变量前置字符：亦即是变量之前需要加的变量取代值</td>
</tr>
<tr>
<td>&amp;</td>
<td>工作控制 （job control）：将指令变成背景下工作</td>
</tr>
<tr>
<td>!</td>
<td>逻辑运算意义上的“非” not 的意思！</td>
</tr>
<tr>
<td>/</td>
<td>目录符号：路径分隔的符号</td>
</tr>
<tr>
<td>&gt;, &gt;&gt;</td>
<td>数据流重导向：输出导向，分别是“取代”与“累加”</td>
</tr>
<tr>
<td>&lt;, &lt;&lt;</td>
<td>数据流重导向：输入导向</td>
</tr>
<tr>
<td>’ ’</td>
<td>单引号，不具有变量置换的功能 （$ 变为纯文本）</td>
</tr>
<tr>
<td>&quot; &quot;</td>
<td>具有变量置换的功能！ （$ 可保留相关功能）</td>
</tr>
<tr>
<td></td>
<td>两个“ ` ”中间为可以先执行的指令，亦可使用 $（ ）</td>
</tr>
<tr>
<td>（ ）</td>
<td>在中间为子 shell 的起始与结束</td>
</tr>
<tr>
<td>{ }</td>
<td>在中间为命令区块的组合！</td>
</tr>
</tbody>
</table>
<h3 id="bash-shell的内置命令查询type"><a class="markdownIt-Anchor" href="#bash-shell的内置命令查询type"></a> bash shell的内置命令查询：type</h3>
<p>通过<code>type</code>指令我们可以知道每个指令是否为 bash 的内置指令，此外利用<code>type</code>搜寻后面的名称时，如果后面接的名称并不能以可执行文件的状态被找到， 那么该名称是不会被显示出来的。因此我们可以也可以直接运行可执行文件来代替原命令。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">type</span> -a <span class="built_in">ls</span></span><br></pre></td></tr></table></figure>
<p>得到的输出为：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">ls is aliased to `ls --color=auto&#x27;</span><br><span class="line">ls is /usr/bin/ls</span><br><span class="line">ls is /bin/ls</span><br></pre></td></tr></table></figure>
<p>因此我们也可以直接运行<code>/usr/bin/ls</code>或<code>/bin/ls</code>来代替<code>ls</code>。找指令也可以使用<code>which</code>。</p>
<h3 id="bash-shell中的快速编辑按钮"><a class="markdownIt-Anchor" href="#bash-shell中的快速编辑按钮"></a> bash shell中的快速编辑按钮</h3>
<ul>
<li><code>ctrl-r</code>搜索命令行历史记录（按下按键之后，输入关键字便可以搜索，重复按下<code>ctrl-r</code>会向后查找匹配项，按下<code>Enter</code>键会执行当前匹配的命令，而按下右方向键会将匹配项放入当前行中，不会直接执行，以便做出修改）。</li>
<li><code>ctrl-w</code>删除你键入的最后一个单词，<code>ctrl-u</code>可以删除行内光标所在位置之前的内容，<code>alt-b</code>和 <code>alt-f</code>可以以单词为单位移动光标，<code>ctrl-a</code>可以将光标移至行首，<code>ctrl-e</code>可以将光标移至行尾，<code>ctrl-k</code>可以删除光标至行尾的所有内容，<code>ctrl-l</code>可以清屏。键入 <code>man readline</code> 可以查看 Bash 中的默认快捷键。内容有很多，例如 <code>alt-.</code>循环地移向前一个参数，而<code>alt-*</code>可以展开通配符。</li>
</ul>
<h3 id="shell的变量"><a class="markdownIt-Anchor" href="#shell的变量"></a> shell的变量</h3>
<p><strong>字符串</strong>和<strong>数组</strong>是 Shell 中最常用的变量类型。<br />
如果变量类型是<strong>数组</strong>，我们可以通过以下方式遍历所有元素：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">names=(<span class="string">&quot;delkon&quot;</span> <span class="string">&quot;ikun&quot;</span> <span class="string">&quot;bob&quot;</span>)</span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;<span class="variable">$&#123;names[@]&#125;</span>&quot;</span></span><br><span class="line"><span class="comment"># delkon ikun bob</span></span><br><span class="line"><span class="comment"># 但是下面的命令只能索引到第一个元素</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;<span class="variable">$names</span>&quot;</span></span><br><span class="line">delkon</span><br></pre></td></tr></table></figure>
<p>使用<code>echo</code>命令可以读出变量<br />
<code>echo $&#123;variable&#125;</code>读出variable的值(variable是<font color="#c00000">字符串</font>)<br />
<code>echo $(command)</code>读出command的stdout<br />
<code>echo $((expression))</code>读出表达式的值</p>
<h3 id="系统自带的环境变量"><a class="markdownIt-Anchor" href="#系统自带的环境变量"></a> 系统自带的环境变量</h3>
<p>利用<code>etc</code>观察系统自带环境变量如下：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">HOSTNAME=611b47761989 # 这部主机的主机名称</span><br><span class="line">PWD=/ # 当前工作目录</span><br><span class="line">HOME=/root # 当前用户的主目录</span><br><span class="line">LS_COLORS=rs=0:di=01;34:ln=01;36:mh=00:pi=40;33:so=01;35:do=01;35:bd=40;33;01:cd=40;33;01:or=40;31;01:mi=00:su=37;41:sg=30;43:ca=00:tw=30;42:ow=34;42:st=37;44:ex=01;32:*.tar=01;31:*.tgz=01;31:*.arc=01;31:*.arj=01;31:*.taz=01;31:*.lha=01;31:*.lz4=01;31:*.lzh=01;31:*.lzma=01;31:*.tlz=01;31:*.txz=01;31:*.tzo=01;31:*.t7z=01;31:*.zip=01;31:*.z=01;31:*.dz=01;31:*.gz=01;31:*.lrz=01;31:*.lz=01;31:*.lzo=01;31:*.xz=01;31:*.zst=01;31:*.tzst=01;31:*.bz2=01;31:*.bz=01;31:*.tbz=01;31:*.tbz2=01;31:*.tz=01;31:*.deb=01;31:*.rpm=01;31:*.jar=01;31:*.war=01;31:*.ear=01;31:*.sar=01;31:*.rar=01;31:*.alz=01;31:*.ace=01;31:*.zoo=01;31:*.cpio=01;31:*.7z=01;31:*.rz=01;31:*.cab=01;31:*.wim=01;31:*.swm=01;31:*.dwm=01;31:*.esd=01;31:*.avif=01;35:*.jpg=01;35:*.jpeg=01;35:*.mjpg=01;35:*.mjpeg=01;35:*.gif=01;35:*.bmp=01;35:*.pbm=01;35:*.pgm=01;35:*.ppm=01;35:*.tga=01;35:*.xbm=01;35:*.xpm=01;35:*.tif=01;35:*.tiff=01;35:*.png=01;35:*.svg=01;35:*.svgz=01;35:*.mng=01;35:*.pcx=01;35:*.mov=01;35:*.mpg=01;35:*.mpeg=01;35:*.m2v=01;35:*.mkv=01;35:*.webm=01;35:*.webp=01;35:*.ogm=01;35:*.mp4=01;35:*.m4v=01;35:*.mp4v=01;35:*.vob=01;35:*.qt=01;35:*.nuv=01;35:*.wmv=01;35:*.asf=01;35:*.rm=01;35:*.rmvb=01;35:*.flc=01;35:*.avi=01;35:*.fli=01;35:*.flv=01;35:*.gl=01;35:*.dl=01;35:*.xcf=01;35:*.xwd=01;35:*.yuv=01;35:*.cgm=01;35:*.emf=01;35:*.ogv=01;35:*.ogx=01;35:*.aac=00;36:*.au=00;36:*.flac=00;36:*.m4a=00;36:*.mid=00;36:*.midi=00;36:*.mka=00;36:*.mp3=00;36:*.mpc=00;36:*.ogg=00;36:*.ra=00;36:*.wav=00;36:*.oga=00;36:*.opus=00;36:*.spx=00;36:*.xspf=00;36:*~=00;90:*#=00;90:*.bak=00;90:*.crdownload=00;90:*.dpkg-dist=00;90:*.dpkg-new=00;90:*.dpkg-old=00;90:*.dpkg-tmp=00;90:*.old=00;90:*.orig=00;90:*.part=00;90:*.rej=00;90:*.rpmnew=00;90:*.rpmorig=00;90:*.rpmsave=00;90:*.swp=00;90:*.tmp=00;90:*.ucf-dist=00;90:*.ucf-new=00;90:*.ucf-old=00;90:</span><br><span class="line">TERM=xterm # 这个终端机使用的环境是什么类型</span><br><span class="line">SHLVL=1 # 当前 shell 会话的深度层级</span><br><span class="line">PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin # 系统搜索可执行文件的路径列表</span><br><span class="line">_=/usr/bin/env # 这是最后一个执行命令的路径</span><br></pre></td></tr></table></figure>
<p>比较重要的除了上面介绍的之外还有一个RANDOM变量，在 BASH 的环境下，这个 RANDOM 变量的内容，介于 0~32767 之间，由<code>/dev/random</code>乱数器产生。</p>
<h3 id="bash内的自订变量"><a class="markdownIt-Anchor" href="#bash内的自订变量"></a> bash内的自订变量</h3>
<p>利用<code>set</code>命令观察bash内的其他变量如下：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">BASH=/bin/bash</span><br><span class="line">......</span><br><span class="line">BASH_ALIASES=()</span><br><span class="line">BASH_ARGC=([0]=&quot;0&quot;)</span><br><span class="line">BASH_ARGV=()</span><br><span class="line">BASH_CMDS=()</span><br><span class="line">BASH_LINENO=()</span><br><span class="line">BASH_LOADABLES_PATH=/usr/local/lib/bash:/usr/lib/bash:/opt/local/lib/bash:/usr/pkg/lib/bash:/opt/pkg/lib/bash:.</span><br><span class="line">BASH_SOURCE=()</span><br><span class="line">BASH_VERSINFO=([0]=&quot;5&quot; [1]=&quot;2&quot; [2]=&quot;21&quot; [3]=&quot;1&quot; [4]=&quot;release&quot; [5]=&quot;aarch64-unknown-linux-gnu&quot;)</span><br><span class="line">BASH_VERSION=&#x27;5.2.21(1)-release&#x27;</span><br><span class="line">COLUMNS=80</span><br><span class="line">DIRSTACK=()</span><br><span class="line">EUID=0</span><br><span class="line">GROUPS=()</span><br><span class="line">HISTCONTROL=ignoredups:ignorespace</span><br><span class="line">HISTFILE=/root/.bash_history</span><br><span class="line">HISTFILESIZE=2000</span><br><span class="line">HISTSIZE=1000</span><br><span class="line">HOME=/root</span><br><span class="line">HOSTNAME=611b47761989</span><br><span class="line">HOSTTYPE=aarch64</span><br><span class="line">IFS=$&#x27; \t\n&#x27;</span><br><span class="line">LINES=25</span><br><span class="line">......</span><br><span class="line">MACHTYPE=aarch64-unknown-linux-gnu</span><br><span class="line">MAILCHECK=60</span><br><span class="line">OPTERR=1</span><br><span class="line">OPTIND=1</span><br><span class="line">OSTYPE=linux-gnu</span><br><span class="line">PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin</span><br><span class="line">PIPESTATUS=([0]=&quot;0&quot;)</span><br><span class="line">PPID=0</span><br><span class="line">PS1=&#x27;\[\e]0;\u@\h: \w\a\]$&#123;debian_chroot:+($debian_chroot)&#125;\u@\h:\w\$ &#x27;</span><br><span class="line">PS2=&#x27;&gt; &#x27;</span><br><span class="line">PS4=&#x27;+ &#x27;</span><br><span class="line">PWD=/</span><br><span class="line">SHELL=/bin/bash</span><br><span class="line">SHELLOPTS=braceexpand:emacs:hashall:histexpand:history:interactive-comments:monitor</span><br><span class="line">SHLVL=1</span><br><span class="line">TERM=xterm</span><br><span class="line">UID=0</span><br><span class="line">_=clear</span><br></pre></td></tr></table></figure>
<p>上面的变量有一些比较重要，我们分而述之。</p>
<ol>
<li>PS1：（提示字符的设置）<br />
当我们每次按下<code>enter</code>键去执行某个指令后，最后要再次出现提示符时就会去读取这个变量值。</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">PS1=<span class="string">&#x27;\[\e]0;\u@\h: \w\a\]$&#123;debian_chroot:+($debian_chroot)&#125;\u@\h:\w\$ &#x27;</span></span><br></pre></td></tr></table></figure>
<p>其中<code>\[\e]0;...\a\]</code>: 这是一个 ANSI 转义序列，用于在支持的终端中设置窗口的标题或图标。<code>$&#123;debian_chroot:+($debian_chroot)&#125;</code>是条件表达式，用于在 Debian 或其他带有 chroot 环境时显示当前的 chroot 名称。<code>\u@\h</code>显示格式为 用户名@主机名，<code>\w</code>显示当前的工作目录，最后<code>\$</code>根据当前用户是否是超级用户显示不同的符号。还有一些其他比较有意思的符号，比如</p>
<ul>
<li><code>#</code> ： 下达的第几个指令。</li>
<li><code>\t</code> ：显示时间，为 24 小时格式的“HH:MM:SS”</li>
<li><code>\T</code> ：显示时间，为 12 小时格式的“HH:MM:SS”</li>
<li><code>\A</code> ：显示时间，为 24 小时格式的“HH:MM”</li>
<li><code>\@</code> ：显示时间，为 12 小时格式的“am/pm”样式</li>
</ul>
<ol start="2">
<li><code>$</code>：本shell的PID<br />
我们可以运行<code>echo $&#123;$&#125;</code>来展示本shell的PID号码，结果如下:</li>
</ol>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">51</span><br></pre></td></tr></table></figure>
<ol start="3">
<li><code>?</code>：上个执行指令的回传值<br />
如果成功的执行上个指令， 则会回传一个 0 值，如果执行过程发生错误，就会回传非为 0 的数值。具体情形如下所示：</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> cati</span><br></pre></td></tr></table></figure>
<p>结果为：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">bash: cd: cati: No such file or directory</span><br></pre></td></tr></table></figure>
<p>再运行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="variable">$&#123;?&#125;</span></span><br></pre></td></tr></table></figure>
<p>结果为：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">1</span><br></pre></td></tr></table></figure>
<h3 id="自订变量和环境变量的转换"><a class="markdownIt-Anchor" href="#自订变量和环境变量的转换"></a> 自订变量和环境变量的转换</h3>
<ul>
<li>自订变量转成环境变量<br />
采用<code>export</code>命令或<code>declare -x</code>：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> variable</span><br><span class="line"><span class="built_in">declare</span> -x variable</span><br></pre></td></tr></table></figure>
<ul>
<li>环境变量（由自订变量转换而成的环境变量）转成自订变量</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">declare</span> [-aixr] variable</span><br><span class="line">选项与参数：</span><br><span class="line">-a  ：将后面名为 variable 的变量定义成为阵列 （array） 类型</span><br><span class="line">-i  ：将后面名为 variable 的变量定义成为整数数字 （<span class="built_in">integer</span>） 类型</span><br><span class="line">-x  ：用法与 <span class="built_in">export</span> 一样，就是将后面的 variable 变成环境变量；</span><br><span class="line">-r  ：将变量设置成为 <span class="built_in">readonly</span> 类型，该变量不可被更改内容，也不能 <span class="built_in">unset</span></span><br></pre></td></tr></table></figure>
<p>采用<code>declare +x</code>:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">declare</span> +x variable</span><br></pre></td></tr></table></figure>
<h3 id="bash中与变量扩展方式"><a class="markdownIt-Anchor" href="#bash中与变量扩展方式"></a> bash中与变量扩展方式</h3>
<ul>
<li><code>#</code>：从字符串开头删除与pattern匹配的最短部分，并返回剩下的字符串。也可以用来获取变量的字符长度。<br />
语法：<code>$&#123;variable#pattern&#125;</code><br />
语法：<code>$&#123;#variable&#125;</code></li>
<li><code>##</code>：从字符串开头删除与pattern匹配的最长部分，并返回剩下的字符串。<br />
语法：<code>$&#123;variable##pattern&#125;</code></li>
</ul>
<p>具体示例如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">str=abc.defg.hijkl.txt</span><br><span class="line"><span class="built_in">echo</span> <span class="variable">$&#123;str#*.&#125;</span> <span class="comment"># defg.hijkl.txt</span></span><br><span class="line"><span class="built_in">echo</span> <span class="variable">$&#123;str##*.&#125;</span> <span class="comment"># txt</span></span><br></pre></td></tr></table></figure>
<ul>
<li><code>%</code>: 从字符串末尾删除与 pattern 匹配的最短部分，并返回剩下的字符串。<br />
语法：<code>$&#123;variable%pattern&#125;</code></li>
<li><code>%%</code>：从字符串末尾删除与 pattern 匹配的最短部分，并返回剩下的字符串。<br />
语法：<code>$&#123;variable%%pattern&#125;</code><br />
具体示例如下：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="variable">$&#123;str%.*&#125;</span> <span class="comment"># abc.defg.hijkl</span></span><br><span class="line"><span class="built_in">echo</span> <span class="variable">$&#123;str%%.*&#125;</span> <span class="comment"># abc</span></span><br></pre></td></tr></table></figure>
<ul>
<li><code>/</code>：将变量 variable 中第一个匹配 pattern 的部分替换为 replacement。<br />
语法：<code>$&#123;variable/pattern/replacement&#125;</code></li>
<li><code>//</code>：将变量 variable 中所有匹配 pattern 的部分替换为 replacement。<br />
语法：<code>$&#123;variable//pattern/replacement&#125;</code><br />
具体示例如下：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">fname=delkon-university-works</span><br><span class="line"><span class="built_in">echo</span> <span class="variable">$&#123;fname/-/_&#125;</span> <span class="comment"># delkon_university-works</span></span><br><span class="line"><span class="built_in">echo</span> <span class="variable">$&#123;fname//-/_&#125;</span> <span class="comment"># delkon_university_works</span></span><br></pre></td></tr></table></figure>
<ul>
<li><code>:-</code>：如果变量未定义或为空，则返回default值。<br />
语法：<code>$&#123;variable:-default&#125;</code></li>
<li><code>:=</code>：如果变量未定义或为空，则将其赋值为 default。<br />
语法：<code>$&#123;variable:=default&#125;</code></li>
<li><code>:+</code>：如果变量已定义且不为空，则返回 replacement，否则返回空字符串。<br />
语法：<code>$&#123;variable:+replacement&#125;</code></li>
<li><code>:?</code>：如果变量未定义或为空，则输出 error 消息并退出脚本。<br />
语法：<code>$&#123;variable:?error&#125;</code></li>
</ul>
<p>具体一些情况如下表格所示：</p>
<table>
<thead>
<tr>
<th>变量设置方式</th>
<th>str 没有设置</th>
<th>str 为空字串</th>
<th>str 已设置非为空字串</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>var=$&#123;str-expr&#125;</code></td>
<td><code>var=expr</code></td>
<td><code>var=</code></td>
<td><code>var=$str</code></td>
</tr>
<tr>
<td><code>var=$&#123;str:-expr&#125;</code></td>
<td><code>var=expr</code></td>
<td><code>var=expr</code></td>
<td><code>var=$str</code></td>
</tr>
<tr>
<td><code>var=$&#123;str+expr&#125;</code></td>
<td><code>var=</code></td>
<td><code>var=expr</code></td>
<td><code>var=expr</code></td>
</tr>
<tr>
<td><code>var=$&#123;str:+expr&#125;</code></td>
<td><code>var=</code></td>
<td><code>var=</code></td>
<td><code>var=expr</code></td>
</tr>
<tr>
<td><code>var=$&#123;str=expr&#125;</code></td>
<td><code>str=expr</code> <code>var=expr</code></td>
<td><code>str 不变 var=</code></td>
<td><code>str 不变</code> <code>var=$str</code></td>
</tr>
<tr>
<td><code>var=$&#123;str:=expr&#125;</code></td>
<td><code>str=expr</code> <code>var=expr</code></td>
<td><code>str=expr</code> <code>var=expr</code></td>
<td><code>str 不变</code> <code>var=$str</code></td>
</tr>
<tr>
<td><code>var=$&#123;str?expr&#125;</code></td>
<td><code>expr 输出至 stderr</code></td>
<td><code>var=</code></td>
<td><code>var=$str</code></td>
</tr>
<tr>
<td><code>var=$&#123;str:?expr&#125;</code></td>
<td><code>expr 输出至 stderr</code></td>
<td><code>expr 输出至 stderr</code></td>
<td><code>var=$str</code></td>
</tr>
</tbody>
</table>
<ul>
<li><code>$&#123;variable:offset&#125;</code>和<code>$&#123;variable:offset:length&#125;</code>用来从变量中提取子字符串。</li>
</ul>
<h3 id="bash配置"><a class="markdownIt-Anchor" href="#bash配置"></a> bash配置</h3>
<p><code>source</code>或<code>.</code>后接配置文件会立即使用该配置文件。</p>
]]></content>
      <categories>
        <category>Linux操作系统</category>
      </categories>
      <tags>
        <tag>Shell</tag>
        <tag>Linux</tag>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>PyTorch训练器配置</title>
    <url>/2025/03/14/2025-3-14-PyTorch%20Trainer%20Config/</url>
    <content><![CDATA[<p>在使用PyTorch进行深度学习模型搭建的时候，构建我们自己的模型训练器是必不可少的一步。如果你仅仅会使用<code>optimizer.zero_grad()</code>，<code>loss.backward()</code>，<code>optimizer.step()</code>三连来进行反向传播训练（就像之前的我一样），那么现在或许是时候来学习如何做出更加酷炫的训练器了…</p>
<span id="more"></span>
<h2 id="优化器optimizer系列"><a class="markdownIt-Anchor" href="#优化器optimizer系列"></a> 优化器（Optimizer）系列</h2>
<p>PyTorch的封装使得一个简易优化器的搭建变得十分简单，比如说建立一个Adam优化器，我们可以直接写出如下代码：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">optimizer = torch.optim.Adam(model.parameters(), lr=<span class="number">1e-5</span>)</span><br></pre></td></tr></table></figure>
<p>这样一个简易的Adam优化器就完成了。这里的model是我自己随便写着作实验所使用的玩具模型，具体实现如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">SimpleConv</span>(nn.Module):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="built_in">super</span>(SimpleConv, <span class="variable language_">self</span>).__init__()</span><br><span class="line">        <span class="variable language_">self</span>.conv = nn.Conv2d(<span class="number">3</span>, <span class="number">1</span>, <span class="number">3</span>, padding=<span class="number">1</span>)</span><br><span class="line">        <span class="variable language_">self</span>.bn = nn.BatchNorm2d(<span class="number">1</span>)</span><br><span class="line">        <span class="variable language_">self</span>.act = nn.ELU()</span><br><span class="line">        <span class="variable language_">self</span>.flatten = nn.Flatten()</span><br><span class="line">        <span class="variable language_">self</span>.fc = nn.Linear(<span class="number">16</span>, <span class="number">4</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>.fc(<span class="variable language_">self</span>.flatten(<span class="variable language_">self</span>.act(<span class="variable language_">self</span>.bn(<span class="variable language_">self</span>.conv(x)))))</span><br></pre></td></tr></table></figure>
<p>我们需要了解<code>optimizer</code>的一些属性比如<code>optimizer.param_groups</code>，要知道进行反向传播计算所需要的参数基本上都在这里。不妨看一下里面的内容：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">optimizer.param_groups</span><br></pre></td></tr></table></figure>
<p>输出结果为：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">[&#123;&#x27;params&#x27;: [Parameter containing:</span><br><span class="line">   tensor([[[[-0.1824,  0.0391, -0.0919],</span><br><span class="line">             [ 0.1336,  0.0415, -0.0073],</span><br><span class="line">             [ 0.1174,  0.0020, -0.0896]],</span><br><span class="line">   </span><br><span class="line">            [[-0.0493,  0.1663,  0.0991],</span><br><span class="line">             [ 0.1541,  0.0731,  0.0382],</span><br><span class="line">             [ 0.0245, -0.0701,  0.0841]],</span><br><span class="line">   </span><br><span class="line">            [[-0.0188,  0.0573,  0.0740],</span><br><span class="line">             [-0.0024, -0.0112, -0.0193],</span><br><span class="line">             [-0.1160,  0.1549,  0.1194]]]], requires_grad=True),</span><br><span class="line">   Parameter containing:</span><br><span class="line">   tensor([0.0944], requires_grad=True),</span><br><span class="line">   Parameter containing:</span><br><span class="line">   tensor([1.], requires_grad=True),</span><br><span class="line">   Parameter containing:</span><br><span class="line">   tensor([0.], requires_grad=True),</span><br><span class="line">   Parameter containing:</span><br><span class="line">   tensor([[ 0.1952,  0.1926, -0.1615,  0.1012,  0.0987, -0.1303,  0.1945, -0.2085,</span><br><span class="line">            -0.1188,  0.0407, -0.0926,  0.0139,  0.1961,  0.0196,  0.1596,  0.1035],</span><br><span class="line">           [ 0.2256, -0.1728, -0.0649, -0.2252, -0.1049,  0.1704,  0.0607, -0.1786,</span><br><span class="line">            -0.1384,  0.1639,  0.0035, -0.0320,  0.2407, -0.1826, -0.0959,  0.2487],</span><br><span class="line">           [-0.1408,  0.1796, -0.0666,  0.0469, -0.2406,  0.0904,  0.2499, -0.2015,</span><br><span class="line">             0.0664, -0.2087, -0.1013, -0.1080,  0.1512,  0.1545,  0.0384, -0.1783],</span><br><span class="line">           [ 0.2023, -0.1636,  0.1479, -0.1352,  0.1680,  0.0547, -0.0309, -0.0961,</span><br><span class="line">             0.1528,  0.0792,  0.1917, -0.1388,  0.0384, -0.0111,  0.1132, -0.0392]],</span><br><span class="line">          requires_grad=True),</span><br><span class="line">   Parameter containing:</span><br><span class="line">   tensor([-0.1700,  0.2120, -0.0687,  0.0041], requires_grad=True)],</span><br><span class="line">  &#x27;lr&#x27;: 1e-05,</span><br><span class="line">  &#x27;betas&#x27;: (0.9, 0.999),</span><br><span class="line">  &#x27;eps&#x27;: 1e-08,</span><br><span class="line">  &#x27;weight_decay&#x27;: 0,</span><br><span class="line">  &#x27;amsgrad&#x27;: False,</span><br><span class="line">  &#x27;maximize&#x27;: False,</span><br><span class="line">  &#x27;foreach&#x27;: None,</span><br><span class="line">  &#x27;capturable&#x27;: False,</span><br><span class="line">  &#x27;differentiable&#x27;: False,</span><br><span class="line">  &#x27;fused&#x27;: None&#125;]</span><br></pre></td></tr></table></figure>
<p>可以看出该输出类型是一个列表，列表的元素是字典，我们不妨称之为<code>param_group</code>。每个字典都有一系列关键字，我们所关注的关键字是<code>lr</code>和<code>weight_decay</code>。上面的优化器只有一个<code>param_group</code>，这个<code>param_group</code>中所包含的参数即含有权重又含有偏置还含有一系列其他的参数（在该例子中是含有<code>nn.BatchNorm</code>的参数）。我们可不可以对这些不同类型的参数分门别类放在不同的<code>param_group</code>中以供之后优化的时候我们针对它们使用不同的优化策略（实际上就是分门别类地设置<code>lr</code>和<code>weight_decay</code>），实践上我们通常不对偏置和归一化层的参数进行权重衰减，嵌入层参数可能也需要相较于权重更小的衰减值；而且我们对偏置的学习率可以预设的稍高一些（相较于权重）。</p>
<p>下面我们根据上面的情况来设计一个更好的优化器。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">better_optimizer</span>(<span class="params">model, name=<span class="string">&quot;Adam&quot;</span>, weight_lr=<span class="number">0.001</span>, bias_lr=<span class="number">0.1</span>, momentum=<span class="number">0.9</span>, decay=<span class="number">1e-5</span></span>):</span><br><span class="line">    <span class="comment"># g[0]：为包含常规权重的参数组添加权重衰减（decay）</span></span><br><span class="line">    <span class="comment"># g[1]：为归一化层的权重添加一个权重衰减为 0 的参数组，因为 Norm 层通常不需要权重衰减</span></span><br><span class="line">    <span class="comment"># g[2]：为偏置项添加一个权重衰减为 0 的参数组，因为偏置通常也不应用权重衰减</span></span><br><span class="line">    <span class="comment"># g[3]：为嵌入层添加一个权重衰减为 略低 的参数组，因为嵌入层通常要保留语义信息</span></span><br><span class="line">    g = [], [], [], [] <span class="comment"># optimizer parameter groups</span></span><br><span class="line">    n = <span class="built_in">tuple</span>(v <span class="keyword">for</span> k, v <span class="keyword">in</span> nn.__dict__.items() <span class="keyword">if</span> <span class="string">&quot;Norm&quot;</span> <span class="keyword">in</span> k) <span class="comment"># normalization layers, i.e. BatchNorm2d()</span></span><br><span class="line">    e = <span class="built_in">tuple</span>(v <span class="keyword">for</span> k, v <span class="keyword">in</span> nn.__dict__.items() <span class="keyword">if</span> <span class="string">&quot;Embedding&quot;</span> <span class="keyword">in</span> k) <span class="comment"># embedding layers, i.e. Embedding()</span></span><br><span class="line">    <span class="keyword">for</span> v <span class="keyword">in</span> model.modules():</span><br><span class="line">        <span class="keyword">for</span> p_name, p <span class="keyword">in</span> v.named_parameters(recurse=<span class="number">0</span>):</span><br><span class="line">            <span class="keyword">if</span> p_name == <span class="string">&quot;bias&quot;</span>: <span class="comment"># bias (no decay)</span></span><br><span class="line">                g[<span class="number">2</span>].append(p)</span><br><span class="line">            <span class="keyword">elif</span> p_name == <span class="string">&quot;weight&quot;</span> <span class="keyword">and</span> <span class="built_in">isinstance</span>(v, n): <span class="comment"># weight (no decay)</span></span><br><span class="line">                g[<span class="number">1</span>].append(p)</span><br><span class="line">            <span class="keyword">elif</span> p_name == <span class="string">&quot;weight&quot;</span> <span class="keyword">and</span> <span class="built_in">isinstance</span>(v, e): <span class="comment"># weight (less decay)</span></span><br><span class="line">	            g[<span class="number">3</span>].append(p)</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                g[<span class="number">0</span>].append(p) <span class="comment"># weight (with decay)</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> name == <span class="string">&quot;Adam&quot;</span>:</span><br><span class="line">        optimizer = torch.optim.Adam(g[<span class="number">2</span>], lr=bias_lr, betas=(momentum, <span class="number">0.999</span>)) <span class="comment"># adjust beta1 to momentum</span></span><br><span class="line">    <span class="keyword">elif</span> name == <span class="string">&quot;AdamW&quot;</span>:</span><br><span class="line">        optimizer = torch.optim.AdamW(g[<span class="number">2</span>], lr=bias_lr, betas=(momentum, <span class="number">0.999</span>), weight_decay=<span class="number">0.0</span>)</span><br><span class="line">    <span class="keyword">elif</span> name == <span class="string">&quot;RMSProp&quot;</span>:</span><br><span class="line">        optimizer = torch.optim.RMSprop(g[<span class="number">2</span>], lr=bias_lr, momentum=momentum)</span><br><span class="line">    <span class="keyword">elif</span> name == <span class="string">&quot;SGD&quot;</span>:</span><br><span class="line">        optimizer = torch.optim.SGD(g[<span class="number">2</span>], lr=bias_lr, momentum=momentum, nesterov=<span class="literal">True</span>)</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">raise</span> NotImplementedError(<span class="string">f&quot;Optimizer <span class="subst">&#123;name&#125;</span> not implemented.&quot;</span>)</span><br><span class="line">    </span><br><span class="line">    optimizer.add_param_group(&#123;<span class="string">&quot;params&quot;</span>: g[<span class="number">0</span>], <span class="string">&quot;weight_decay&quot;</span>: decay, <span class="string">&quot;lr&quot;</span>: weight_lr&#125;) <span class="comment"># add g0 with weight_decay, other keys are default value</span></span><br><span class="line">    optimizer.add_param_group(&#123;<span class="string">&quot;params&quot;</span>: g[<span class="number">1</span>], <span class="string">&quot;weight_decay&quot;</span>: <span class="number">0.0</span>, <span class="string">&quot;lr&quot;</span>: weight_lr&#125;) <span class="comment"># add g1 (Norm weights), other keys are default value</span></span><br><span class="line">    optimizer.add_param_group(&#123;<span class="string">&quot;params&quot;</span>: g[<span class="number">3</span>], <span class="string">&quot;weight_decay&quot;</span>: decay * <span class="number">1e-3</span>, <span class="string">&quot;lr&quot;</span>: weight_lr&#125;)</span><br><span class="line">    <span class="keyword">return</span> optimizer</span><br></pre></td></tr></table></figure>
<p>然后我们来看看针对我们上面的玩具模型我们的更好的优化器是如何表现的：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">model = SimpleConv()</span><br><span class="line">optimizer = better_optimizer(model)</span><br><span class="line">optimizer.param_groups</span><br></pre></td></tr></table></figure>
<p>输出为：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">[&#123;&#x27;params&#x27;: [Parameter containing:</span><br><span class="line">   tensor([-0.0329], requires_grad=True),</span><br><span class="line">   Parameter containing:</span><br><span class="line">   tensor([0.], requires_grad=True),</span><br><span class="line">   Parameter containing:</span><br><span class="line">   tensor([-0.1927,  0.1622, -0.2305, -0.0664], requires_grad=True)],</span><br><span class="line">  &#x27;lr&#x27;: 0.1,</span><br><span class="line">  &#x27;betas&#x27;: (0.9, 0.999),</span><br><span class="line">  &#x27;eps&#x27;: 1e-08,</span><br><span class="line">  &#x27;weight_decay&#x27;: 0,</span><br><span class="line">  &#x27;amsgrad&#x27;: False,</span><br><span class="line">  &#x27;maximize&#x27;: False,</span><br><span class="line">  &#x27;foreach&#x27;: None,</span><br><span class="line">  &#x27;capturable&#x27;: False,</span><br><span class="line">  &#x27;differentiable&#x27;: False,</span><br><span class="line">  &#x27;fused&#x27;: None&#125;,</span><br><span class="line"> &#123;&#x27;params&#x27;: [Parameter containing:</span><br><span class="line">   tensor([[[[ 0.1524,  0.0430,  0.0528],</span><br><span class="line">             [-0.0505, -0.1589, -0.0095],</span><br><span class="line">             [ 0.0396,  0.1596, -0.0442]],</span><br><span class="line">   </span><br><span class="line">            [[ 0.1700,  0.0073,  0.0427],</span><br><span class="line">             [ 0.1555,  0.0093, -0.0197],</span><br><span class="line">             [-0.0451,  0.0600,  0.0382]],</span><br><span class="line">   </span><br><span class="line">            [[-0.1472, -0.1074,  0.1445],</span><br><span class="line">             [ 0.1147,  0.1683,  0.0128],</span><br><span class="line">             [ 0.1591, -0.1426, -0.1424]]]], requires_grad=True),</span><br><span class="line">   Parameter containing:</span><br><span class="line">   tensor([[ 0.0932, -0.0430, -0.0116,  0.1545,  0.2393,  0.2054, -0.1988, -0.0746,</span><br><span class="line">            -0.1791,  0.0920,  0.2305,  0.1367, -0.0554, -0.0617, -0.1641,  0.1947],</span><br><span class="line">           [-0.1728,  0.1580,  0.2043,  0.0815,  0.0452, -0.2036, -0.1372, -0.1023,</span><br><span class="line">             0.0635, -0.1043, -0.0741,  0.0701, -0.1954, -0.2244,  0.0282, -0.1941],</span><br><span class="line">           [-0.0876,  0.0210,  0.2124,  0.2064,  0.2474, -0.1098, -0.1336, -0.0387,</span><br><span class="line">             0.1281,  0.0718,  0.0654, -0.1938,  0.0171,  0.2355,  0.0411,  0.0575],</span><br><span class="line">           [-0.0863, -0.1515, -0.0365, -0.0780,  0.0237,  0.0128,  0.1393, -0.1560,</span><br><span class="line">             0.1935,  0.0848, -0.1106,  0.0364,  0.0485,  0.1788,  0.1589,  0.0800]],</span><br><span class="line">          requires_grad=True)],</span><br><span class="line">  &#x27;weight_decay&#x27;: 1e-05,</span><br><span class="line">  &#x27;lr&#x27;: 0.001,</span><br><span class="line">  &#x27;betas&#x27;: (0.9, 0.999),</span><br><span class="line">  &#x27;eps&#x27;: 1e-08,</span><br><span class="line">  &#x27;amsgrad&#x27;: False,</span><br><span class="line">  &#x27;maximize&#x27;: False,</span><br><span class="line">  &#x27;foreach&#x27;: None,</span><br><span class="line">  &#x27;capturable&#x27;: False,</span><br><span class="line">  &#x27;differentiable&#x27;: False,</span><br><span class="line">  &#x27;fused&#x27;: None&#125;,</span><br><span class="line"> &#123;&#x27;params&#x27;: [Parameter containing:</span><br><span class="line">   tensor([1.], requires_grad=True)],</span><br><span class="line">  &#x27;weight_decay&#x27;: 0.0,</span><br><span class="line">  &#x27;lr&#x27;: 0.001,</span><br><span class="line">  &#x27;betas&#x27;: (0.9, 0.999),</span><br><span class="line">  &#x27;eps&#x27;: 1e-08,</span><br><span class="line">  &#x27;amsgrad&#x27;: False,</span><br><span class="line">  &#x27;maximize&#x27;: False,</span><br><span class="line">  &#x27;foreach&#x27;: None,</span><br><span class="line">  &#x27;capturable&#x27;: False,</span><br><span class="line">  &#x27;differentiable&#x27;: False,</span><br><span class="line">  &#x27;fused&#x27;: None&#125;,</span><br><span class="line"> &#123;&#x27;params&#x27;: [],</span><br><span class="line">  &#x27;weight_decay&#x27;: 1e-08,</span><br><span class="line">  &#x27;lr&#x27;: 0.001,</span><br><span class="line">  &#x27;betas&#x27;: (0.9, 0.999),</span><br><span class="line">  &#x27;eps&#x27;: 1e-08,</span><br><span class="line">  &#x27;amsgrad&#x27;: False,</span><br><span class="line">  &#x27;maximize&#x27;: False,</span><br><span class="line">  &#x27;foreach&#x27;: None,</span><br><span class="line">  &#x27;capturable&#x27;: False,</span><br><span class="line">  &#x27;differentiable&#x27;: False,</span><br><span class="line">  &#x27;fused&#x27;: None&#125;]</span><br></pre></td></tr></table></figure>
<p>很明显，达到我们分门别类放置权重、偏置、归一化层、嵌入层的<code>lr</code>和<code>weight_decay</code>的要求啦。</p>
<h2 id="学习率调度器系列"><a class="markdownIt-Anchor" href="#学习率调度器系列"></a> 学习率调度器系列</h2>
<p>还在苦苦使用固定学习率来进行模型训练吗？不少研究表明阶梯学习率调度更加适合大型卷积神经网络，而先预热再进行余弦调度更加适合Transformer模型的训练。单纯调用<code>torch.optim.lr_scheduler</code>模块中的函数也可以，但是有些时候它们无法满足我们的需求，比如说我上面自定义的<code>better_optimizer</code>所构造的<code>optimizer</code>。因此我们要自定义学习率调度器！！！</p>
<p>自定义学习率调度器我们需要继承<code>torch.optim.lr_scheduler._LRScheduler</code>模块，下面我们来自定义先预热再进行余弦调度的学习率调度器:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">get_position_from_periods</span>(<span class="params">iteration, cumulative_period</span>):</span><br><span class="line">	<span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">	Get the position from a period list.</span></span><br><span class="line"><span class="string">	</span></span><br><span class="line"><span class="string">	It will return the index of the right-closest number in the period list.</span></span><br><span class="line"><span class="string">	For example, the cumulative_period = [100, 200, 300, 400], </span></span><br><span class="line"><span class="string">	if iteration == 50, return 0;</span></span><br><span class="line"><span class="string">	if iteration == 210, return 2;</span></span><br><span class="line"><span class="string">	if iteration == 300, return 2</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">	Args: </span></span><br><span class="line"><span class="string">		iteration (int): Current iteration.</span></span><br><span class="line"><span class="string">		cumulative_period (list[int]): Cumulative period list.</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">	Returns:</span></span><br><span class="line"><span class="string">		int: The position of the right-closest number in the period list.</span></span><br><span class="line"><span class="string">	&quot;&quot;&quot;</span></span><br><span class="line">	<span class="keyword">for</span> i, period <span class="keyword">in</span> <span class="built_in">enumerate</span>(cumulative_period):</span><br><span class="line">		<span class="keyword">if</span> iteration &lt;= period:</span><br><span class="line">			<span class="keyword">return</span> i</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">WarmingUpCosineAnnealingRestartLR</span>(<span class="title class_ inherited__">_LRScheduler</span>):</span><br><span class="line">	<span class="string">&quot;&quot;&quot; </span></span><br><span class="line"><span class="string">	First warming up then cosine annealing with restarts learning rate scheme.</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">	An example of config:</span></span><br><span class="line"><span class="string">	period = [10, 40, 150]</span></span><br><span class="line"><span class="string">	restart_weights = [1, 0.1]</span></span><br><span class="line"><span class="string">	eta_min = 1e-7</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">	It has three periods, At 0~9, 10~49, 50~199, the scheduler</span></span><br><span class="line"><span class="string">	will restart with the weights in restart weights at 10th and 50th. </span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">	Args: </span></span><br><span class="line"><span class="string">		optimizer (torch.nn.optimizer)</span></span><br><span class="line"><span class="string">		periods (list): periods[0] warming up cycle, periods[1:] cosine cycle.</span></span><br><span class="line"><span class="string">		restart_weights (list): Restart weights at each cosine restart iteration. Default: [1]</span></span><br><span class="line"><span class="string">		eta_min (float): The minimum lr. Default = 0.</span></span><br><span class="line"><span class="string">		last_epoch (int): Used in _LRScheduler. Default: -1</span></span><br><span class="line"><span class="string">	&quot;&quot;&quot;</span></span><br><span class="line">	<span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, optimizer, periods, warmingup_lr, </span></span><br><span class="line"><span class="params">			  restart_weights=[<span class="number">1</span>], eta_min=<span class="number">0</span>, last_epoch=-<span class="number">1</span></span>):</span><br><span class="line">		<span class="variable language_">self</span>.periods = periods</span><br><span class="line">		<span class="variable language_">self</span>.warmingup_lr = warmingup_lr</span><br><span class="line">		<span class="variable language_">self</span>.restart_weights = restart_weights</span><br><span class="line">		<span class="variable language_">self</span>.eta_min = eta_min</span><br><span class="line">		<span class="keyword">assert</span> (<span class="built_in">len</span>(<span class="variable language_">self</span>.periods) - <span class="number">1</span> == <span class="built_in">len</span>(</span><br><span class="line">			<span class="variable language_">self</span>.restart_weights</span><br><span class="line">		)), <span class="string">&quot;Periods and restart_weights should have the same length.&quot;</span></span><br><span class="line">		<span class="variable language_">self</span>.cumulative_period = [<span class="built_in">sum</span>(<span class="variable language_">self</span>.periods[<span class="number">0</span>:i+<span class="number">1</span>]) <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="built_in">len</span>(<span class="variable language_">self</span>.periods))]</span><br><span class="line">		<span class="built_in">super</span>().__init__(optimizer, last_epoch)</span><br><span class="line"></span><br><span class="line">	<span class="keyword">def</span> <span class="title function_">get_lr</span>(<span class="params">self</span>):</span><br><span class="line">		<span class="comment"># return的结果是在self.last_epoch中每一个param_group的lr所组成的列表</span></span><br><span class="line">		<span class="comment"># self.base_lrs = [group[&#x27;initial_lr&#x27;] for group in optimizer.param_groups]</span></span><br><span class="line">		idx = get_position_from_periods(<span class="variable language_">self</span>.last_epoch, <span class="variable language_">self</span>.cumulative_period)</span><br><span class="line">		<span class="keyword">if</span> idx == <span class="number">0</span>:</span><br><span class="line">			<span class="keyword">return</span> [</span><br><span class="line">				base_lr - <span class="variable language_">self</span>.last_epoch / <span class="variable language_">self</span>.periods[<span class="number">0</span>] * (base_lr - <span class="variable language_">self</span>.warmingup_lr) </span><br><span class="line">				<span class="keyword">if</span> i == <span class="number">0</span></span><br><span class="line">				<span class="keyword">else</span> base_lr + <span class="variable language_">self</span>.last_epoch / <span class="variable language_">self</span>.periods[<span class="number">0</span>] * (<span class="variable language_">self</span>.warmingup_lr - base_lr)</span><br><span class="line">				<span class="keyword">for</span> i, base_lr <span class="keyword">in</span> <span class="built_in">enumerate</span>(<span class="variable language_">self</span>.base_lrs)</span><br><span class="line">			]</span><br><span class="line">		<span class="keyword">else</span>:</span><br><span class="line">			current_weight = <span class="variable language_">self</span>.restart_weights[idx - <span class="number">1</span>]</span><br><span class="line">			nearest_restart = <span class="variable language_">self</span>.cumulative_period[idx - <span class="number">1</span>]</span><br><span class="line">			current_period = <span class="variable language_">self</span>.periods[idx]</span><br><span class="line">			<span class="keyword">return</span> [</span><br><span class="line">				<span class="variable language_">self</span>.eta_min + current_weight * <span class="number">0.5</span> * (base_lr - <span class="variable language_">self</span>.eta_min) * </span><br><span class="line">				(<span class="number">1</span> + math.cos(math.pi * ((<span class="variable language_">self</span>.last_epoch - nearest_restart) / current_period)))</span><br><span class="line">				<span class="keyword">for</span> base_lr <span class="keyword">in</span> [<span class="variable language_">self</span>.warmingup_lr] * <span class="built_in">len</span>(<span class="variable language_">self</span>.base_lrs)</span><br><span class="line">			]</span><br></pre></td></tr></table></figure>
<p>我们可以看到，对学习率的是如何调度主要封装在<code>torch.optim.lr_scheduler._LRScheduler</code>的<code>get_lr</code>方法中，在该方法中我们返回的结果是在<code>self.last_epoch</code>中<code>optimizer</code>中的每一个<code>param_group</code>的<code>lr</code>关键字所对应的值所组成的列表。</p>
<p>下面对该调度器进行测试</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">scheduler = WarmingUpCosineAnnealingRestartLR(optimizer, periods=[<span class="number">2</span>, <span class="number">4</span>, <span class="number">4</span>], warmingup_lr=<span class="number">0.01</span>, restart_weights=[<span class="number">1</span>, <span class="number">0.1</span>])</span><br><span class="line"></span><br><span class="line">weight_lr_list = []</span><br><span class="line">bias_lr_list = []</span><br><span class="line"></span><br><span class="line">weight_lr_list = []</span><br><span class="line">bias_lr_list = []</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> epoch <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">10</span>):</span><br><span class="line">	<span class="keyword">for</span> i, _ <span class="keyword">in</span> <span class="built_in">enumerate</span>(<span class="built_in">range</span>(<span class="number">100</span>)):</span><br><span class="line">		<span class="keyword">if</span> i == <span class="number">0</span>:</span><br><span class="line">			bias_current_lr = optimizer.param_groups[<span class="number">0</span>][<span class="string">&#x27;lr&#x27;</span>]</span><br><span class="line">			weight_current_lr = optimizer.param_groups[<span class="number">1</span>][<span class="string">&#x27;lr&#x27;</span>]</span><br><span class="line">			<span class="built_in">print</span>(<span class="string">f&quot;Epoch <span class="subst">&#123;epoch&#125;</span> at stage <span class="subst">&#123;i&#125;</span>: Current weight learning rate = <span class="subst">&#123;weight_current_lr&#125;</span>&quot;</span>)</span><br><span class="line">			<span class="built_in">print</span>(<span class="string">f&quot;Epoch <span class="subst">&#123;epoch&#125;</span> at stage <span class="subst">&#123;i&#125;</span>: Current bias learning rate = <span class="subst">&#123;bias_current_lr&#125;</span>&quot;</span>)</span><br><span class="line">			<span class="comment"># print(f&quot;Optimizer State: &#123;optimizer.state&#125;&quot;)</span></span><br><span class="line">		<span class="keyword">if</span> i == <span class="number">49</span>:</span><br><span class="line">			bias_current_lr = optimizer.param_groups[<span class="number">0</span>][<span class="string">&#x27;lr&#x27;</span>]</span><br><span class="line">			weight_current_lr = optimizer.param_groups[<span class="number">1</span>][<span class="string">&#x27;lr&#x27;</span>]</span><br><span class="line">			<span class="built_in">print</span>(<span class="string">f&quot;Epoch <span class="subst">&#123;epoch&#125;</span> at stage <span class="subst">&#123;i&#125;</span>: Current weight learning rate = <span class="subst">&#123;weight_current_lr&#125;</span>&quot;</span>)</span><br><span class="line">			<span class="built_in">print</span>(<span class="string">f&quot;Epoch <span class="subst">&#123;epoch&#125;</span> at stage <span class="subst">&#123;i&#125;</span>: Current bias learning rate = <span class="subst">&#123;bias_current_lr&#125;</span>&quot;</span>)</span><br><span class="line">			<span class="comment"># print(f&quot;Optimizer State: &#123;optimizer.state&#125;&quot;)</span></span><br><span class="line">		<span class="keyword">if</span> i == <span class="number">99</span>:</span><br><span class="line">			bias_current_lr = optimizer.param_groups[<span class="number">0</span>][<span class="string">&#x27;lr&#x27;</span>]</span><br><span class="line">			weight_current_lr = optimizer.param_groups[<span class="number">1</span>][<span class="string">&#x27;lr&#x27;</span>]</span><br><span class="line">			<span class="built_in">print</span>(<span class="string">f&quot;Epoch <span class="subst">&#123;epoch&#125;</span> at stage <span class="subst">&#123;i&#125;</span>: Current weight learning rate = <span class="subst">&#123;weight_current_lr&#125;</span>&quot;</span>)</span><br><span class="line">			<span class="built_in">print</span>(<span class="string">f&quot;Epoch <span class="subst">&#123;epoch&#125;</span> at stage <span class="subst">&#123;i&#125;</span>: Current bias learning rate = <span class="subst">&#123;bias_current_lr&#125;</span>&quot;</span>)</span><br><span class="line">			<span class="comment"># print(f&quot;Optimizer State: &#123;optimizer.state&#125;&quot;)</span></span><br><span class="line">		scheduler.step(epoch + (i + <span class="number">1</span>) / <span class="number">100</span>)</span><br><span class="line">		weight_lr_list.append(optimizer.param_groups[<span class="number">1</span>][<span class="string">&#x27;lr&#x27;</span>])</span><br><span class="line">		bias_lr_list.append(optimizer.param_groups[<span class="number">0</span>][<span class="string">&#x27;lr&#x27;</span>])</span><br></pre></td></tr></table></figure>
<p>输出为：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">Epoch 0 at stage 0: Current weight learning rate = 0.0</span><br><span class="line">Epoch 0 at stage 0: Current bias learning rate = 0.0</span><br><span class="line">Epoch 0 at stage 49: Current weight learning rate = 0.0032050000000000004</span><br><span class="line">Epoch 0 at stage 49: Current bias learning rate = 0.07795</span><br><span class="line">Epoch 0 at stage 99: Current weight learning rate = 0.005455000000000001</span><br><span class="line">Epoch 0 at stage 99: Current bias learning rate = 0.05545</span><br><span class="line">Epoch 1 at stage 0: Current weight learning rate = 0.0055000000000000005</span><br><span class="line">Epoch 1 at stage 0: Current bias learning rate = 0.055</span><br><span class="line">Epoch 1 at stage 49: Current weight learning rate = 0.007705000000000001</span><br><span class="line">Epoch 1 at stage 49: Current bias learning rate = 0.03294999999999999</span><br><span class="line">Epoch 1 at stage 99: Current weight learning rate = 0.009955000000000002</span><br><span class="line">Epoch 1 at stage 99: Current bias learning rate = 0.010450000000000001</span><br><span class="line">Epoch 2 at stage 0: Current weight learning rate = 0.010000000000000002</span><br><span class="line">Epoch 2 at stage 0: Current bias learning rate = 0.009999999999999995</span><br><span class="line">Epoch 2 at stage 49: Current weight learning rate = 0.009634282978200603</span><br><span class="line">Epoch 2 at stage 49: Current bias learning rate = 0.009634282978200603</span><br><span class="line">Epoch 2 at stage 99: Current weight learning rate = 0.008563192594626026</span><br><span class="line">Epoch 2 at stage 99: Current bias learning rate = 0.008563192594626026</span><br><span class="line">Epoch 3 at stage 0: Current weight learning rate = 0.008535533905932738</span><br><span class="line">Epoch 3 at stage 0: Current bias learning rate = 0.008535533905932738</span><br><span class="line">Epoch 3 at stage 49: Current weight learning rate = 0.00694963843894094</span><br><span class="line">Epoch 3 at stage 49: Current bias learning rate = 0.00694963843894094</span><br><span class="line">Epoch 3 at stage 99: Current weight learning rate = 0.005039269504443555</span><br><span class="line">Epoch 3 at stage 99: Current bias learning rate = 0.005039269504443555</span><br><span class="line">Epoch 4 at stage 0: Current weight learning rate = 0.005</span><br><span class="line">Epoch 4 at stage 0: Current bias learning rate = 0.005</span><br><span class="line">Epoch 4 at stage 49: Current weight learning rate = 0.003122922143873584</span><br><span class="line">Epoch 4 at stage 49: Current bias learning rate = 0.003122922143873584</span><br><span class="line">Epoch 4 at stage 99: Current weight learning rate = 0.0014923428711457215</span><br><span class="line">Epoch 4 at stage 99: Current bias learning rate = 0.0014923428711457215</span><br><span class="line">Epoch 5 at stage 0: Current weight learning rate = 0.0014644660940672626</span><br><span class="line">Epoch 5 at stage 0: Current bias learning rate = 0.0014644660940672626</span><br><span class="line">Epoch 5 at stage 49: Current weight learning rate = 0.00039577259929486907</span><br><span class="line">Epoch 5 at stage 49: Current bias learning rate = 0.00039577259929486907</span><br><span class="line">Epoch 5 at stage 99: Current weight learning rate = 1.5421177605168258e-07</span><br><span class="line">Epoch 5 at stage 99: Current bias learning rate = 1.5421177605168258e-07</span><br><span class="line">Epoch 6 at stage 0: Current weight learning rate = 0.0</span><br><span class="line">Epoch 6 at stage 0: Current bias learning rate = 0.0</span><br><span class="line">Epoch 6 at stage 49: Current weight learning rate = 0.0009634282978200604</span><br><span class="line">Epoch 6 at stage 49: Current bias learning rate = 0.0009634282978200604</span><br><span class="line">Epoch 6 at stage 99: Current weight learning rate = 0.0008563192594626027</span><br><span class="line">Epoch 6 at stage 99: Current bias learning rate = 0.0008563192594626027</span><br><span class="line">Epoch 7 at stage 0: Current weight learning rate = 0.0008535533905932737</span><br><span class="line">Epoch 7 at stage 0: Current bias learning rate = 0.0008535533905932737</span><br><span class="line">Epoch 7 at stage 49: Current weight learning rate = 0.0006949638438940941</span><br><span class="line">Epoch 7 at stage 49: Current bias learning rate = 0.0006949638438940941</span><br><span class="line">Epoch 7 at stage 99: Current weight learning rate = 0.0005039269504443555</span><br><span class="line">Epoch 7 at stage 99: Current bias learning rate = 0.0005039269504443555</span><br><span class="line">Epoch 8 at stage 0: Current weight learning rate = 0.0005</span><br><span class="line">Epoch 8 at stage 0: Current bias learning rate = 0.0005</span><br><span class="line">Epoch 8 at stage 49: Current weight learning rate = 0.0003122922143873584</span><br><span class="line">Epoch 8 at stage 49: Current bias learning rate = 0.0003122922143873584</span><br><span class="line">Epoch 8 at stage 99: Current weight learning rate = 0.00014923428711457215</span><br><span class="line">Epoch 8 at stage 99: Current bias learning rate = 0.00014923428711457215</span><br><span class="line">Epoch 9 at stage 0: Current weight learning rate = 0.00014644660940672628</span><br><span class="line">Epoch 9 at stage 0: Current bias learning rate = 0.00014644660940672628</span><br><span class="line">Epoch 9 at stage 49: Current weight learning rate = 3.957725992948691e-05</span><br><span class="line">Epoch 9 at stage 49: Current bias learning rate = 3.957725992948691e-05</span><br><span class="line">Epoch 9 at stage 99: Current weight learning rate = 1.5421177605168258e-08</span><br><span class="line">Epoch 9 at stage 99: Current bias learning rate = 1.5421177605168258e-08</span><br><span class="line">/Users/delkon/miniconda3/lib/python3.10/site-packages/torch/optim/lr_scheduler.py:152: UserWarning: The epoch parameter in `scheduler.step()` was not necessary and is being deprecated where possible. Please use `scheduler.step()` to step the scheduler. During the deprecation, if epoch is different from None, the closed form is used instead of the new chainable form, where available. Please open an issue if you are unable to replicate your use case: https://github.com/pytorch/pytorch/issues/new/choose.</span><br><span class="line">  warnings.warn(EPOCH_DEPRECATION_WARNING, UserWarning)</span><br></pre></td></tr></table></figure>
<p>可以看到按照上面的学习率你的意愿先预热在进行余弦调度。可视化结果如下:<br />
<img data-src="../../codes/pytorch_trainer/scheduler.png" alt="image" /></p>
<h2 id="损失优化系列"><a class="markdownIt-Anchor" href="#损失优化系列"></a> 损失优化系列</h2>
<p>有时候会因为只有单个GPU，我们训练的时候所能够设置单次训练的批次很少（否则就会<code>out of memory</code>）。但这种时候由于批次太小训练有时候会不太稳定，我们可以使用累加梯度优化来达到近似大批量训练的效果。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">last_opt_step = -<span class="number">1</span></span><br><span class="line"><span class="comment"># nbs是我们打算近似的大批量，一半是batch_size的倍数</span></span><br><span class="line">accumulate = <span class="built_in">max</span>(<span class="built_in">round</span>(nbs / batch_size), <span class="number">1</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> epoch <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>, epochs + <span class="number">1</span>):</span><br><span class="line">	model.train()</span><br><span class="line">	optimizer.zero_grad()</span><br><span class="line">	<span class="keyword">for</span> i, (X, y) <span class="keyword">in</span> <span class="built_in">enumerate</span>(train_loader):</span><br><span class="line">		ni = i + <span class="built_in">len</span>(train_loader) * epoch</span><br><span class="line">		<span class="comment"># Forward</span></span><br><span class="line">		X, y = X.to(device), y.to(device)</span><br><span class="line">		logits = model(X)	</span><br><span class="line">		loss = nn.functional.cross_entropy(logits, y)</span><br><span class="line">		<span class="comment"># Backward</span></span><br><span class="line">		loss.backward()</span><br><span class="line">		<span class="comment"># Optimize</span></span><br><span class="line">		<span class="keyword">if</span> ni - last_opt_step &gt;= accumulate:</span><br><span class="line">			optimizer.step()</span><br><span class="line">			optimizer.zero_grad()</span><br><span class="line">			last_opt_step = ni</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Deep Learning Programming</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>DL</tag>
      </tags>
  </entry>
  <entry>
    <title></title>
    <url>/2025/03/16/2025-3-16-Make%20and%20CMake/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>Git详解</title>
    <url>/2025/03/07/2025-3-7-git/</url>
    <content><![CDATA[<p>这篇博客主要自底向上地介绍Git命令行，有时甚至会使用一些<code>python</code>代码来对一些Git的功能进行更加详细的分析。我一直认为基础一旦打好，那么一些更高级的用法也可以循序渐进地了解，而且是更加透彻的了解。不是像博主一开始那样以为只要死记硬背<code>git add</code>，<code>git commit</code>,<code>git pull</code>，<code>git clone</code>等常见命令的一些用法就可以了😭（实际上网上大多数教程就是这样教的…），在这上面走了不少弯路，所以从头开始打地基写作此文留以警示⚠️。</p>
<span id="more"></span>
<h2 id="git仓库git-repository"><a class="markdownIt-Anchor" href="#git仓库git-repository"></a> Git仓库（Git Repository）</h2>
<p>要学习Git，首先我们要知道我们运行Git命令是对什么对象进行操作的，在Git的相关术语中，我们把这个对象叫做Git仓库（Git Repository），Git仓库实际上就是一个文件夹，在这个文件夹里我们通过.git文件夹（隐藏文件夹）对Git仓库中的文件（非隐藏文件夹）内容的改变进行记录，当然并不是所有非隐藏文件夹中文件的改变都会被记录，被.gitingore文件所标识的文件被改变后.git文件夹不会对其进行记录。根据我上面所说，<strong>Git操作最隐晦的地方就在于对于.git文件夹中内容的修改来保证对于Git仓库非隐藏文件夹中文件内容修改的记录</strong>。</p>
<p>根据上面的描述，我们可以得到一下简略的公式：</p>
<p class='katex-block'><span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mtable width="100%"><mtr><mtd width="50%"></mtd><mtd><mrow><mi>G</mi><mi>i</mi><mi>t</mi><mtext> </mtext><mi>R</mi><mi>e</mi><mi>p</mi><mi>o</mi><mi>s</mi><mi>i</mi><mi>t</mi><mi>o</mi><mi>r</mi><mi>y</mi><mo>=</mo><mi mathvariant="normal">.</mi><mi>g</mi><mi>i</mi><mi>t</mi><mtext>文件夹</mtext><mo>+</mo><mtext>非隐藏文件夹</mtext></mrow></mtd><mtd width="50%"></mtd><mtd><mtext>(1)</mtext></mtd></mtr></mtable><annotation encoding="application/x-tex">Git\ Repository = .git文件夹 + 非隐藏文件夹\tag{1}
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.8777699999999999em;vertical-align:-0.19444em;"></span><span class="mord mathnormal">G</span><span class="mord mathnormal">i</span><span class="mord mathnormal">t</span><span class="mspace"> </span><span class="mord mathnormal" style="margin-right:0.00773em;">R</span><span class="mord mathnormal">e</span><span class="mord mathnormal">p</span><span class="mord mathnormal">o</span><span class="mord mathnormal">s</span><span class="mord mathnormal">i</span><span class="mord mathnormal">t</span><span class="mord mathnormal">o</span><span class="mord mathnormal" style="margin-right:0.02778em;">r</span><span class="mord mathnormal" style="margin-right:0.03588em;">y</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span></span><span class="base"><span class="strut" style="height:0.8777699999999999em;vertical-align:-0.19444em;"></span><span class="mord">.</span><span class="mord mathnormal" style="margin-right:0.03588em;">g</span><span class="mord mathnormal">i</span><span class="mord mathnormal">t</span><span class="mord cjk_fallback">文</span><span class="mord cjk_fallback">件</span><span class="mord cjk_fallback">夹</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span><span class="mbin">+</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span></span><span class="base"><span class="strut" style="height:0.68333em;vertical-align:0em;"></span><span class="mord cjk_fallback">非</span><span class="mord cjk_fallback">隐</span><span class="mord cjk_fallback">藏</span><span class="mord cjk_fallback">文</span><span class="mord cjk_fallback">件</span><span class="mord cjk_fallback">夹</span></span><span class="tag"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mord text"><span class="mord">(</span><span class="mord"><span class="mord">1</span></span><span class="mord">)</span></span></span></span></span></span></p>
<p>更加详尽的来说以<code>python</code>来构建Git仓库类：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">GitRepository</span> (<span class="title class_ inherited__">object</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;A git repository&quot;&quot;&quot;</span></span><br><span class="line"></span><br><span class="line">    worktree = <span class="literal">None</span></span><br><span class="line">    gitdir = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, path</span>):</span><br><span class="line">        <span class="variable language_">self</span>.worktree = path</span><br><span class="line">        <span class="variable language_">self</span>.gitdir = os.path.join(path, <span class="string">&quot;.git&quot;</span>)</span><br></pre></td></tr></table></figure>
<h2 id="git文件夹内部结构探究"><a class="markdownIt-Anchor" href="#git文件夹内部结构探究"></a> .git文件夹内部结构探究</h2>
<p>前面我们已经知道了<strong>Git操作最隐晦的地方就在于对于.git文件夹中内容的修改来保证对于Git仓库非隐藏文件夹中文件内容修改的记录</strong>，那接下来我们就要对此进行脱贫攻坚了。由于.git是隐藏文件夹，所以我们要先对.git文件夹中的文件分布有一定的了解，<code>git init</code>初始化后的.git文件夹中的文件分布如图1所示，此时Git仓库内除了.git文件夹之外一片荒芜。</p>
<p><img data-src="../../codes/git/gitinit.png" alt="我的图片" /></p>
<center>
<strong>图1：运行git init之后.git文件夹的文件分布结构</strong>
</center>
<p>然后我们再本地Git仓库中添加可见文件，添加后的结果如图2所示。</p>
<p><img data-src="../../codes/git/add.png" alt="image" /><br />
<left><br />
<strong>图2：Git仓库添加文件后的文件分布</strong><br />
</left></p>
<p>将上述所有文件运行<code>git add</code>之后在进行<code>git commit</code>观察.git文件夹中的文件分布，如图3所示。可以看到相较于最初的.git文件，我们在<code>git add</code>和<code>git commit</code>之后文件夹中多出了<code>index</code>文件，<code>log</code>文件夹，<code>objects</code>文件夹中的8个文件以及<code>refs/heads</code>下的<code>main</code>文件，其他的一些变化我们不做考虑。<strong>有果导因我们可以知道.git文件夹内部的这些变化中有对于Git仓库非隐藏文件夹文件修改的记录。</strong></p>
<p><img data-src="../../codes/git/gitaddcommit.png" alt="image" /></p>
<center>
<strong>图3：运行git add和git commit之后.git文件夹的文件分布结构</strong>
</center>
<h2 id="git对象git-object"><a class="markdownIt-Anchor" href="#git对象git-object"></a> Git对象（Git Object）</h2>
<p>上面的变化究竟是如何实现的呢？要知道Git中的几乎一切都被存储为Git对象！也就是说我们可以通过Git对象进行操作来完成上面的变化。说了这么多，让我们来为Git对象下一个比较官方的定义：<strong>Git对象就是在 Git 仓库中的文件，它们的路径由它们的内容决定。</strong></p>
<p>Git对象范性可以借由<code>python</code>来实现：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">GitObject</span> (<span class="title class_ inherited__">object</span>):</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, data=<span class="literal">None</span></span>):</span><br><span class="line">        <span class="keyword">if</span> data != <span class="literal">None</span>:</span><br><span class="line">            <span class="variable language_">self</span>.deserialize(data)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="variable language_">self</span>.init()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">serialize</span>(<span class="params">self</span>):</span><br><span class="line">	    <span class="string">&quot;&quot;&quot;将对象转变为zip文件解压缩后的byte格式&quot;&quot;&quot;</span></span><br><span class="line">        <span class="keyword">raise</span> Exception(<span class="string">&quot;Unimplemented!&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">deserialize</span>(<span class="params">self, data</span>):</span><br><span class="line">	    <span class="string">&quot;&quot;&quot;data通常为zip文件解压缩后的byte格式，根据类型解码&quot;&quot;&quot;</span></span><br><span class="line">        <span class="keyword">raise</span> Exception(<span class="string">&quot;Unimplemented!&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">init</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="keyword">pass</span> <span class="comment"># Just do nothing. This is a reasonable default!</span></span><br></pre></td></tr></table></figure>
<p>Git对象又细分为以下四种类型：GitBlob（Binary Large Object）对象， GitCommit对象， GitTree对象， GitTag对象，以下分别进行介绍。</p>
<h3 id="gitblob对象"><a class="markdownIt-Anchor" href="#gitblob对象"></a> GitBlob对象</h3>
<p>我们先最直观的看看Git中对于Blob内容的展示，如图4所示：</p>
<p><img data-src="../../codes/git/blob.png" alt="image" /></p>
<center>
<strong>图4：运行git cat-file之后对a.txt所对应的类型和类型内容的展示</strong>
</center>
<p>Blobs 是用户数据：你放入 Git 中的每个文件的内容（如a.txt、b.txt、src/c.txt、src/d.txt中的内容）的字节形式都被存储为一个 blob。这样做使它们很容易操作，因为 blob 没有特定的语法或约束，除了基本的对象存储机制之外，它们只是未指定的数据。</p>
<p>GitBlob对象的<code>python</code>实现如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">GitBlob</span>(<span class="title class_ inherited__">GitObject</span>):</span><br><span class="line">    fmt=<span class="string">b&#x27;blob&#x27;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">serialize</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>.blobdata</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">deserialize</span>(<span class="params">self, data</span>):</span><br><span class="line">        <span class="variable language_">self</span>.blobdata = data</span><br></pre></td></tr></table></figure>
<h3 id="gittree对象"><a class="markdownIt-Anchor" href="#gittree对象"></a> GitTree对象</h3>
<p>同上，我们先最直观的看看Git中对于树内容的展示，如图5所示：</p>
<p><img data-src="../../codes/git/tree.png" alt="image" /></p>
<center>
<strong>图5：运行git cat-file之后对最顶层的树所对应的类型和类型内容的展示</strong>
</center>
<p>非正式地说，树（tree）描述了Git仓库非隐藏文件夹中的内容，它将 blobs（文件对象）与路径关联起来。树是一个由三个元素组成的元组数组，这些元素包括文件模式、相对于工作树的路径，以及一个 SHA-1 值。一个典型的树的内容如下所示：</p>
<table>
<thead>
<tr>
<th>Mode</th>
<th>SHA-1</th>
<th>Path</th>
</tr>
</thead>
<tbody>
<tr>
<td>100644</td>
<td>06f844865cfc4b68116d2cbf00833b294aae63ec</td>
<td>.DS_Store</td>
</tr>
<tr>
<td>100644</td>
<td>e12523960bf9941182c801077be75dce699ff37c</td>
<td>a.txt</td>
</tr>
<tr>
<td>100644</td>
<td>753e270e92068316ec7fa2b6a40e780a4e1d14d7</td>
<td>b.txt</td>
</tr>
<tr>
<td>040000</td>
<td>30d66379dddee7068ab4fd3a17cc1ef9555278ac</td>
<td>src</td>
</tr>
</tbody>
</table>
<p>模式（Mode）就是文件的模式，路径（Path）是文件的位置。SHA-1 是指向一个 blob 或另一个树对象。如果是 blob，路径表示的是一个文件；如果是树对象，路径表示的是一个目录。</p>
<p>常见的文件模式有以下几种：</p>
<ul>
<li><strong>100644</strong>: 普通文件，具有标准的读写权限（普通的文本文件或代码文件）。</li>
<li><strong>100755</strong>: 可执行文件，具有执行权限（如脚本或可执行程序）。</li>
<li><strong>040000</strong>: 目录（directory）。</li>
<li><strong>120000</strong>: 符号链接（symbolic link）。</li>
<li><strong>160000</strong>: Git 子模块（submodule）。</li>
</ul>
<p>而实际上当我们解析树的时候，也就是对<code>.git/objects</code>中树的SHA-1路径所代表的文件进行解压缩后提纯到的树对象内容的字节编码，它们的格式通常如下：</p>
<blockquote>
<p>[mode] space [path] 0x00 [sha-1] （byte格式）</p>
</blockquote>
<p>由于树对应的文件夹中的文件不止一个，有时甚至会有在文件夹中进行文件夹嵌套。因此我们要设计出叶子对应单个文件，树对应由不同文件所组成的文件夹。</p>
<p>我们可以将叶子对象和树对象按以下<code>python</code>代码来表示：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">GitTreeLeaf</span> (<span class="title class_ inherited__">object</span>):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, mode, path, sha</span>):</span><br><span class="line">        <span class="variable language_">self</span>.mode = mode <span class="comment"># 模式</span></span><br><span class="line">        <span class="variable language_">self</span>.path = path <span class="comment"># 文件路径</span></span><br><span class="line">        <span class="variable language_">self</span>.sha = sha <span class="comment"># 文件路径索引到的文件内容标准化后所对应的sha-1值</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">GitTree</span>(<span class="title class_ inherited__">GitObject</span>):</span><br><span class="line">    fmt=<span class="string">b&#x27;tree&#x27;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">deserialize</span>(<span class="params">self, data</span>):</span><br><span class="line">	    <span class="comment"># tree_parse是解析所得到的树对象的字节编码数据</span></span><br><span class="line">        <span class="variable language_">self</span>.items = tree_parse(data)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">serialize</span>(<span class="params">self</span>):</span><br><span class="line">	    <span class="comment"># tree_serialize将解析后的结果求逆得到树对象的字节编码数据</span></span><br><span class="line">        <span class="keyword">return</span> tree_serialize(<span class="variable language_">self</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">init</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="variable language_">self</span>.items = <span class="built_in">list</span>()</span><br></pre></td></tr></table></figure>
<p>由于一个树中有时不单单包含叶子更有可能包含子树，具体情形见上面一棵树的典型情况，<code>tree_parse</code>和<code>tree_serialize</code>函数如下所示：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">tree_parse</span>(<span class="params">raw</span>):</span><br><span class="line">    pos = <span class="number">0</span></span><br><span class="line">    <span class="built_in">max</span> = <span class="built_in">len</span>(raw)</span><br><span class="line">    ret = <span class="built_in">list</span>()</span><br><span class="line">    <span class="keyword">while</span> pos &lt; <span class="built_in">max</span>:</span><br><span class="line">        pos, data = tree_parse_one(raw, pos)</span><br><span class="line">        ret.append(data)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> ret</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">tree_parse_one</span>(<span class="params">raw, start=<span class="number">0</span></span>):</span><br><span class="line">	<span class="comment"># [mode] space [path] 0x00 [sha-1] （byte格式）</span></span><br><span class="line">    <span class="comment"># 查找字节形式空格以确定mode</span></span><br><span class="line">    x = raw.find(<span class="string">b&#x27; &#x27;</span>, start)</span><br><span class="line">    <span class="keyword">assert</span> x-start == <span class="number">5</span> <span class="keyword">or</span> x-start==<span class="number">6</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 读取mode</span></span><br><span class="line">    mode = raw[start:x]</span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">len</span>(mode) == <span class="number">5</span>:</span><br><span class="line">        <span class="comment"># 标准到6字节</span></span><br><span class="line">        mode = <span class="string">b&quot;0&quot;</span> + mode</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 查找字节形式空终止符以确定path</span></span><br><span class="line">    y = raw.find(<span class="string">b&#x27;\x00&#x27;</span>, x)</span><br><span class="line">    <span class="comment"># 读取path</span></span><br><span class="line">    path = raw[x+<span class="number">1</span>:y]</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 读取sha…</span></span><br><span class="line">    <span class="comment"># raw[y+1:y+21] 提取的是 20 字节的二进制数据（SHA-1 哈希值）。</span></span><br><span class="line">    raw_sha = <span class="built_in">int</span>.from_bytes(raw[y+<span class="number">1</span>:y+<span class="number">21</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">    <span class="comment"># int.from_bytes(raw[y+1:y+21], &quot;big&quot;)将提取的 20 字节二进制数据转</span></span><br><span class="line">    <span class="comment"># 换为一个整数，然后使用 format(raw_sha, &quot;040x&quot;) 将这个整数转换为 40 </span></span><br><span class="line">    <span class="comment"># 位的十六进制字符串 。Git中通常使用的sha值时这种形式。比如.DS_Store对应</span></span><br><span class="line">    <span class="comment"># 的06f844865cfc4b68116d2cbf00833b294aae63ec</span></span><br><span class="line">    sha = <span class="built_in">format</span>(raw_sha, <span class="string">&quot;040x&quot;</span>)</span><br><span class="line">    <span class="keyword">return</span> y+<span class="number">21</span>, GitTreeLeaf(mode, path.decode(<span class="string">&quot;utf8&quot;</span>), sha)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">tree_serialize</span>(<span class="params">obj</span>):</span><br><span class="line">    obj.items.sort(key=tree_leaf_sort_key)</span><br><span class="line">    ret = <span class="string">b&#x27;&#x27;</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> obj.items:</span><br><span class="line">        ret += i.mode</span><br><span class="line">        ret += <span class="string">b&#x27; &#x27;</span></span><br><span class="line">        ret += i.path.encode(<span class="string">&quot;utf8&quot;</span>)</span><br><span class="line">        ret += <span class="string">b&#x27;\x00&#x27;</span></span><br><span class="line">        sha = <span class="built_in">int</span>(i.sha, <span class="number">16</span>)</span><br><span class="line">        ret += sha.to_bytes(<span class="number">20</span>, byteorder=<span class="string">&quot;big&quot;</span>)</span><br><span class="line">    <span class="keyword">return</span> ret</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">tree_leaf_sort_key</span>(<span class="params">leaf</span>):</span><br><span class="line">    <span class="keyword">if</span> leaf.mode.startswith(<span class="string">b&quot;10&quot;</span>):</span><br><span class="line">        <span class="keyword">return</span> leaf.path <span class="comment"># 文件</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">return</span> leaf.path + <span class="string">&quot;/&quot;</span> <span class="comment"># 文件夹</span></span><br></pre></td></tr></table></figure>
<h3 id="gitcommit对象"><a class="markdownIt-Anchor" href="#gitcommit对象"></a> GitCommit对象</h3>
<p>同上，我们先最直观的看看Git中对于树内容的展示，如图6所示：</p>
<p><img data-src="../../codes/git/commit.png" alt="image" /></p>
<center>
<strong>图6：运行git cat-file之后对最顶层的提交所对应的类型和类型内容的展示</strong>
</center>
<p>一个提交（Commit）的典型如下所示：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">tree 50306289de5ae719af85eaad3430d785517a11e7</span><br><span class="line">parent 735a65cef921f8e30ae55fdf19b695eb15ea8d45</span><br><span class="line">author fordelkon &lt;27xxx88816@qq.com&gt; 1741406298 +0800</span><br><span class="line">committer fordelkon &lt;27xxx88816@qq.com&gt; 1741406298 +0800</span><br><span class="line"></span><br><span class="line">life is short, I use Python</span><br></pre></td></tr></table></figure>
<p>从上面的典型来看，一个提交由以下几部分组成：</p>
<ul>
<li>树对象，也就是工作树的内容、文件和目录；</li>
<li>零个、一个或多个父提交；</li>
<li>一个作者身份（姓名和邮箱）以及一个时间戳；</li>
<li>一个提交者身份（姓名和邮箱）以及一个时间戳；</li>
<li>一个提交信息。<br />
从提交的组成来看，我们可以明白已经创建好的提交是不可变的。如果我们更改了作者、父提交或任何一个文件，那么我们实际上最终会创建一个新的、不同的提交对象与改变后的结果绑定。所以每一个提交都与它在整个仓库中的位置及其与最初提交的关系紧密相连。换句话说，一个给定的提交 ID （sha-1）不仅标识了某些文件的内容，还将提交与它的整个历史及整个仓库绑定在一起。也就是说，有了提交，我们的Git的历史记录功能就有了保证！</li>
</ul>
<p>一个提交对象的<code>python</code>代码如下所示：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">GitCommit</span>(<span class="title class_ inherited__">GitObject</span>):</span><br><span class="line">    fmt=<span class="string">b&#x27;commit&#x27;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">deserialize</span>(<span class="params">self, data</span>):</span><br><span class="line">	    <span class="comment"># kvlm_parse是解析所得到的提交对象的字节编码数据</span></span><br><span class="line">        <span class="variable language_">self</span>.kvlm = kvlm_parse(data)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">serialize</span>(<span class="params">self</span>):</span><br><span class="line">	    <span class="comment"># kvlm_serialize将解析后的结果求逆得到提交对象的字节编码数据</span></span><br><span class="line">        <span class="keyword">return</span> kvlm_serialize(<span class="variable language_">self</span>.kvlm)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">init</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="variable language_">self</span>.kvlm = <span class="built_in">dict</span>()</span><br></pre></td></tr></table></figure>
<p>由提交的典型来看，我们可以将提交对象的字节编码数据转换成键值对的字典格式，由于有空格和回车。更形象的表示如下:</p>
<blockquote>
<p>[key1] space [value1] \n … [keyn] space [valuen] \n [commit]</p>
</blockquote>
<p>因此，<code>kvlm_parse</code>和<code>kvlm_serialize</code>的python代码如下所示</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">kvlm_parse</span>(<span class="params">raw, start=<span class="number">0</span>, dct=<span class="literal">None</span></span>):</span><br><span class="line">    <span class="comment"># 如果 dct 为空，初始化一个空字典</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> dct:</span><br><span class="line">        dct = <span class="built_in">dict</span>()</span><br><span class="line">        <span class="comment"># 不能在函数参数中直接声明 dct=dict()，否则所有函数调用会共用同一个字典，</span></span><br><span class="line">        <span class="comment"># 导致每次调用时字典不断增长。</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 这个函数是递归的：它读取一个键/值对，然后调用自身来处理下一个位置。</span></span><br><span class="line">    <span class="comment"># 因此我们首先需要知道当前的位置：是在一个关键字，还是已经到了消息部分。</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 查找下一个空格和换行符的位置</span></span><br><span class="line">    spc = raw.find(<span class="string">b&#x27; &#x27;</span>, start)</span><br><span class="line">    nl = raw.find(<span class="string">b&#x27;\n&#x27;</span>, start)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 如果空格出现在换行符之前，说明我们有一个关键字。否则，它是最终的消息部分，</span></span><br><span class="line">    <span class="comment"># 我们会读取直到文件的末尾。</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 基本情况</span></span><br><span class="line">    <span class="comment"># =========</span></span><br><span class="line">    <span class="comment"># 如果换行符先出现（或者根本没有空格，这种情况下 find 返回 -1），</span></span><br><span class="line">    <span class="comment"># 我们假设遇到了一个空行。空行意味着剩余的数据是消息部分。</span></span><br><span class="line">    <span class="comment"># 我们将它存储在字典中，键为 None，然后返回。</span></span><br><span class="line">    <span class="keyword">if</span> (spc &lt; <span class="number">0</span>) <span class="keyword">or</span> (nl &lt; spc):</span><br><span class="line">        <span class="keyword">assert</span> nl == start</span><br><span class="line">        dct[<span class="literal">None</span>] = raw[start+<span class="number">1</span>:]  <span class="comment"># 存储消息部分</span></span><br><span class="line">        <span class="keyword">return</span> dct</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 递归情况</span></span><br><span class="line">    <span class="comment"># ==============</span></span><br><span class="line">    <span class="comment"># 读取一个键值对并递归处理下一个。</span></span><br><span class="line">    key = raw[start:spc]</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 查找值的结尾。以空格开头的行表示值的延续行，</span></span><br><span class="line">    <span class="comment"># 因此我们需要循环，直到找到一个不是以空格开头的换行符。</span></span><br><span class="line">    end = start</span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        end = raw.find(<span class="string">b&#x27;\n&#x27;</span>, end+<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">if</span> raw[end+<span class="number">1</span>] != <span class="built_in">ord</span>(<span class="string">&#x27; &#x27;</span>): <span class="keyword">break</span>  <span class="comment"># 找到一个不以空格开头的换行符</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 获取值，同时删除延续行的前导空格</span></span><br><span class="line">    value = raw[spc+<span class="number">1</span>:end].replace(<span class="string">b&#x27;\n &#x27;</span>, <span class="string">b&#x27;\n&#x27;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 不要覆盖现有的数据内容</span></span><br><span class="line">    <span class="keyword">if</span> key <span class="keyword">in</span> dct:</span><br><span class="line">        <span class="comment"># 如果字典中已有该键，且值为列表，则直接添加新值</span></span><br><span class="line">        <span class="keyword">if</span> <span class="built_in">type</span>(dct[key]) == <span class="built_in">list</span>:</span><br><span class="line">            dct[key].append(value)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="comment"># 如果已有的值不是列表，创建一个列表来存储多个值</span></span><br><span class="line">            dct[key] = [ dct[key], value ]</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="comment"># 如果字典中没有该键，则直接存储键值对</span></span><br><span class="line">        dct[key]=value</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 递归处理剩余的部分</span></span><br><span class="line">    <span class="keyword">return</span> kvlm_parse(raw, start=end+<span class="number">1</span>, dct=dct)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">kvlm_serialize</span>(<span class="params">kvlm</span>):</span><br><span class="line">    ret = <span class="string">b&#x27;&#x27;</span>  <span class="comment"># 初始化一个空的字节串用于存储序列化结果</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 输出字段</span></span><br><span class="line">    <span class="keyword">for</span> k <span class="keyword">in</span> kvlm.keys():</span><br><span class="line">        <span class="comment"># 跳过消息体本身（消息存储在字典的 None 键中）</span></span><br><span class="line">        <span class="keyword">if</span> k == <span class="literal">None</span>: <span class="keyword">continue</span></span><br><span class="line">        val = kvlm[k]</span><br><span class="line">        <span class="comment"># 将单个值规范化为列表，以便统一处理</span></span><br><span class="line">        <span class="keyword">if</span> <span class="built_in">type</span>(val) != <span class="built_in">list</span>:</span><br><span class="line">            val = [ val ]</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 遍历值列表，处理每个值</span></span><br><span class="line">        <span class="keyword">for</span> v <span class="keyword">in</span> val:</span><br><span class="line">            <span class="comment"># 拼接键和值，并确保换行符后面有一个空格（用于表示值的延续行）</span></span><br><span class="line">            ret += k + <span class="string">b&#x27; &#x27;</span> + (v.replace(<span class="string">b&#x27;\n&#x27;</span>, <span class="string">b&#x27;\n &#x27;</span>)) + <span class="string">b&#x27;\n&#x27;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 最后拼接消息体，消息体之前需要一个空行分隔</span></span><br><span class="line">    ret += <span class="string">b&#x27;\n&#x27;</span> + kvlm[<span class="literal">None</span>]</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> ret  <span class="comment"># 返回序列化后的字节串</span></span><br></pre></td></tr></table></figure>
<h3 id="gittag对象"><a class="markdownIt-Anchor" href="#gittag对象"></a> GitTag对象</h3>
<p>标签实际上就是引用（refs）。它们位于 .git/refs/tags/ 目录下。值得注意的是，标签有两种类型：轻量标签和标签对象。</p>
<h4 id="轻量标签lightweight-tags"><a class="markdownIt-Anchor" href="#轻量标签lightweight-tags"></a> 轻量标签（“Lightweight” tags）</h4>
<p>仅仅是指向提交、树对象或 blob 对象的普通引用。</p>
<h4 id="标签对象tag-objects"><a class="markdownIt-Anchor" href="#标签对象tag-objects"></a> 标签对象（Tag objects）</h4>
<p>是指向标签类型对象的普通引用。与轻量标签不同，标签对象包含作者、日期、可选的 PGP 签名以及可选的注释。它们的格式与提交对象相同。</p>
<p>因此，GitTag的<code>python</code>代码如下所示：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">GitTag</span>(<span class="title class_ inherited__">GitCommit</span>):</span><br><span class="line">    fmt = <span class="string">b&#x27;tag&#x27;</span></span><br></pre></td></tr></table></figure>
<p>我们已经了解了上面四种Git对象，那么也就知道了Git对象的内容决定路径的性质可以保证对于非隐藏文件夹中文件内容修改的记录。也就是说，Git仓库非隐藏文件夹中的文件一旦被修改，那么相应的Git对象的路径也会发生改变，将修改后的不同的Git对象的文件标准化后压缩按照改变的路径存储在<code>.git/objects</code>文件夹下，这样可以保证对该改变的记录。那么文件的内容和文件的路径有着明确的映射关系吗？Git给出的答案是对标准化后的文件内容进行 <a href="https://en.wikipedia.org/wiki/SHA-1">SHA-1</a><a href="https://en.wikipedia.org/wiki/Cryptographic_hash_function">哈希</a>后得到文件的路径。如何进行标准化也是我们值得注意的，Git中实现的方案是<strong>字节形式类型头+字节形式空格+字节形式大小+字节形式空分离符+对象内容的字节编码。</strong> 具体方案如下所示：</p>
<blockquote>
<p>[fmt] space [size] 0x00 [data] (byte格式)</p>
</blockquote>
<p>根据这一标准化的形式我们可以对对象进行读和写操作，所有对象的核心载体是对象内容的字节编码。具体<code>python</code>代码如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">object_read</span>(<span class="params">repo, sha</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;Read object sha from Git repository repo.  Return a</span></span><br><span class="line"><span class="string">    GitObject whose exact type depends on the object.&quot;&quot;&quot;</span></span><br><span class="line">	<span class="comment"># 由sha1值得到对应路径</span></span><br><span class="line">    path = repo_file(repo, <span class="string">&quot;objects&quot;</span>, sha[<span class="number">0</span>:<span class="number">2</span>], sha[<span class="number">2</span>:])</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> os.path.isfile(path):</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">with</span> <span class="built_in">open</span> (path, <span class="string">&quot;rb&quot;</span>) <span class="keyword">as</span> f:</span><br><span class="line">        raw = zlib.decompress(f.read())</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 读取对象类型</span></span><br><span class="line">        x = raw.find(<span class="string">b&#x27; &#x27;</span>) <span class="comment"># 0x20 字节形式空格</span></span><br><span class="line">        fmt = raw[<span class="number">0</span>:x] <span class="comment"># 字节形式类型头</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 读取有效的对象大小</span></span><br><span class="line">        y = raw.find(<span class="string">b&#x27;\x00&#x27;</span>, x) <span class="comment"># 0x00 字节形式空分离符</span></span><br><span class="line">        size = <span class="built_in">int</span>(raw[x:y].decode(<span class="string">&quot;ascii&quot;</span>))</span><br><span class="line">        <span class="keyword">if</span> size != <span class="built_in">len</span>(raw)-y-<span class="number">1</span>:</span><br><span class="line">            <span class="keyword">raise</span> Exception(<span class="string">f&quot;Malformed object <span class="subst">&#123;sha&#125;</span>: bad length&quot;</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 根据对象类型选择生成器</span></span><br><span class="line">        <span class="keyword">match</span> fmt:</span><br><span class="line">            <span class="keyword">case</span> <span class="string">b&#x27;commit&#x27;</span> : c=GitCommit</span><br><span class="line">            <span class="keyword">case</span> <span class="string">b&#x27;tree&#x27;</span>   : c=GitTree</span><br><span class="line">            <span class="keyword">case</span> <span class="string">b&#x27;tag&#x27;</span>    : c=GitTag</span><br><span class="line">            <span class="keyword">case</span> <span class="string">b&#x27;blob&#x27;</span>   : c=GitBlob</span><br><span class="line">            <span class="keyword">case</span> _:</span><br><span class="line">                <span class="keyword">raise</span> Exception(<span class="string">f&quot;Unknown type <span class="subst">&#123;fmt.decode(<span class="string">&quot;ascii&quot;</span>)&#125;</span> for object <span class="subst">&#123;sha&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 调用生成器返回对象</span></span><br><span class="line">        <span class="keyword">return</span> c(raw[y+<span class="number">1</span>:]) <span class="comment"># raw[y+1:]对象内容的字节编码</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">object_write</span>(<span class="params">obj, repo=<span class="literal">None</span></span>):</span><br><span class="line">    <span class="comment"># 对象内容的字节编码</span></span><br><span class="line">    data = obj.serialize()</span><br><span class="line">    <span class="comment"># 添加一些前缀成为标准格式</span></span><br><span class="line">    result = obj.fmt + <span class="string">b&#x27; &#x27;</span> + <span class="built_in">str</span>(<span class="built_in">len</span>(data)).encode() + <span class="string">b&#x27;\x00&#x27;</span> + data</span><br><span class="line">    <span class="comment"># 计算sha1</span></span><br><span class="line">    sha = hashlib.sha1(result).hexdigest()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> repo:</span><br><span class="line">        <span class="comment"># 由sha1值得到对应路径</span></span><br><span class="line">        path=repo_file(repo, <span class="string">&quot;objects&quot;</span>, sha[<span class="number">0</span>:<span class="number">2</span>], sha[<span class="number">2</span>:], mkdir=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> os.path.exists(path):</span><br><span class="line">            <span class="keyword">with</span> <span class="built_in">open</span>(path, <span class="string">&#x27;wb&#x27;</span>) <span class="keyword">as</span> f:</span><br><span class="line">                <span class="comment"># Compress and write</span></span><br><span class="line">                f.write(zlib.compress(result))</span><br><span class="line">    <span class="keyword">return</span> sha</span><br></pre></td></tr></table></figure>
<p>好了，前戏已经准备了这么多，我们可以凭借Git仓库和Git对象来实现最最最重要的Git两连击吗（<code>git add</code>和<code>git commit</code>）？答案当然是…否定的。是的，我们已经拥有了这么多的对象，也有了读取和写入对象的功能，但是一旦我们改变文件（不妨就改变a.txt文件的内容），由前面我们说的主要的三种对象来看，GitBlob元数据会改变，随之而来的GitTree元数据会改变，进而GitCommit元数据也会改变。这个过程不是一挥而就的，所以Git种才会分为<code>git add</code>和<code>git commit</code>两步走。在<code>git add</code>的过程中我们将修改后的文件属性变化提交到暂存区（Staging area），也就是我们图3中的<code>.git/index</code>文件中。然后使用<code>git commit</code>将暂存区中的所有记录的文件统合生成新的GitTree，进而生成新的GitCommit。很明显，我们还需要一个表示暂存区的类型。</p>
<h2 id="暂存区staging-area"><a class="markdownIt-Anchor" href="#暂存区staging-area"></a> 暂存区（Staging area）</h2>
<p>暂存区在.git文件夹中的展现形式就是<code>.git/index</code>文件，在一次提交之后，<code>.git/index</code>文件可以看作是该提交的一种副本：它保存了与对应的树相同的路径/Blob（对象）的关联关系。但它还保存了Git仓库中关于文件的额外信息，比如它们的创建/修改时间，因此 <code>git status</code> 通常不需要实际比较文件：它只需要检查文件的修改时间是否与索引文件中存储的时间相同，只有当它们不同时，才会执行实际的比较。类比树对象的表示，<code>.git/index</code>文件中包含许多元素，所以我们的第一个任务是对单个元素进行表示，之后是对整体<code>.git/index</code>文件进行表示。</p>
<p>单个元素和整体<code>.git/index</code>文件的<code>python</code>表示如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">GitIndexEntry</span> (<span class="title class_ inherited__">object</span>):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, ctime=<span class="literal">None</span>, mtime=<span class="literal">None</span>, dev=<span class="literal">None</span>, ino=<span class="literal">None</span>,</span></span><br><span class="line"><span class="params">                 mode_type=<span class="literal">None</span>, mode_perms=<span class="literal">None</span>, uid=<span class="literal">None</span>, gid=<span class="literal">None</span>,</span></span><br><span class="line"><span class="params">                 fsize=<span class="literal">None</span>, sha=<span class="literal">None</span>, flag_assume_valid=<span class="literal">None</span>,</span></span><br><span class="line"><span class="params">                 flag_stage=<span class="literal">None</span>, name=<span class="literal">None</span></span>):</span><br><span class="line">        <span class="comment"># 文件元数据最后更改的时间。格式是一个二元组</span></span><br><span class="line">        <span class="comment"># (以秒为单位的时间戳, 纳秒)</span></span><br><span class="line">        <span class="variable language_">self</span>.ctime = ctime</span><br><span class="line">        <span class="comment"># 文件数据最后更改的时间。格式是一个二元组</span></span><br><span class="line">        <span class="comment"># (以秒为单位的时间戳, 纳秒)</span></span><br><span class="line">        <span class="variable language_">self</span>.mtime = mtime</span><br><span class="line">        <span class="comment"># 包含该文件的设备ID</span></span><br><span class="line">        <span class="variable language_">self</span>.dev = dev</span><br><span class="line">        <span class="comment"># 文件的inode编号</span></span><br><span class="line">        <span class="variable language_">self</span>.ino = ino</span><br><span class="line">        <span class="comment"># 对象类型，可以是 b1000 (普通文件), b1010 (符号链接),</span></span><br><span class="line">        <span class="comment"># b1110 (gitlink)</span></span><br><span class="line">        <span class="variable language_">self</span>.mode_type = mode_type</span><br><span class="line">        <span class="comment"># 对象的权限，表示为一个整数</span></span><br><span class="line">        <span class="variable language_">self</span>.mode_perms = mode_perms</span><br><span class="line">        <span class="comment"># 文件所有者的用户ID</span></span><br><span class="line">        <span class="variable language_">self</span>.uid = uid</span><br><span class="line">        <span class="comment"># 文件所有者的组ID</span></span><br><span class="line">        <span class="variable language_">self</span>.gid = gid</span><br><span class="line">        <span class="comment"># 对象的大小，以字节为单位</span></span><br><span class="line">        <span class="variable language_">self</span>.fsize = fsize</span><br><span class="line">        <span class="comment"># 对象的SHA哈希值</span></span><br><span class="line">        <span class="variable language_">self</span>.sha = sha</span><br><span class="line">        <span class="comment"># 标志：假设该对象有效</span></span><br><span class="line">        <span class="variable language_">self</span>.flag_assume_valid = flag_assume_valid</span><br><span class="line">        <span class="comment"># 文件的阶段标志</span></span><br><span class="line">        <span class="variable language_">self</span>.flag_stage = flag_stage</span><br><span class="line">        <span class="comment"># 对象的名称（包括完整路径）</span></span><br><span class="line">        <span class="variable language_">self</span>.name = name</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">GitIndex</span> (<span class="title class_ inherited__">object</span>):</span><br><span class="line">    version = <span class="literal">None</span></span><br><span class="line">    entries = []</span><br><span class="line">    <span class="comment"># ext = None</span></span><br><span class="line">    <span class="comment"># sha = None</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, version=<span class="number">2</span>, entries=<span class="literal">None</span></span>):</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> entries:</span><br><span class="line">            entries = <span class="built_in">list</span>()</span><br><span class="line"></span><br><span class="line">        <span class="variable language_">self</span>.version = version</span><br><span class="line">        <span class="variable language_">self</span>.entries = entries</span><br></pre></td></tr></table></figure>
<p><code>.git/index</code>文件是一个二进制文件，可能是出于性能考虑。尽管如此，它的格式相对简单。文件开始部分是一个头部信息，包含了 DIRC 魔法字节、版本号以及该索引文件中的条目总数。更形象的格式如下：</p>
<blockquote>
<p>[DIRC]  [version]  [count]  [entries]</p>
</blockquote>
<p>所以我们可以定义<code>index_read</code>和<code>index_write</code>函数如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">index_read</span>(<span class="params">repo</span>):</span><br><span class="line">    <span class="comment"># 获取仓库索引文件的路径</span></span><br><span class="line">    index_file = repo_file(repo, <span class="string">&quot;index&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 如果索引文件不存在（例如新仓库），则返回一个空的 GitIndex 实例</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> os.path.exists(index_file):</span><br><span class="line">        <span class="keyword">return</span> GitIndex()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 打开索引文件，以二进制方式读取</span></span><br><span class="line">    <span class="keyword">with</span> <span class="built_in">open</span>(index_file, <span class="string">&#x27;rb&#x27;</span>) <span class="keyword">as</span> f:</span><br><span class="line">        raw = f.read()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 索引文件的头部包含 12 个字节</span></span><br><span class="line">    header = raw[:<span class="number">12</span>]</span><br><span class="line">    signature = header[:<span class="number">4</span>]</span><br><span class="line">    <span class="comment"># 检查索引文件的魔法字节，必须为 &quot;DIRC&quot;，代表 &quot;DirCache&quot;</span></span><br><span class="line">    <span class="keyword">assert</span> signature == <span class="string">b&quot;DIRC&quot;</span></span><br><span class="line">    <span class="comment"># 从头部解析版本号（4-8字节），此处只支持版本 2</span></span><br><span class="line">    version = <span class="built_in">int</span>.from_bytes(header[<span class="number">4</span>:<span class="number">8</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">    <span class="keyword">assert</span> version == <span class="number">2</span>, <span class="string">&quot;wyag 只支持索引文件版本 2&quot;</span></span><br><span class="line">    <span class="comment"># 从头部解析索引文件中的条目数量（8-12字节）</span></span><br><span class="line">    count = <span class="built_in">int</span>.from_bytes(header[<span class="number">8</span>:<span class="number">12</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 用于存储读取到的索引条目</span></span><br><span class="line">    entries = <span class="built_in">list</span>()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 索引文件内容部分从第 12 个字节开始</span></span><br><span class="line">    content = raw[<span class="number">12</span>:]</span><br><span class="line">    idx = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 循环解析每个条目，根据文件头部给出的条目数进行循环</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, count):</span><br><span class="line">        <span class="comment"># 解析创建时间（秒数和纳秒数）</span></span><br><span class="line">        ctime_s =  <span class="built_in">int</span>.from_bytes(content[idx: idx+<span class="number">4</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        ctime_ns = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">4</span>: idx+<span class="number">8</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        <span class="comment"># 解析修改时间（秒数和纳秒数）</span></span><br><span class="line">        mtime_s = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">8</span>: idx+<span class="number">12</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        mtime_ns = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">12</span>: idx+<span class="number">16</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        <span class="comment"># 解析设备 ID</span></span><br><span class="line">        dev = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">16</span>: idx+<span class="number">20</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        <span class="comment"># 解析 inode 编号</span></span><br><span class="line">        ino = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">20</span>: idx+<span class="number">24</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        <span class="comment"># 忽略的字段</span></span><br><span class="line">        unused = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">24</span>: idx+<span class="number">26</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        <span class="keyword">assert</span> <span class="number">0</span> == unused</span><br><span class="line">        <span class="comment"># 解析文件模式（包括类型和权限）</span></span><br><span class="line">        mode = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">26</span>: idx+<span class="number">28</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        mode_type = mode &gt;&gt; <span class="number">12</span></span><br><span class="line">        <span class="keyword">assert</span> mode_type <span class="keyword">in</span> [<span class="number">0b1000</span>, <span class="number">0b1010</span>, <span class="number">0b1110</span>]  <span class="comment"># 文件类型必须是常规文件、符号链接或 gitlink</span></span><br><span class="line">        mode_perms = mode &amp; <span class="number">0b0000000111111111</span></span><br><span class="line">        <span class="comment"># 解析用户 ID 和组 ID</span></span><br><span class="line">        uid = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">28</span>: idx+<span class="number">32</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        gid = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">32</span>: idx+<span class="number">36</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        <span class="comment"># 解析文件大小</span></span><br><span class="line">        fsize = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">36</span>: idx+<span class="number">40</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        <span class="comment"># 解析 SHA（对象 ID），并格式化为 40 字符的小写十六进制字符串</span></span><br><span class="line">        sha = <span class="built_in">format</span>(<span class="built_in">int</span>.from_bytes(content[idx+<span class="number">40</span>: idx+<span class="number">60</span>], <span class="string">&quot;big&quot;</span>), <span class="string">&quot;040x&quot;</span>)</span><br><span class="line">        <span class="comment"># 解析标志位</span></span><br><span class="line">        flags = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">60</span>: idx+<span class="number">62</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        <span class="comment"># 解析假定有效标志位</span></span><br><span class="line">        flag_assume_valid = (flags &amp; <span class="number">0b1000000000000000</span>) != <span class="number">0</span></span><br><span class="line">        <span class="comment"># 解析扩展标志位（被忽略）</span></span><br><span class="line">        flag_extended = (flags &amp; <span class="number">0b0100000000000000</span>) != <span class="number">0</span></span><br><span class="line">        <span class="keyword">assert</span> <span class="keyword">not</span> flag_extended</span><br><span class="line">        <span class="comment"># 解析阶段标志位</span></span><br><span class="line">        flag_stage =  flags &amp; <span class="number">0b0011000000000000</span></span><br><span class="line">        <span class="comment"># 解析文件名长度（12 位）</span></span><br><span class="line">        name_length = flags &amp; <span class="number">0b0000111111111111</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 读取了 62 个字节，继续处理文件名</span></span><br><span class="line">        idx += <span class="number">62</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 如果文件名长度小于 0xFFF，则文件名之后有一个空字节终止符</span></span><br><span class="line">        <span class="keyword">if</span> name_length &lt; <span class="number">0xFFF</span>:</span><br><span class="line">            <span class="keyword">assert</span> content[idx + name_length] == <span class="number">0x00</span></span><br><span class="line">            raw_name = content[idx:idx+name_length]</span><br><span class="line">            idx += name_length + <span class="number">1</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="comment"># 处理文件名长度大于等于 0xFFF 的情况，需要查找空字节终止符</span></span><br><span class="line">            <span class="built_in">print</span>(<span class="string">f&quot;注意: 文件名长度为 0x<span class="subst">&#123;name_length:X&#125;</span> 字节长。&quot;</span>)</span><br><span class="line">            null_idx = content.find(<span class="string">b&#x27;\x00&#x27;</span>, idx + <span class="number">0xFFF</span>)</span><br><span class="line">            raw_name = content[idx: null_idx]</span><br><span class="line">            idx = null_idx + <span class="number">1</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 将文件名解析为 UTF-8 字符串</span></span><br><span class="line">        name = raw_name.decode(<span class="string">&quot;utf8&quot;</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 索引文件中的数据以 8 字节对齐，因此需要根据指针对齐调整 idx</span></span><br><span class="line">        idx = <span class="number">8</span> * ceil(idx / <span class="number">8</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 将解析出的索引条目添加到列表中</span></span><br><span class="line">        entries.append(GitIndexEntry(ctime=(ctime_s, ctime_ns),</span><br><span class="line">                                     mtime=(mtime_s,  mtime_ns),</span><br><span class="line">                                     dev=dev,</span><br><span class="line">                                     ino=ino,</span><br><span class="line">                                     mode_type=mode_type,</span><br><span class="line">                                     mode_perms=mode_perms,</span><br><span class="line">                                     uid=uid,</span><br><span class="line">                                     gid=gid,</span><br><span class="line">                                     fsize=fsize,</span><br><span class="line">                                     sha=sha,</span><br><span class="line">                                flag_assume_valid=flag_assume_valid,</span><br><span class="line">                                     flag_stage=flag_stage,</span><br><span class="line">                                     name=name))</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 返回包含所有索引条目的 GitIndex 实例</span></span><br><span class="line">    <span class="keyword">return</span> GitIndex(version=version, entries=entries)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">index_write</span>(<span class="params">repo, index</span>):</span><br><span class="line">    <span class="keyword">with</span> <span class="built_in">open</span>(repo_file(repo, <span class="string">&quot;index&quot;</span>), <span class="string">&quot;wb&quot;</span>) <span class="keyword">as</span> f:</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 写入头部信息</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 写入魔术字节 &quot;DIRC&quot; 以标识该文件为 Git 索引文件。</span></span><br><span class="line">        f.write(<span class="string">b&quot;DIRC&quot;</span>)</span><br><span class="line">        <span class="comment"># 写入版本号（固定为 2）。</span></span><br><span class="line">        f.write(index.version.to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line">        <span class="comment"># 写入索引条目数量。</span></span><br><span class="line">        f.write(<span class="built_in">len</span>(index.entries).to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 写入条目数据</span></span><br><span class="line"></span><br><span class="line">        idx = <span class="number">0</span></span><br><span class="line">        <span class="keyword">for</span> e <span class="keyword">in</span> index.entries:</span><br><span class="line">            <span class="comment"># 写入创建时间（秒）和创建时间的纳秒部分。</span></span><br><span class="line">            f.write(e.ctime[<span class="number">0</span>].to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line">            f.write(e.ctime[<span class="number">1</span>].to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line">            <span class="comment"># 写入修改时间（秒）和修改时间的纳秒部分。</span></span><br><span class="line">            f.write(e.mtime[<span class="number">0</span>].to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line">            f.write(e.mtime[<span class="number">1</span>].to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line">            <span class="comment"># 写入设备号和 inode 号。</span></span><br><span class="line">            f.write(e.dev.to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line">            f.write(e.ino.to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line"></span><br><span class="line">            <span class="comment"># 写入文件权限模式（类型和权限合并）。</span></span><br><span class="line">            mode = (e.mode_type &lt;&lt; <span class="number">12</span>) | e.mode_perms</span><br><span class="line">            f.write(mode.to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line"></span><br><span class="line">            <span class="comment"># 写入用户 ID 和组 ID。</span></span><br><span class="line">            f.write(e.uid.to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line">            f.write(e.gid.to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line"></span><br><span class="line">            <span class="comment"># 写入文件大小。</span></span><br><span class="line">            f.write(e.fsize.to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line">            <span class="comment"># 写入 SHA-1 对象 ID。</span></span><br><span class="line">            f.write(<span class="built_in">int</span>(e.sha, <span class="number">16</span>).to_bytes(<span class="number">20</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line"></span><br><span class="line">            <span class="comment"># 处理标志位 `flag_assume_valid`。</span></span><br><span class="line">            flag_assume_valid = <span class="number">0x1</span> &lt;&lt; <span class="number">15</span> <span class="keyword">if</span> e.flag_assume_valid <span class="keyword">else</span> <span class="number">0</span></span><br><span class="line"></span><br><span class="line">            <span class="comment"># 将文件名编码为 UTF-8，并计算长度。</span></span><br><span class="line">            name_bytes = e.name.encode(<span class="string">&quot;utf8&quot;</span>)</span><br><span class="line">            bytes_len = <span class="built_in">len</span>(name_bytes)</span><br><span class="line">            <span class="keyword">if</span> bytes_len &gt;= <span class="number">0xFFF</span>:</span><br><span class="line">                name_length = <span class="number">0xFFF</span>  <span class="comment"># 长度过长时，标记为 0xFFF。</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                name_length = bytes_len</span><br><span class="line"></span><br><span class="line">            <span class="comment"># 将 `flag_assume_valid`、`flag_stage` 和名称长度合并到 2 个字节中。</span></span><br><span class="line">            f.write((flag_assume_valid | e.flag_stage | name_length).to_bytes(<span class="number">2</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line"></span><br><span class="line">            <span class="comment"># 写入文件名，并附加一个 0x00 作为结尾。</span></span><br><span class="line">            f.write(name_bytes)</span><br><span class="line">            f.write((<span class="number">0</span>).to_bytes(<span class="number">1</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line"></span><br><span class="line">            <span class="comment"># 计算当前条目的字节数。</span></span><br><span class="line">            idx += <span class="number">62</span> + <span class="built_in">len</span>(name_bytes) + <span class="number">1</span></span><br><span class="line"></span><br><span class="line">            <span class="comment"># 若条目不是 8 字节对齐，则进行填充。</span></span><br><span class="line">            <span class="keyword">if</span> idx % <span class="number">8</span> != <span class="number">0</span>:</span><br><span class="line">                pad = <span class="number">8</span> - (idx % <span class="number">8</span>)</span><br><span class="line">                f.write((<span class="number">0</span>).to_bytes(pad, <span class="string">&quot;big&quot;</span>))</span><br><span class="line">                idx += pad</span><br></pre></td></tr></table></figure>
<p>按照我们前面所说的，<code>git add</code>会根据所修改的文件路径对<code>.git/index</code>文件中的内容进行修改。具体实现方式是从<code>.git/index</code>文件中移除指定路径的条目（不物理删除文件），然后对每个路径，读取文件内容，计算文件的哈希值并获取文件的元数据信息，然后将文件的相关信息添加到<code>.git/index</code>文件中，包括 ctime、mtime、文件权限等，最后将新的索引条目写回索引文件。具体的<code>add</code>函数实现如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">add</span>(<span class="params">repo, paths, delete=<span class="literal">True</span>, skip_missing=<span class="literal">False</span></span>):</span><br><span class="line">    <span class="comment"># 首先调用 rm 函数从索引中移除这些路径的条目，避免重复（不物理删除文件）。</span></span><br><span class="line">    rm(repo, paths, delete=<span class="literal">False</span>, skip_missing=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 获取工作树的根目录路径（末尾加上路径分隔符）</span></span><br><span class="line">    worktree = repo.worktree + os.sep</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 将路径转换为 (绝对路径，相对路径) 的对，并确保它们在工作树内且为文件</span></span><br><span class="line">    clean_paths = <span class="built_in">set</span>()</span><br><span class="line">    <span class="keyword">for</span> path <span class="keyword">in</span> paths:</span><br><span class="line">        abspath = os.path.abspath(path)  <span class="comment"># 获取绝对路径</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> (abspath.startswith(worktree) <span class="keyword">and</span> os.path.isfile(abspath)):  <span class="comment"># 确保路径在工作树内且为文件</span></span><br><span class="line">            <span class="keyword">raise</span> Exception(<span class="string">f&quot;Not a file, or outside the worktree: <span class="subst">&#123;paths&#125;</span>&quot;</span>)  <span class="comment"># 如果路径无效或不在工作树内，抛出异常</span></span><br><span class="line">        relpath = os.path.relpath(abspath, repo.worktree)  <span class="comment"># 获取相对路径</span></span><br><span class="line">        clean_paths.add((abspath, relpath))  <span class="comment"># 添加到 clean_paths 集合中</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 查找并读取索引。由于 `rm` 已经修改了索引，这里再次读取。</span></span><br><span class="line">    <span class="comment"># @FIXME：可以通过传递命令来移动索引，而不是反复读写索引。</span></span><br><span class="line">    index = index_read(repo)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 遍历每个路径对，读取文件，生成哈希并更新索引条目</span></span><br><span class="line">    <span class="keyword">for</span> (abspath, relpath) <span class="keyword">in</span> clean_paths:</span><br><span class="line">        <span class="keyword">with</span> <span class="built_in">open</span>(abspath, <span class="string">&quot;rb&quot;</span>) <span class="keyword">as</span> fd:</span><br><span class="line">            sha = object_hash(fd, <span class="string">b&quot;blob&quot;</span>, repo)  <span class="comment"># 计算文件的 SHA 哈希值</span></span><br><span class="line"></span><br><span class="line">            stat = os.stat(abspath)  <span class="comment"># 获取文件状态</span></span><br><span class="line"></span><br><span class="line">            <span class="comment"># 获取文件的创建时间和修改时间（秒和纳秒）</span></span><br><span class="line">            ctime_s = <span class="built_in">int</span>(stat.st_ctime)</span><br><span class="line">            ctime_ns = stat.st_ctime_ns % <span class="number">10</span>**<span class="number">9</span></span><br><span class="line">            mtime_s = <span class="built_in">int</span>(stat.st_mtime)</span><br><span class="line">            mtime_ns = stat.st_mtime_ns % <span class="number">10</span>**<span class="number">9</span></span><br><span class="line"></span><br><span class="line">            <span class="comment"># 创建 Git 索引条目对象</span></span><br><span class="line">            entry = GitIndexEntry(ctime=(ctime_s, ctime_ns), mtime=(mtime_s, mtime_ns), dev=stat.st_dev, ino=stat.st_ino,</span><br><span class="line">                                  mode_type=<span class="number">0b1000</span>, mode_perms=<span class="number">0o644</span>, uid=stat.st_uid, gid=stat.st_gid,</span><br><span class="line">                                  fsize=stat.st_size, sha=sha, flag_assume_valid=<span class="literal">False</span>,</span><br><span class="line">                                  flag_stage=<span class="literal">False</span>, name=relpath)</span><br><span class="line"></span><br><span class="line">            <span class="comment"># 将新条目添加到索引条目列表</span></span><br><span class="line">            index.entries.append(entry)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 将更新后的索引写回文件</span></span><br><span class="line">    index_write(repo, index)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">rm</span>(<span class="params">repo, paths, delete=<span class="literal">True</span>, skip_missing=<span class="literal">False</span></span>):</span><br><span class="line">    <span class="comment"># 查找并读取索引文件</span></span><br><span class="line">    index = index_read(repo)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 获取工作树的根目录路径（末尾加上路径分隔符）</span></span><br><span class="line">    worktree = repo.worktree + os.sep</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 将输入路径转换为绝对路径，并确保它们位于工作树内</span></span><br><span class="line">    abspaths = <span class="built_in">set</span>()</span><br><span class="line">    <span class="keyword">for</span> path <span class="keyword">in</span> paths:</span><br><span class="line">        abspath = os.path.abspath(path)  <span class="comment"># 获取绝对路径</span></span><br><span class="line">        <span class="keyword">if</span> abspath.startswith(worktree):  <span class="comment"># 确保路径在工作树中</span></span><br><span class="line">            abspaths.add(abspath)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">raise</span> Exception(<span class="string">f&quot;Cannot remove paths outside of worktree: <span class="subst">&#123;paths&#125;</span>&quot;</span>)  <span class="comment"># 如果路径不在工作树内，抛出异常</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 将要保留的索引条目列表，用于更新索引文件</span></span><br><span class="line">    kept_entries = <span class="built_in">list</span>()</span><br><span class="line">    <span class="comment"># 将要删除的路径列表，用于物理删除文件</span></span><br><span class="line">    remove = <span class="built_in">list</span>()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 遍历索引条目，删除匹配的路径，保留其余条目</span></span><br><span class="line">    <span class="keyword">for</span> e <span class="keyword">in</span> index.entries:</span><br><span class="line">        full_path = os.path.join(repo.worktree, e.name)  <span class="comment"># 获取索引条目的完整路径</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> full_path <span class="keyword">in</span> abspaths:  <span class="comment"># 如果路径在待删除列表中</span></span><br><span class="line">            remove.append(full_path)  <span class="comment"># 加入待删除列表</span></span><br><span class="line">            abspaths.remove(full_path)  <span class="comment"># 从待删除路径中移除</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            kept_entries.append(e)  <span class="comment"># 保留该条目</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 如果有未在索引中找到的路径，且 skip_missing=False，则抛出异常</span></span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">len</span>(abspaths) &gt; <span class="number">0</span> <span class="keyword">and</span> <span class="keyword">not</span> skip_missing:</span><br><span class="line">        <span class="keyword">raise</span> Exception(<span class="string">f&quot;Cannot remove paths not in the index: <span class="subst">&#123;abspaths&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 物理删除文件系统中的路径</span></span><br><span class="line">    <span class="keyword">if</span> delete:</span><br><span class="line">        <span class="keyword">for</span> path <span class="keyword">in</span> remove:</span><br><span class="line">            os.unlink(path)  <span class="comment"># 删除文件</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 更新索引条目，并将其写回索引文件</span></span><br><span class="line">    index.entries = kept_entries</span><br><span class="line">    index_write(repo, index)</span><br></pre></td></tr></table></figure>
<p>然后再使用<code>git add</code>之后的<code>.git/index</code>文件生成树对象，最后再借由树对象生成提交对象！！！具体<code>python</code>代码实现如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">tree_from_index</span>(<span class="params">repo, index</span>):</span><br><span class="line">    <span class="comment"># 初始化一个字典，用于存储目录及其内容，根目录 &quot;&quot; 存储一个空列表。</span></span><br><span class="line">    contents = <span class="built_in">dict</span>()</span><br><span class="line">    contents[<span class="string">&quot;&quot;</span>] = <span class="built_in">list</span>()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 遍历索引中的每个条目，构建目录结构。</span></span><br><span class="line">    <span class="keyword">for</span> entry <span class="keyword">in</span> index.entries:</span><br><span class="line">        dirname = os.path.dirname(entry.name)  <span class="comment"># 获取当前条目的目录名</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 遍历目录路径，直到根目录，确保每个目录都被创建</span></span><br><span class="line">        key = dirname</span><br><span class="line">        <span class="keyword">while</span> key != <span class="string">&quot;&quot;</span>:</span><br><span class="line">            <span class="keyword">if</span> key <span class="keyword">not</span> <span class="keyword">in</span> contents:  <span class="comment"># 如果该目录尚未在 contents 中，创建一个空列表</span></span><br><span class="line">                contents[key] = <span class="built_in">list</span>()</span><br><span class="line">            key = os.path.dirname(key)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 将条目添加到对应目录的内容列表中</span></span><br><span class="line">        contents[dirname].append(entry)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 获取所有目录的路径并按路径长度从长到短排序</span></span><br><span class="line">    sorted_paths = <span class="built_in">sorted</span>(contents.keys(), key=<span class="built_in">len</span>, reverse=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 初始化树的 SHA-1 哈希（根树的哈希值将会保存在这里）</span></span><br><span class="line">    sha = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 遍历排序后的路径（目录），构建树对象</span></span><br><span class="line">    <span class="keyword">for</span> path <span class="keyword">in</span> sorted_paths:</span><br><span class="line">        tree = GitTree()  <span class="comment"># 创建一个新的空树对象</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 遍历该目录下的条目，生成树叶并添加到树中</span></span><br><span class="line">        <span class="keyword">for</span> entry <span class="keyword">in</span> contents[path]:</span><br><span class="line">            <span class="keyword">if</span> <span class="built_in">isinstance</span>(entry, GitIndexEntry):  <span class="comment"># 普通的文件条目</span></span><br><span class="line">                <span class="comment"># 转换文件的权限模式，Git 使用十六进制表示，树对象需要的是八进制 ASCII 字符串</span></span><br><span class="line">                leaf_mode = <span class="string">f&quot;<span class="subst">&#123;entry.mode_type:02o&#125;</span><span class="subst">&#123;entry.mode_perms:04o&#125;</span>&quot;</span>.encode(<span class="string">&quot;ascii&quot;</span>)</span><br><span class="line">                leaf = GitTreeLeaf(mode=leaf_mode, path=os.path.basename(entry.name), sha=entry.sha)</span><br><span class="line">            <span class="keyword">else</span>:  <span class="comment"># 子目录条目（树）</span></span><br><span class="line">                leaf = GitTreeLeaf(mode=<span class="string">b&quot;040000&quot;</span>, path=entry[<span class="number">0</span>], sha=entry[<span class="number">1</span>])</span><br><span class="line"></span><br><span class="line">            tree.items.append(leaf)  <span class="comment"># 将叶子添加到树对象中</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 将生成的树对象写入对象存储，并获取其 SHA-1 哈希</span></span><br><span class="line">        sha = object_write(tree, repo)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 将树对象的哈希值添加到其父目录的内容列表中，作为一对 (文件名, SHA)</span></span><br><span class="line">        parent = os.path.dirname(path)  <span class="comment"># 获取父目录路径</span></span><br><span class="line">        base = os.path.basename(path)   <span class="comment"># 获取当前目录的名称（例如 &quot;main.go&quot;）</span></span><br><span class="line">        contents[parent].append((base, sha))  <span class="comment"># 将父目录与新生成的树的哈希值关联</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> sha  <span class="comment"># 返回根树的 SHA-1 哈希</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">commit_create</span>(<span class="params">repo, tree, parent, author, timestamp, message</span>):</span><br><span class="line">    commit = GitCommit()  <span class="comment"># 创建一个新的提交对象</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 设置提交对象的树字段为当前提交的树对象的 SHA-1 值</span></span><br><span class="line">    commit.kvlm[<span class="string">b&quot;tree&quot;</span>] = tree.encode(<span class="string">&quot;ascii&quot;</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 如果有父提交，设置父提交字段</span></span><br><span class="line">    <span class="keyword">if</span> parent:</span><br><span class="line">        commit.kvlm[<span class="string">b&quot;parent&quot;</span>] = parent.encode(<span class="string">&quot;ascii&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 清理提交信息，并在末尾加上换行符</span></span><br><span class="line">    message = message.strip() + <span class="string">&quot;\n&quot;</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 格式化时区偏移（例如 &quot;+0200&quot; 或 &quot;-0700&quot;）</span></span><br><span class="line">    offset = <span class="built_in">int</span>(timestamp.astimezone().utcoffset().total_seconds())  <span class="comment"># 获取时区偏移（秒）</span></span><br><span class="line">    hours = offset // <span class="number">3600</span>  <span class="comment"># 计算小时</span></span><br><span class="line">    minutes = (offset % <span class="number">3600</span>) // <span class="number">60</span>  <span class="comment"># 计算分钟</span></span><br><span class="line">    tz = <span class="string">&quot;&#123;&#125;&#123;:02&#125;&#123;:02&#125;&quot;</span>.<span class="built_in">format</span>(<span class="string">&quot;+&quot;</span> <span class="keyword">if</span> offset &gt; <span class="number">0</span> <span class="keyword">else</span> <span class="string">&quot;-&quot;</span>, hours, minutes)  <span class="comment"># 格式化时区字符串</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 将作者信息格式化为 &quot;Author Name &lt;email&gt; timestamp timezone&quot;</span></span><br><span class="line">    author = author + timestamp.strftime(<span class="string">&quot; %s &quot;</span>) + tz</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 设置提交对象的作者和提交者字段为相同的值</span></span><br><span class="line">    commit.kvlm[<span class="string">b&quot;author&quot;</span>] = author.encode(<span class="string">&quot;utf8&quot;</span>)</span><br><span class="line">    commit.kvlm[<span class="string">b&quot;committer&quot;</span>] = author.encode(<span class="string">&quot;utf8&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 设置提交信息字段</span></span><br><span class="line">    commit.kvlm[<span class="literal">None</span>] = message.encode(<span class="string">&quot;utf8&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 将提交对象写入对象存储，并返回其 SHA-1 哈希</span></span><br><span class="line">    <span class="keyword">return</span> object_write(commit, repo)</span><br></pre></td></tr></table></figure>
<h2 id="git二连击可视化"><a class="markdownIt-Anchor" href="#git二连击可视化"></a> Git二连击可视化</h2>
<p>已经了解了上面的底层原理后，对Git两连击的的可视化也就十分简单了，详见下图，参考<a href="https://zhuanlan.zhihu.com/p/96631135">这才是真正的Git——Git内部原理揭秘！</a>：<br />
<img data-src="../../codes/git/object.png" alt="image" /><br />
<img data-src="../../codes/git/change.gif" alt="gif" /><br />
<img data-src="../../codes/git/add.gif" alt="gif" /><br />
<img data-src="../../codes/git/commit.gif" alt="gif" /><br />
现在，😮‍💨，所有Git基础的内容的底层原理以及其代码形式差不多就讲解完了。还有一些比较高级的Git内容我们留待后文。</p>
<h2 id="git常用命令进阶"><a class="markdownIt-Anchor" href="#git常用命令进阶"></a> Git常用命令进阶</h2>
<p>我们已经可视化了Git两连击中细节，下面我们可以从高层次的视角（GitCommit）来对Git中的高阶常用命令进行可视化，可视化结果由<a href="https://learngitbranching.js.org/?locale=zh_CN">Learn Git Branch</a>提供。我们经过一系列的<code>git add</code>和<code>git commit</code>以及<code>git checkout</code>之后得到的初始的GitCommit的分布情况如图7所示。</p>
<p><img data-src="../../codes/git/highlevel.png" alt="gif" /></p>
<center>
<strong>图7：初始GitCommit分布情况</strong>
</center>
<p>接下来先介绍的4个命令的联系如图8所示：</p>
<p><img data-src="../../codes/git/git3.png" alt="gif" /><br />
<img data-src="../../codes/git/git3high.png" alt="gif" /></p>
<center>
<strong>图8：git reset、git checkout、git add和 git commit的联系</strong>
</center>
<p><code>git add</code>和<code>git commit</code> 命令我们已经详细介绍过了，下面再简要对对这4个命令进行叙述。</p>
<ul>
<li><code>git add files</code> 把当前文件放入暂存区域。</li>
<li><code>git commit -m message</code> 给暂存区域生成快照并提交，而<code>git commit -a -m message</code>自动将所有被跟踪的文件的更改暂存并提交，省略了<code>git add</code>的步骤。</li>
<li><code>git reset files</code> 用来撤销最后一次<code>git add files</code>，你也可以用<code>git reset</code> 撤销所有暂存区域文件。</li>
<li><code>git checkout files</code> 把文件从暂存区域复制到工作目录，用来丢弃本地修改，而<code>git checkout HEAD files</code> 用来将工作目录中指定文件恢复到当前分支最新一次提交时的状态（即 HEAD 指针指向的提交）。这相当于丢弃对这些文件的所有未提交修改，恢复到上一次提交的版本。</li>
</ul>
<h3 id="git-checkout"><a class="markdownIt-Anchor" href="#git-checkout"></a> <code>git checkout</code></h3>
<h4 id="分支参数"><a class="markdownIt-Anchor" href="#分支参数"></a> 分支参数</h4>
<p>checkout命令用于从历史提交（或者暂存区域）中拷贝文件到工作目录，也可用于切换分支。当不指定文件名，而是给出一个（本地）分支时，那么<code>HEAD</code>标识会移动到那个分支（也就是说，我们“切换”到那个分支了），然后暂存区域和工作目录中的内容会和<code>HEAD</code>对应的提交节点一致。新提交节点（下图中的C1）中的所有文件都会被复制（到暂存区域和工作目录中）；只存在于老的提交节点（C5）中的文件会被删除；不属于上述两者的文件会被忽略，不受影响。(分支在<code>.git</code>文件夹中对应<code>.git/refs/heads/</code>中的文件，<code>HEAD</code>对应<code>.git</code>文件夹中的<code>HEAD</code>文件，<code>git checkout stable</code>将<code>HEAD</code>文件内容由<code>ref: refs/heads/main</code>改变成<code>ref: refs/heads/stable</code>同时暂存区和工作目录也会发生变化)</p>
<p><img data-src="../../codes/git/branch.png" alt="gif" /></p>
<center>
<strong>图9：git checkout stable将分支从main转换到stable</strong>
</center>
<h4 id="被分离的head标识"><a class="markdownIt-Anchor" href="#被分离的head标识"></a> 被分离的HEAD标识</h4>
<p>如果既没有指定文件名，也没有指定分支名，而是一个标签、远程分支、SHA-1值或者是像main~3类似的东西，就得到一个匿名分支，称作detached HEAD（被分离的HEAD标识），暂存区域和工作目录中的内容会和<code>HEAD</code>对应的提交节点一致。</p>
<p><img data-src="../../codes/git/detached.png" alt="gif" /></p>
<center>
<strong>图10：git checkout main~3将HEAD分离出来</strong>
</center>
<p>HEAD处于分离状态时，提交操作可以正常进行如图11所示，但是不会更新任何已命名的分支。(你可以认为这是在更新一个匿名分支。)一旦此后你切换到别的分支，比如说main，那么这个提交节点（可能）再也不会被引用到，然后就会被丢弃掉了，如图12所示。</p>
<p><img data-src="../../codes/git/detached_commit.png" alt="gif" /></p>
<center>
<strong>图11：HEAD标识处于分离状态时的提交操作</strong>
</center>
<p><img data-src="../../codes/git/detached_main.png" alt="gif" /></p>
<center>
<strong>图12：git checkout main运行后之前提交节点的情况</strong>
</center>
<p>但是，如果你想保存这个状态，可以用命令<code>git checkout -b name</code>来创建一个新的分支，如图13所示。</p>
<p><img data-src="../../codes/git/new_branch.png" alt="gif" /></p>
<center>
<strong>图13：git checkout -b new运行后可以保存提交状态</strong>
</center>
<h3 id="git-reset"><a class="markdownIt-Anchor" href="#git-reset"></a> <code>git reset</code></h3>
<p>reset命令把当前分支指向另一个位置，并且有选择的变动工作目录和索引。也用来在从历史仓库中复制文件到索引，而不动工作目录。</p>
<p>如果不给选项，移动<code>HEAD</code>到指定的提交，并且将更改从暂存区移除，但保留在工作目录中（需要<code>git add</code>和<code>git commit</code>）。如果用<code>--hard</code>选项，移动<code>HEAD</code>到指定的提交，并且重置暂存区和工作目录，如果用<code>--soft</code>选项，仅仅移动<code>HEAD</code>到指定的提交，但保留工作区和暂存区的更改（仅需要<code>git commit</code>）。</p>
<p><img data-src="../../codes/git/reset.png" alt="gif" /></p>
<center>
<strong>图14：现将HEAD指向main分支再运行git reset main~3后的情况</strong>
</center>
<p>如果没有给出提交点的版本号，那么默认用HEAD。这样，分支指向不变，但是索引会回滚到最后一次提交，如果用<code>--hard</code>选项，工作目录也同样。</p>
<h3 id="git-merge"><a class="markdownIt-Anchor" href="#git-merge"></a> <code>git merge</code></h3>
<p>merge 命令把不同分支合并起来。合并前，索引必须和当前提交相同。如果另一个分支是当前提交的祖父节点，那么合并命令将什么也不做。 另一种情况是如果当前提交是另一个分支的祖父节点，就导致fast-forward合并。指向只是简单的移动，并生成一个新的提交。</p>
<p><img data-src="../../codes/git/merge_before.png" alt="gif" /></p>
<center>
<strong>图14：运行git merge main前的情况</strong>
</center>
<p><img data-src="../../codes/git/merge_after.png" alt="gif" /></p>
<center>
<strong>图15：运行git merge main后的情况，快速前进</strong>
</center>
<p>否则就是一次真正的合并。默认把当前提交(C5 如下所示)和另一个提交(C7)以及他们的共同祖父节点(C2)进行一次<a href="http://en.wikipedia.org/wiki/Three-way_merge">三方合并</a>。结果是先保存当前目录和索引，然后和父节点C7一起做一次新提交。</p>
<p><img data-src="../../codes/git/merge_before2.png" alt="gif" /></p>
<center>
<strong>图16：运行git merge main前的情况</strong>
</center>
<p><img data-src="../../codes/git/merge_after2.png" alt="gif" /></p>
<center>
<strong>图17：运行git merge main后的情况，正宗融合</strong>
</center>
<h3 id="git-cherry-pick"><a class="markdownIt-Anchor" href="#git-cherry-pick"></a> <code>git cherry-pick</code></h3>
<p><code>git cherry-pick</code>命令&quot;复制&quot;一个提交节点并在当前分支做一次完全一样的新提交。</p>
<h3 id="git-rebase"><a class="markdownIt-Anchor" href="#git-rebase"></a> <code>git rebase</code></h3>
<p>衍合是合并命令的另一种选择。合并把两个父分支合并进行一次提交，提交历史不是线性的。衍合在当前分支上重演另一个分支的历史，提交历史是线性的。 本质上，这是线性化的自动的 <code>cherry-pick</code>。</p>
<h2 id="参考网站"><a class="markdownIt-Anchor" href="#参考网站"></a> 参考网站</h2>
<ul>
<li><a href="https://wyag.thb.lt/#org2e1a2c4">Write yourself a Git!</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/96631135">这才是真正的Git——Git内部原理揭秘！</a></li>
<li><a href="https://learngitbranching.js.org/?locale=zh_CN">Learn Git Branch</a></li>
</ul>
]]></content>
      <categories>
        <category>Linux操作系统</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>使用命令行的艺术2 - 重定向和文件管理</title>
    <url>/2025/03/12/2025-3-12-Command%20Line%20Art2-%E9%87%8D%E5%AE%9A%E5%90%91%E5%92%8C%E6%96%87%E4%BB%B6%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<p>熟练使用命令行是一种常常被忽视，或被认为难以掌握的技能，但实际上，它会提高你作为工程师的灵活性以及生产力。本文旨在了解命令行中的重定向功能和文件管理功能。</p>
<span id="more"></span>
<h2 id="重定向redirect"><a class="markdownIt-Anchor" href="#重定向redirect"></a> 重定向（redirect）</h2>
<h3 id="输入输出重定向"><a class="markdownIt-Anchor" href="#输入输出重定向"></a> 输入输出重定向</h3>
<p><code>&gt;</code>覆盖，<code>&gt;&gt;</code>累加</p>
<ol>
<li>标准输入　　(stdin) ：代码为<code>0</code>，使用<code>&lt;</code>或<code>&lt;&lt;</code>；</li>
<li>标准输出　　(stdout)：代码为<code>1</code>，使用<code>&gt;</code>或<code>&gt;&gt;</code>；</li>
<li>标准错误输出(stderr)：代码为<code>2</code>，使用<code>2&gt;</code>或<code>2&gt;&gt;</code>；</li>
</ol>
<p><code>2&gt;&amp;1</code>来将<code>2&gt;</code>转到<code>1&gt;</code>去，而<code>1&gt;&amp;2</code>来将<code>1&gt;</code>转到<code>2&gt;</code>去。（感觉像是首要定性，先分配好stdout和stderr的走向）<br />
 <code>2&gt;&amp;1</code>:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">type</span> list &gt; list </span><br><span class="line"><span class="comment"># bash: type: list: not found</span></span><br><span class="line"><span class="built_in">cat</span> list</span><br><span class="line"><span class="built_in">type</span> list &gt; list 2&gt;&amp;1</span><br><span class="line"><span class="built_in">cat</span> list</span><br><span class="line"><span class="comment"># bash: type: list: not found </span></span><br><span class="line"><span class="built_in">type</span> <span class="built_in">cd</span> &gt; list 2&gt;&amp;1</span><br><span class="line"><span class="comment"># cd is a shell builtin</span></span><br></pre></td></tr></table></figure>
<p><code>1&gt;&amp;2</code>:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">&quot;error message&quot;</span> 1&gt;&amp;2</span><br><span class="line"><span class="comment"># error message</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;error message&quot;</span> 2&gt; /dev/null 1&gt;&amp;2</span><br></pre></td></tr></table></figure>
<h3 id="管道重定向"><a class="markdownIt-Anchor" href="#管道重定向"></a> 管道重定向</h3>
<p><code>|</code>：仅能处理经由前面一个指令传来的正确信息，也就是 stdout 的信息，对于 stderr并没有直接处理的能力；必须要能够接受来自前一个指令的数据成为 standard input 继续处理才行。</p>
<p>常用的管道命令<code>grep</code>:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">grep [-acinv] [--color=auto] <span class="string">&#x27;pattern&#x27;</span> filename</span><br><span class="line">选项与参数：</span><br><span class="line">-i ：忽略大小写的不同，所以大小写视为相同</span><br><span class="line">-o ：只输出匹配的部分而不是整行</span><br><span class="line">-v ：反向选择，亦即显示出没有匹配<span class="string">&#x27;pattern&#x27;</span>的字符串内容的那些行！</span><br><span class="line">-A ：显示匹配行+匹配行行后的几行(after)</span><br><span class="line">-B ：显示匹配行+匹配行行前的几行(before)</span><br><span class="line">-C ：显示匹配行+匹配行行前后的几行(context)</span><br><span class="line">--color=auto ：可以将找到的关键字部分加上颜色的显示喔！</span><br><span class="line"><span class="comment"># 将有出现 root 的那一行就取出来；</span></span><br><span class="line">last | grep <span class="string">&#x27;root&#x27;</span></span><br><span class="line"><span class="comment"># 只要没有 root 的就取出！</span></span><br><span class="line">last | grep -v <span class="string">&#x27;root&#x27;</span></span><br><span class="line"><span class="comment"># </span></span><br><span class="line"><span class="comment"># wtmp begins Tue Mar 11 22:41:15 2025</span></span><br><span class="line"><span class="comment"># 在 last 的输出消息中，只要有 root 就取出，并且仅取第一栏</span></span><br><span class="line">last | grep -v <span class="string">&#x27;root&#x27;</span> | <span class="built_in">cut</span> -d <span class="string">&#x27; &#x27;</span> -f 1</span><br><span class="line"><span class="comment"># </span></span><br><span class="line"><span class="comment"># wtmp</span></span><br><span class="line"><span class="comment"># 在取出 root 之后，利用上个指令 cut 的处理，就能够仅取得第一栏啰！</span></span><br><span class="line"><span class="comment"># 神奇的是，如果加上 --color=auto 的选项，找到的关键字部分会用特殊颜色显示喔！</span></span><br></pre></td></tr></table></figure>
<p>常用的管道命令<code>awk</code>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">awk [-F] [delimiter] <span class="string">&#x27;pattern &#123;action&#125;&#x27;</span> filename</span><br><span class="line">选项与参数：</span><br><span class="line">-F ：制定分隔符，默认为空格或制表符</span><br><span class="line"><span class="comment"># 列出当前GPU下所有的python进程号</span></span><br><span class="line">nvidia-smi | grep python | awk <span class="string">&#x27;&#123;print $5&#125;&#x27;</span></span><br><span class="line"><span class="comment"># 18108</span></span><br><span class="line"><span class="comment"># 18737</span></span><br><span class="line"><span class="comment"># 19383</span></span><br><span class="line"><span class="comment"># 19565</span></span><br><span class="line"><span class="comment"># 19725</span></span><br><span class="line"><span class="comment"># 19799</span></span><br><span class="line"><span class="comment"># 19859</span></span><br><span class="line"><span class="comment"># 19977</span></span><br><span class="line"><span class="comment"># 20550</span></span><br><span class="line"><span class="comment"># 20733</span></span><br></pre></td></tr></table></figure>
<p><code>xargs + |</code>：<code>xargs</code>可以读入stdin的数据，并且以空格或换行字符作为分辨，将stdin的数据分隔成为arguments。</p>
<p><code>xargs</code>搭配<code>｜</code>更加美味😋：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">command1 | xargs [-ndI&#123;&#125;] command2</span><br><span class="line">选项与参数：</span><br><span class="line">-n  ：后面接次数，每次command2指令运行时，要使用几个参数的意思。</span><br><span class="line">-d  ：指定定界符，默认是空格或换行符。delimiter</span><br><span class="line">-I&#123;&#125;：指定替换字符串。每次读取一行数据并将&#123;&#125;替换为该行数据。</span><br><span class="line"><span class="built_in">cat</span> hosts</span><br><span class="line"><span class="comment"># delkon</span></span><br><span class="line"><span class="comment"># study</span></span><br><span class="line"><span class="comment"># flypig</span></span><br><span class="line"><span class="comment"># ikun</span></span><br><span class="line"><span class="comment"># daniel</span></span><br><span class="line"><span class="built_in">cat</span> hosts | xargs -I&#123;&#125; <span class="built_in">echo</span> root@&#123;&#125;</span><br><span class="line"><span class="comment"># root@delkon</span></span><br><span class="line"><span class="comment"># root@study</span></span><br><span class="line"><span class="comment"># root@flypig</span></span><br><span class="line"><span class="comment"># root@ikun</span></span><br><span class="line"><span class="comment"># root@daniel</span></span><br><span class="line"><span class="built_in">cat</span> hosts | xargs -n 2 <span class="built_in">echo</span></span><br><span class="line"><span class="comment"># delkon study</span></span><br><span class="line"><span class="comment"># flypig ikun</span></span><br><span class="line"><span class="comment"># daniel</span></span><br><span class="line"><span class="built_in">ls</span></span><br><span class="line"><span class="comment"># dataloaders.py  lr_scheduler.py  torch_utils.py</span></span><br><span class="line">find . -name <span class="string">&#x27;*.py&#x27;</span> | xargs grep <span class="string">&#x27;matplotlib&#x27;</span></span><br><span class="line"><span class="comment"># ./dataloaders.py:import matplotlib.pyplot as plt</span></span><br><span class="line"><span class="comment"># ./dataloaders.py:from matplotlib import rcParams</span></span><br></pre></td></tr></table></figure>
<h2 id="文件管理"><a class="markdownIt-Anchor" href="#文件管理"></a> 文件管理</h2>
<h3 id="ls-l展示文件属性"><a class="markdownIt-Anchor" href="#ls-l展示文件属性"></a> <code>ls -l</code>展示文件属性</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">ls</span> -l </span><br><span class="line"><span class="comment"># total 64</span></span><br><span class="line"><span class="comment"># lrwxrwxrwx   1 root root    7 Apr 22  2024 bin -&gt; usr/bin</span></span><br><span class="line"><span class="comment"># drwxr-xr-x   2 root root 4096 Apr 22  2024 boot</span></span><br><span class="line"><span class="comment"># drwxr-xr-x   5 root root  360 Mar 13 15:38 dev</span></span><br><span class="line"><span class="comment"># drwxr-xr-x   1 root root 4096 Mar 12 20:33 etc</span></span><br><span class="line"><span class="comment"># drwxr-xr-x   1 root root 4096 Jan 27 10:19 home</span></span><br><span class="line"><span class="comment"># lrwxrwxrwx   1 root root    7 Apr 22  2024 lib -&gt; usr/lib</span></span><br><span class="line"><span class="comment"># drwxr-xr-x   2 root root 4096 Jan 27 10:12 media</span></span><br><span class="line"><span class="comment"># drwxr-xr-x   2 root root 4096 Jan 27 10:12 mnt</span></span><br><span class="line"><span class="comment"># drwxr-xr-x   2 root root 4096 Jan 27 10:12 opt</span></span><br><span class="line"><span class="comment"># dr-xr-xr-x 213 root root    0 Mar 13 15:38 proc</span></span><br><span class="line"><span class="comment"># drwx------   1 root root 4096 Mar 13 16:25 root</span></span><br><span class="line"><span class="comment"># drwxr-xr-x   4 root root 4096 Jan 27 10:19 run</span></span><br><span class="line"><span class="comment"># lrwxrwxrwx   1 root root    8 Apr 22  2024 sbin -&gt; usr/sbin</span></span><br><span class="line"><span class="comment"># drwxr-xr-x   2 root root 4096 Jan 27 10:12 srv</span></span><br><span class="line"><span class="comment"># dr-xr-xr-x  11 root root    0 Mar 13 15:51 sys</span></span><br><span class="line"><span class="comment"># drwxrwxrwt   1 root root 4096 Mar 12 17:30 tmp</span></span><br><span class="line"><span class="comment"># drwxr-xr-x   1 root root 4096 Jan 27 10:12 usr</span></span><br><span class="line"><span class="comment"># drwxr-xr-x   1 root root 4096 Jan 27 10:19 var</span></span><br></pre></td></tr></table></figure>
<p>第一列总共10位</p>
<ul>
<li>第一位表示文件类型，可以有以下几种字母表示：
<ul>
<li><code>-</code>：普通文件</li>
<li><code>d</code>：目录</li>
<li><code>l</code>：符号连接</li>
<li><code>b</code>：块设备文件</li>
<li><code>c</code>：字符设备文件</li>
<li><code>s</code>：套接字</li>
<li><code>p</code>：命名管道</li>
</ul>
</li>
<li>剩下9位分为3组，每组3位分别有以下权限<code>r</code>（可读）<code>w</code>（可写）<code>x</code>（可执行）
<ul>
<li>2-4位：文件属主（owner）的权限</li>
<li>5-7位：同组用户（group）的权限</li>
<li>8-10位：其他用户（others）的权限</li>
</ul>
</li>
</ul>
<p>第二列表示硬链接数，硬链接是指向同一个文件的多个文件名。对于目录，这个数字表示目录下的子目录数量加 2（当前目录 . 和父目录 …）。</p>
<p>第三列和第四列分别表示文件属主和文件属组。</p>
<p>第五列表示文件大小，以字节为单位。如果文件是符号链接，大小为链接目标的路径长度（如 bin -&gt; usr/bin 中，大小为 7 是因为 usr/bin 有 7 个字符）。如果是目录，则一般固定为 4096 字节。</p>
<p>第六列第七列第八列表示最后修改时间。</p>
<p>第九列表示文件名，如果是符号链接（如 bin -&gt; usr/bin），则后面会显示链接的目标。</p>
<h3 id="ln进行符号链接和硬链接"><a class="markdownIt-Anchor" href="#ln进行符号链接和硬链接"></a> <code>ln</code>进行符号链接和硬链接</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">ln</span> [-sf] src dst</span><br><span class="line">选项与参数：</span><br><span class="line">-s  ：创建符号链接。</span><br><span class="line">-f  ：强制创建链接文件，覆盖已经存在的目标文件。</span><br><span class="line"><span class="built_in">ls</span></span><br><span class="line"><span class="comment"># Python  hosts</span></span><br><span class="line"><span class="built_in">ln</span> hosts hardlink_hosts</span><br><span class="line"><span class="built_in">ls</span></span><br><span class="line"><span class="comment"># Python  hardlink_hosts  hosts</span></span><br><span class="line"><span class="built_in">ln</span> -s hosts symboliclink_hosts</span><br><span class="line"><span class="built_in">ls</span> </span><br><span class="line"><span class="comment"># Python  hardlink_hosts  hosts  symboliclink_hosts</span></span><br><span class="line"><span class="built_in">ls</span> -l </span><br><span class="line"><span class="comment"># drwxr-xr-x 2 root root 4096 Mar 13 16:25 Python</span></span><br><span class="line"><span class="comment"># -rw-r--r-- 2 root root   32 Mar 13 16:09 hardlink_hosts</span></span><br><span class="line"><span class="comment"># -rw-r--r-- 2 root root   32 Mar 13 16:09 hosts</span></span><br><span class="line"><span class="comment"># lrwxrwxrwx 1 root root    5 Mar 13 17:25 symboliclink_hosts -&gt; hosts</span></span><br><span class="line"><span class="built_in">cat</span> hosts</span><br><span class="line"><span class="built_in">cat</span> hardlink_hosts</span><br><span class="line"><span class="built_in">cat</span> symboliclink_hosts</span><br><span class="line"><span class="comment"># delkon</span></span><br><span class="line"><span class="comment"># study</span></span><br><span class="line"><span class="comment"># flypig</span></span><br><span class="line"><span class="comment"># ikun</span></span><br><span class="line"><span class="comment"># daniel</span></span><br></pre></td></tr></table></figure>
<table>
<thead>
<tr>
<th><strong>特性</strong></th>
<th><strong>硬链接（Hard Link）</strong></th>
<th><strong>符号链接（Symbolic Link）</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>指向</td>
<td>指向文件的物理数据块</td>
<td>指向文件的路径</td>
</tr>
<tr>
<td>文件大小</td>
<td>与原文件相同</td>
<td>符号链接本身只占用少量空间（保存路径）</td>
</tr>
<tr>
<td>文件系统限制</td>
<td>只能在同一文件系统上创建</td>
<td>可以跨文件系统创建</td>
</tr>
<tr>
<td>删除效果</td>
<td>删除硬链接不会删除文件数据，除非所有硬链接被删除</td>
<td>删除符号链接不会影响原文件</td>
</tr>
<tr>
<td>适用场景</td>
<td>文件备份，防止意外删除</td>
<td>快捷访问其他文件、跨目录、文件系统使用</td>
</tr>
</tbody>
</table>
<h3 id="硬盘"><a class="markdownIt-Anchor" href="#硬盘"></a> 硬盘</h3>
<p>硬盘的使用：硬盘分区<code>-&gt;</code>硬盘格式化（format)<code>-&gt;</code>文件系统的挂载</p>
<h4 id="硬盘分区"><a class="markdownIt-Anchor" href="#硬盘分区"></a> 硬盘分区</h4>
<ul>
<li><code>/dev/sd[a-p][1-128]</code>：为分区后的实体硬盘分配的硬盘文件名；</li>
<li><code>/dev/vd[a-d][1-128]</code>：为分区后的虚拟硬盘分配的硬盘文件名。</li>
</ul>
<p>本系统下的所有磁盘与磁盘内的分割信息使用<code>lsblk</code>(list block device)查看</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lsblk [-dfimpt] [device]</span><br><span class="line">选项与参数：</span><br><span class="line">-d  ：仅列出磁盘本身，并不会列出该磁盘的分割数据</span><br><span class="line">-f  ：同时列出该磁盘内的文件系统名称</span><br><span class="line">-i  ：使用 ASCII 的线段输出，不要使用复杂的编码 (再某些环境下很有用)</span><br><span class="line">-m  ：同时输出该设备在 /dev 底下的权限数据 (rwx 的数据)</span><br><span class="line">-p  ：列出该设备的完整文件名！而不是仅列出最后的名字而已。</span><br><span class="line">-t  ：列出该磁盘设备的详细数据，包括磁盘队列机制、预读写的数据量大小等</span><br><span class="line">lsblk</span><br><span class="line"><span class="comment"># NAME   MAJ:MIN RM   SIZE RO TYPE MOUNTPOINTS</span></span><br><span class="line"><span class="comment"># nbd0    43:0    0     0B  0 disk</span></span><br><span class="line"><span class="comment"># nbd1    43:32   0     0B  0 disk</span></span><br><span class="line"><span class="comment"># nbd2    43:64   0     0B  0 disk</span></span><br><span class="line"><span class="comment"># nbd3    43:96   0     0B  0 disk</span></span><br><span class="line"><span class="comment"># nbd4    43:128  0     0B  0 disk</span></span><br><span class="line"><span class="comment"># nbd5    43:160  0     0B  0 disk</span></span><br><span class="line"><span class="comment"># nbd6    43:192  0     0B  0 disk</span></span><br><span class="line"><span class="comment"># nbd7    43:224  0     0B  0 disk</span></span><br><span class="line"><span class="comment"># vda    254:0    0  59.6G  0 disk</span></span><br><span class="line"><span class="comment"># `-vda1 254:1    0  59.6G  0 part /etc/hosts</span></span><br><span class="line"><span class="comment">#                                  /etc/hostname</span></span><br><span class="line"><span class="comment">#                                  /etc/resolv.conf</span></span><br><span class="line"><span class="comment"># vdb    254:16   0 535.2M  1 disk</span></span><br><span class="line"><span class="comment"># nbd8    43:256  0     0B  0 disk</span></span><br><span class="line"><span class="comment"># nbd9    43:288  0     0B  0 disk</span></span><br><span class="line"><span class="comment"># nbd10   43:320  0     0B  0 disk</span></span><br><span class="line"><span class="comment"># nbd11   43:352  0     0B  0 disk</span></span><br><span class="line"><span class="comment"># nbd12   43:384  0     0B  0 disk</span></span><br><span class="line"><span class="comment"># nbd13   43:416  0     0B  0 disk</span></span><br><span class="line"><span class="comment"># nbd14   43:448  0     0B  0 disk</span></span><br><span class="line"><span class="comment"># nbd15   43:480  0     0B  0 disk</span></span><br></pre></td></tr></table></figure>
<ul>
<li>NAME：就是设备的文件名！会省略 /dev 等前导目录！</li>
<li>MAJ:MIN：其实核心认识的设备都是透过这两个代码来熟悉的！分别是主要：次要设备代码！</li>
<li>RM：是否为可卸载设备 (removable device)，如光盘、USB 磁盘等等</li>
<li>SIZE：当然就是容量啰！</li>
<li>RO：是否为唯读设备的意思</li>
<li>TYPE：是磁盘 (disk)、分区 (partition) 还是唯读内存 (rom) 等输出</li>
<li>MOUTPOINT：就是挂载点！</li>
</ul>
<h4 id="硬盘格式化"><a class="markdownIt-Anchor" href="#硬盘格式化"></a> 硬盘格式化</h4>
<p>分割完毕后自然就是要进行文件系统的格式化，我们使用<code>mkfs</code>(make filesystem)来进行分区后硬盘的格式化。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mkfs[tab][tab]</span><br><span class="line"><span class="comment"># mkfs         mkfs.cramfs  mkfs.ext3    mkfs.minix</span></span><br><span class="line"><span class="comment"># mkfs.bfs     mkfs.ext2    mkfs.ext4</span></span><br></pre></td></tr></table></figure>
<h4 id="文件系统挂载"><a class="markdownIt-Anchor" href="#文件系统挂载"></a> 文件系统挂载</h4>
<p>我们首先要明确，挂载点是目录，这个目录是进入硬盘分区（文件系统）的入口。同时挂载之前我们还应该确定一些事：</p>
<ul>
<li>单一文件系统不应该被重复挂载在不同的挂载点(目录)中；</li>
<li>单一目录不应该重复挂载多个文件系统；</li>
<li>要作为挂载点的目录，理论上应该都是空目录才是。</li>
</ul>
<p>挂载使用的命令是<code>mount</code>，具体如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mount -a</span><br><span class="line">mount [-l]</span><br><span class="line">mount [-t 文件系统] LABEL=<span class="string">&#x27;&#x27;</span>  挂载点</span><br><span class="line">mount [-t 文件系统] UUID=<span class="string">&#x27;&#x27;</span>   挂载点  <span class="comment"># 鸟哥近期建议用这种方式喔！</span></span><br><span class="line">mount [-t 文件系统] 设备文件名  挂载点</span><br><span class="line">选项与参数：</span><br><span class="line">-a  ：依照设置档 [/etc/fstab](https://muicoder.github.io/linux_basic/0230filesystem.html#fstab)的数据将所有未挂载的磁盘都挂载上来</span><br><span class="line">-l  ：单纯的输入 mount 会显示目前挂载的信息。加上 -l 可增列 Label 名称！</span><br><span class="line">-t  ：可以加上文件系统种类来指定欲挂载的类型。常见的 Linux 支持类型有：xfs, ext3, ext4, reiserfs, vfat, iso9660(光盘格式), nfs, cifs, smbfs (后三种为网络文件系统类型)</span><br></pre></td></tr></table></figure>
<h4 id="设备文件的卸载"><a class="markdownIt-Anchor" href="#设备文件的卸载"></a> 设备文件的卸载</h4>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">umount [-fn] 设备文件名或挂载点</span><br><span class="line">选项与参数：</span><br><span class="line">-f  ：强制卸载！可用在类似网络文件系统 (NFS) 无法读取到的情况下；</span><br><span class="line">-l  ：立刻卸载文件系统，比 -f 还强！</span><br><span class="line">-n  ：不更新 /etc/mtab 情况下卸载。</span><br></pre></td></tr></table></figure>
<h4 id="硬盘状态"><a class="markdownIt-Anchor" href="#硬盘状态"></a> 硬盘状态</h4>
<p>df（<strong>disk free</strong>）用于显示<strong>磁盘的整体使用情况</strong>。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">df</span> [-akmhHTi] [目录或文件名]</span><br><span class="line">选项与参数：</span><br><span class="line">-a  ：列出所有的文件系统，包括系统特有的 /proc 等文件系统；</span><br><span class="line">-k  ：以 KBytes 的容量显示各文件系统；</span><br><span class="line">-m  ：以 MBytes 的容量显示各文件系统；</span><br><span class="line">-h  ：以人们较易阅读的 GBytes, MBytes, KBytes 等格式自行显示；</span><br><span class="line">-H  ：以 M=1000K 取代 M=1024K 的进位方式；</span><br><span class="line">-T  ：连同该 partition 的 filesystem 名称 (例如 xfs) 也列出；</span><br><span class="line">-i  ：不用磁盘容量，而以 inode 的数量来显示</span><br><span class="line"><span class="built_in">df</span> -h</span><br><span class="line"><span class="comment"># Filesystem      Size  Used Avail Use% Mounted on</span></span><br><span class="line"><span class="comment"># overlay          59G  1.3G   55G   3% /</span></span><br><span class="line"><span class="comment"># tmpfs            64M     0   64M   0% /dev</span></span><br><span class="line"><span class="comment"># shm              64M     0   64M   0% /dev/shm</span></span><br><span class="line"><span class="comment"># /dev/vda1        59G  1.3G   55G   3% /etc/hosts</span></span><br><span class="line"><span class="comment"># tmpfs           2.0G     0  2.0G   0% /proc/scsi</span></span><br><span class="line"><span class="comment"># tmpfs           2.0G     0  2.0G   0% /sys/firmware</span></span><br></pre></td></tr></table></figure>
<p>du（<strong>disk usage</strong>）用于<strong>查看文件和目录的磁盘使用情况</strong>。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">du</span> [-ahskm] 文件或目录名称</span><br><span class="line">-a  ：列出所有的文件与目录容量，因为缺省仅统计目录底下的文件量而已。</span><br><span class="line">-h  ：以人们较易读的容量格式 (G/M) 显示；</span><br><span class="line">-s  ：列出总量而已，而不列出每个各别的目录占用容量；</span><br><span class="line">-S  ：不包括子目录下的总计，与 -s 有点差别。</span><br><span class="line">-k  ：以 KBytes 列出容量显示；</span><br><span class="line">-m  ：以 MBytes 列出容量显示；</span><br></pre></td></tr></table></figure>
<h3 id="linux文件"><a class="markdownIt-Anchor" href="#linux文件"></a> Linux文件</h3>
<h4 id="文件属性和权限的更改"><a class="markdownIt-Anchor" href="#文件属性和权限的更改"></a> 文件属性和权限的更改</h4>
<p>• <strong>chown</strong> 用于更改文件或目录的所有者和组。要注意的是， 用户必须是已经存在系统中的帐号，也就是在<code>/etc/passwd</code> 这个文件中有纪录的用户名才能改变。可更改的组名也记录在<code>/etc/group</code>中。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">chown</span> [-R] 用户 文件或目录</span><br><span class="line"><span class="built_in">chown</span> [-R] 用户:组 文件或目录</span><br><span class="line">选项与参数：</span><br><span class="line">-R : 进行递归(recursive)的持续变更，亦即连同次目录下的所有文件都变更</span><br><span class="line">ll </span><br><span class="line"><span class="comment"># total 32</span></span><br><span class="line"><span class="comment"># drwxr-xr-x 2 ubuntu ubuntu 4096 Mar 14 11:24 ./</span></span><br><span class="line"><span class="comment"># drwxr-x--- 1 ubuntu ubuntu 4096 Mar 14 11:24 ../</span></span><br><span class="line"><span class="comment"># -rw-r--r-- 1 ubuntu ubuntu 8733 Mar 13 16:25 dataloaders.py</span></span><br><span class="line"><span class="comment"># -rw-r--r-- 1 ubuntu ubuntu 4231 Mar 13 16:24 lr_scheduler.py</span></span><br><span class="line"><span class="comment"># -rw-r--r-- 1 ubuntu ubuntu 4043 Mar 13 16:24 torch_utils.py</span></span><br><span class="line"><span class="built_in">chown</span> -R root:root ./</span><br><span class="line">ll </span><br><span class="line"><span class="comment"># total 32</span></span><br><span class="line"><span class="comment"># drwxr-xr-x 2 root   root   4096 Mar 14 11:27 ./</span></span><br><span class="line"><span class="comment"># drwxr-x--- 1 ubuntu ubuntu 4096 Mar 14 11:24 ../</span></span><br><span class="line"><span class="comment"># -rw-r--r-- 1 root   root   8748 Mar 14 11:27 dataloaders.py</span></span><br><span class="line"><span class="comment"># -rw-r--r-- 1 root   root   4231 Mar 13 16:24 lr_scheduler.py</span></span><br><span class="line"><span class="comment"># -rw-r--r-- 1 root   root   4043 Mar 13 16:24 torch_utils.py</span></span><br></pre></td></tr></table></figure>
<p>• <strong>chmod</strong> 用于更改文件或目录的权限，控制读、写、执行权限。</p>
<ol>
<li>数字</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">chmod</span> [-R] xyz 文件或目录</span><br><span class="line">选项与参数：</span><br><span class="line">xyz : 就是刚刚提到的数字类型的权限属性，为 r(4)w(2)x(1) 属性数值的相加。</span><br><span class="line">-R : 进行递归(recursive)的持续变更，亦即连同次目录下的所有文件都会变更</span><br><span class="line">ll ./dataloader.py</span><br><span class="line"><span class="comment"># -rw-r--r-- 1 root   root   8748 Mar 14 11:27 dataloaders.py</span></span><br><span class="line"><span class="built_in">chmod</span> a+w ./dataloader.py</span><br><span class="line">ll ./dataloader.py</span><br><span class="line"><span class="comment"># -rw-rw-rw- 1 root   root   8748 Mar 14 11:27 dataloaders.py</span></span><br><span class="line"><span class="built_in">chmod</span> u=rw.go=r ./dataloader.py</span><br><span class="line">ll ./dataloader.py</span><br><span class="line"><span class="comment"># -rw-r--r-- 1 root root 8748 Mar 14 11:27 ./dataloaders.py</span></span><br></pre></td></tr></table></figure>
<ol start="2">
<li>符号</li>
</ol>
<table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody>
<tr>
<td>chmod</td>
<td>u  (user)<br>g  (group)<br>o  (others)<br>a  (all)</td>
<td>+(加入)  <br>-(除去)  <br>=(设置)</td>
<td>r  <br>w  <br>x</td>
<td>文件或目录</td>
</tr>
</tbody>
</table>
<h4 id="文件搜索"><a class="markdownIt-Anchor" href="#文件搜索"></a> 文件搜索</h4>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">find [PATH] [option] [action]</span><br><span class="line">选项与参数：</span><br><span class="line">与文件权限及名称有关的参数：</span><br><span class="line">-name filename：搜索文件名称为 filename 的文件；</span><br><span class="line">-size [+-]SIZE：搜索比 SIZE 还要大(+)或小(-)的文件。这个 SIZE 的规格有：c: 代表 byte， k: 代表 1024bytes。所以，要找比 50KB还要大的文件，就是『 -size +50k 』</span><br><span class="line">-<span class="built_in">type</span> TYPE    ：搜索文件的类型为 TYPE 的，类型主要有：一般正规文件 (f), 设备文件 (b, c), 目录 (d), 链接档 (l), socket (s), 及 FIFO (p) 等属性。</span><br><span class="line">ll /Python</span><br><span class="line"><span class="comment"># total 32</span></span><br><span class="line"><span class="comment"># drwxr-xr-x 2 root   root   4096 Mar 14 11:40 ./</span></span><br><span class="line"><span class="comment"># drwxr-x--- 1 ubuntu ubuntu 4096 Mar 14 12:50 ../</span></span><br><span class="line"><span class="comment"># -rw-r--r-- 1 root   root   8748 Mar 14 11:27 dataloaders.py</span></span><br><span class="line"><span class="comment"># -rw-r--r-- 1 root   root   4231 Mar 13 16:24 lr_scheduler.py</span></span><br><span class="line"><span class="comment"># -rw-r--r-- 1 root   root   4043 Mar 13 16:24 torch_utils.py</span></span><br><span class="line">find Python/ -name <span class="string">&#x27;*.py&#x27;</span></span><br><span class="line"><span class="comment"># Python/torch_utils.py</span></span><br><span class="line"><span class="comment"># Python/lr_scheduler.py</span></span><br><span class="line"><span class="comment"># Python/dataloaders.py</span></span><br><span class="line">find Python/ -size -20k</span><br><span class="line"><span class="comment"># Python/</span></span><br><span class="line"><span class="comment"># Python/torch_utils.py</span></span><br><span class="line"><span class="comment"># Python/lr_scheduler.py</span></span><br><span class="line"><span class="comment"># Python/dataloaders.py</span></span><br><span class="line">find Python/ -<span class="built_in">type</span> f</span><br><span class="line"><span class="comment"># Python/torch_utils.py</span></span><br><span class="line"><span class="comment"># Python/lr_scheduler.py</span></span><br><span class="line"><span class="comment"># Python/dataloaders.py</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Linux操作系统</category>
      </categories>
      <tags>
        <tag>Shell</tag>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>使用命令行的艺术3 - Shell Script相关</title>
    <url>/2025/03/15/2025-3-15-Command%20Line%20Art3-Shell%20Script%E7%9B%B8%E5%85%B3/</url>
    <content><![CDATA[<p>学会如何编写shell script对于编程能力是很大的提升，尤其是在进行大型项目是使用命令行来控制多个文件分别运行的时候，直接bash一下.<code>sh</code>文件可以省下多少重复的操作！往小处说，一些复杂的功能可以由shell script很简单的实现，所以了解shell script还是很有必要的…</p>
<span id="more"></span>
<h2 id="shell-script"><a class="markdownIt-Anchor" href="#shell-script"></a> Shell Script</h2>
<h3 id="运行方式"><a class="markdownIt-Anchor" href="#运行方式"></a> 运行方式</h3>
<ul>
<li>利用bash运行script，在bash的子进程中运行</li>
<li>利用source运行script，在父进程bash中运行<br />
编写<code>showname.sh</code>来对两种运行方法进行测试：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#!bin/bash</span></span><br><span class="line"><span class="comment">#Program:</span></span><br><span class="line"><span class="comment">#       User inputs his first name and last name. Program shows his full name.</span></span><br><span class="line"><span class="comment">#History:</span></span><br><span class="line"><span class="comment">#2025/03/15     delkon  First release as showname.sh</span></span><br><span class="line"></span><br><span class="line">PATH=<span class="variable">$PATH</span>:/home/ubuntu/ShellScript</span><br><span class="line"><span class="built_in">export</span> PATH</span><br><span class="line"><span class="built_in">read</span> -p <span class="string">&quot;Please input your first name:&quot;</span> firstname</span><br><span class="line"><span class="built_in">read</span> -p <span class="string">&quot;Please input your last name:&quot;</span> lastname</span><br><span class="line"><span class="built_in">echo</span> -e <span class="string">&quot;Your full name is: <span class="variable">$&#123;firstname&#125;</span> <span class="variable">$&#123;lastname&#125;</span>&quot;</span></span><br></pre></td></tr></table></figure>
<p>利用bash和source分别运行<code>showname.sh</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">bash showname.sh</span><br><span class="line">Please input your first name:DeLiang</span><br><span class="line">Please input your last name:Kong</span><br><span class="line">Your full name is: DeLiang Kong</span><br><span class="line"><span class="built_in">echo</span> <span class="variable">$&#123;firstname&#125;</span></span><br><span class="line"><span class="comment"># </span></span><br><span class="line"><span class="built_in">source</span> showname.sh</span><br><span class="line">Please input your first name:DeLiang</span><br><span class="line">Please input your last name:Kong</span><br><span class="line">Your full name is: DeLiang Kong</span><br><span class="line"><span class="built_in">echo</span> <span class="variable">$&#123;firstname&#125;</span></span><br><span class="line"><span class="comment"># DeLiang</span></span><br></pre></td></tr></table></figure>
<p>可以看到使用source运行的脚本中定义的变量依然可以在bash环境中被捕捉到。source还可以进行shell文件的配置，影响shell的整体行为。</p>
<h3 id="bash配置文件想要在shell中加载配置文件运行source-xxrc"><a class="markdownIt-Anchor" href="#bash配置文件想要在shell中加载配置文件运行source-xxrc"></a> bash配置文件（想要在shell中加载配置文件运行<code>source/. .xxrc</code>)</h3>
<h4 id="login-shell运行echo-0查看终端名称前带有-"><a class="markdownIt-Anchor" href="#login-shell运行echo-0查看终端名称前带有-"></a> login shell（运行<code>echo $0</code>查看终端名称前带有<code>-</code>）</h4>
<p>全局配置文件：<code>/etc/profile</code>总是会被自动加载。<br />
用户级配置文件：<code>~/.bash_profile</code>，<code>~/.bash_login</code>，<code>~/.profile</code>按照上面的次序进行查找，先查到的被自动加载。</p>
<h4 id="non-login-shell运行echo-0查看终端名称前没有-"><a class="markdownIt-Anchor" href="#non-login-shell运行echo-0查看终端名称前没有-"></a> non-login shell（运行<code>echo $0</code>查看终端名称前没有<code>-</code>）</h4>
<p>只会自动加载<code>~/.bashrc</code>用户级配置文件。</p>
<h3 id="判断"><a class="markdownIt-Anchor" href="#判断"></a> 判断</h3>
<ul>
<li>利用<code>test</code>命令搭配<code>&amp;&amp;</code>和<code>||</code>字符进行选择</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">ls</span></span><br><span class="line"><span class="comment"># hello.sh  showname.sh</span></span><br><span class="line"><span class="built_in">test</span> -e hello.sh &amp;&amp; <span class="built_in">echo</span> <span class="string">&quot;exist&quot;</span> || <span class="built_in">echo</span> <span class="string">&quot;Not exist&quot;</span></span><br><span class="line"><span class="comment"># exist</span></span><br><span class="line"><span class="built_in">test</span> -e file.sh &amp;&amp; <span class="built_in">echo</span> <span class="string">&quot;exist&quot;</span> || <span class="built_in">echo</span> <span class="string">&quot;Not exist&quot;</span></span><br><span class="line"><span class="comment"># Not exist</span></span><br></pre></td></tr></table></figure>
<p><code>test</code>命令详解：</p>
<table>
<thead>
<tr>
<th></th>
<th></th>
</tr>
</thead>
<tbody>
<tr>
<td>测试的标志</td>
<td>代表意义</td>
</tr>
<tr>
<td>1. 关于某个文件名的『文件类型』判断，如 test -e filename 表示存在否</td>
<td></td>
</tr>
<tr>
<td>-e</td>
<td>该『文件名』是否存在(exist)？(常用)</td>
</tr>
<tr>
<td>-f</td>
<td>该『文件名』是否存在且为文件(file)？(常用)</td>
</tr>
<tr>
<td>-d</td>
<td>该『文件名』是否存在且为目录(directory)？(常用)</td>
</tr>
<tr>
<td>-b</td>
<td>该『文件名』是否存在且为一个 block device 设备？</td>
</tr>
<tr>
<td>-c</td>
<td>该『文件名』是否存在且为一个 character device 设备？</td>
</tr>
<tr>
<td>-S</td>
<td>该『文件名』是否存在且为一个 Socket 文件？</td>
</tr>
<tr>
<td>-p</td>
<td>该『文件名』是否存在且为一个 FIFO (pipe) 文件？</td>
</tr>
<tr>
<td>-L</td>
<td>该『文件名』是否存在且为一个链接档？</td>
</tr>
<tr>
<td>2. 关于文件的权限侦测，如 test -r filename 表示可读否 (但 root 权限常有例外)</td>
<td></td>
</tr>
<tr>
<td>-r</td>
<td>侦测该文件名是否存在且具有『可读』的权限？</td>
</tr>
<tr>
<td>-w</td>
<td>侦测该文件名是否存在且具有『可写』的权限？</td>
</tr>
<tr>
<td>-x</td>
<td>侦测该文件名是否存在且具有『可运行』的权限？</td>
</tr>
<tr>
<td>-u</td>
<td>侦测该文件名是否存在且具有『SUID』的属性？</td>
</tr>
<tr>
<td>-g</td>
<td>侦测该文件名是否存在且具有『SGID』的属性？</td>
</tr>
<tr>
<td>-k</td>
<td>侦测该文件名是否存在且具有『Sticky bit』的属性？</td>
</tr>
<tr>
<td>-s</td>
<td>侦测该文件名是否存在且为『非空白文件』？</td>
</tr>
<tr>
<td>3. 两个文件之间的比较，如： test file1 -nt file2</td>
<td></td>
</tr>
<tr>
<td>-nt</td>
<td>(newer than)判断 file1 是否比 file2 新</td>
</tr>
<tr>
<td>-ot</td>
<td>(older than)判断 file1 是否比 file2 旧</td>
</tr>
<tr>
<td>-ef</td>
<td>判断 file1 与 file2 是否为同一文件，可用在判断 hard link 的判定上。 主要意义在判定，两个文件是否均指向同一个 inode 哩！</td>
</tr>
<tr>
<td>4. 关于两个整数之间的判定，例如 test n1 -eq n2</td>
<td></td>
</tr>
<tr>
<td>-eq</td>
<td>两数值相等 (equal)</td>
</tr>
<tr>
<td>-ne</td>
<td>两数值不等 (not equal)</td>
</tr>
<tr>
<td>-gt</td>
<td>n1 大于 n2 (greater than)</td>
</tr>
<tr>
<td>-lt</td>
<td>n1 小于 n2 (less than)</td>
</tr>
<tr>
<td>-ge</td>
<td>n1 大于等于 n2 (greater than or equal)</td>
</tr>
<tr>
<td>-le</td>
<td>n1 小于等于 n2 (less than or equal)</td>
</tr>
<tr>
<td>5. 判定字符串的数据</td>
<td></td>
</tr>
<tr>
<td>test -z string</td>
<td>判定字符串是否为 0 ？若 string 为空字符串，则为 true</td>
</tr>
<tr>
<td>test -n string</td>
<td>判定字符串是否非为 0 ？若 string 为空字符串，则为 false。  <br>注： -n 亦可省略</td>
</tr>
<tr>
<td>test str1 == str2</td>
<td>判定 str1 是否等于 str2 ，若相等，则回传 true</td>
</tr>
<tr>
<td>test str1 != str2</td>
<td>判定 str1 是否不等于 str2 ，若相等，则回传 false</td>
</tr>
<tr>
<td>6. 多重条件判定，例如： test -r filename -a -x filename</td>
<td></td>
</tr>
<tr>
<td>-a</td>
<td>(and)两状况同时成立！例如 test -r file -a -x file，则 file 同时具有 r 与 x 权限时，才回传 true。</td>
</tr>
<tr>
<td>-o</td>
<td>(or)两状况任何一个成立！例如 test -r file -o -x file，则 file 具有 r 或 x 权限时，就可回传 true。</td>
</tr>
<tr>
<td>!</td>
<td>反相状态，如 test ! -x file ，当 file 不具有 x 时，回传 true</td>
</tr>
</tbody>
</table>
<ul>
<li>利用判断符号<code>[]</code>搭配<code>if</code>关键字</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">ls</span></span><br><span class="line"><span class="comment"># hello.sh  showname.sh</span></span><br><span class="line">[ -e hello.sh ] &amp;&amp; <span class="built_in">echo</span> <span class="string">&quot;exist&quot;</span> || <span class="built_in">echo</span> <span class="string">&quot;Not exist&quot;</span></span><br><span class="line"><span class="comment"># exist</span></span><br><span class="line">[ -e file.sh ] &amp;&amp; <span class="built_in">echo</span> <span class="string">&quot;exist&quot;</span> || <span class="built_in">echo</span> <span class="string">&quot;Not exist&quot;</span></span><br><span class="line"><span class="comment"># Not exist</span></span><br></pre></td></tr></table></figure>
<h3 id="缺省变量"><a class="markdownIt-Anchor" href="#缺省变量"></a> 缺省变量</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">script  opt1  opt2  opt3  opt4      </span><br><span class="line"><span class="variable">$0</span>      <span class="variable">$1</span>    <span class="variable">$2</span>    <span class="variable">$3</span>    <span class="variable">$4</span></span><br></pre></td></tr></table></figure>
<p>运行的脚本文件名为 <code>$0</code> 这个变量，第一个接的参数就是 <code>$1</code> ，以此类推。 所以，只要我们在 script 里面善用 <code>$1</code> 的话，就可以很简单的立即下达某些指令功能了！除了这些数字的变量之外， 我们还有一些较为特殊的变量可以在 script 内使用来调用这些参数：</p>
<ul>
<li><code>$#</code> ：代表后接的参数『个数』，以上表为例这里显示为 <code>4</code>；</li>
<li><code>$&#123;!#&#125;</code>：<strong>引用最后一个位置参数的值</strong>，它结合了<code>$#</code>表示参数个数，并用 <code>!</code> 来实现间接引用。这个扩展会取出传递给脚本或函数的最后一个参数。</li>
<li><code>$@</code> ：代表<code>&quot;$1&quot; &quot;$2&quot; &quot;$3&quot; &quot;$4&quot;</code>之意，每个变量是独立的(用双引号括起来)；</li>
<li><code>$*</code> ：代表<code>&quot;$1c$2c$3c$4&quot;</code>，其中 c 为分隔字符，缺省为空白键， 所以本例中代表<code>&quot;$1 $2 $3 $4&quot;</code>之意。</li>
</ul>
<h3 id="条件判断式"><a class="markdownIt-Anchor" href="#条件判断式"></a> 条件判断式</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 多个条件判断 (if ... elif ... elif ... else) 分多种不同情况运行</span></span><br><span class="line"><span class="keyword">if</span> [ 条件判断式一 ]; <span class="keyword">then</span></span><br><span class="line">	当条件判断式一成立时，可以进行的指令工作内容；</span><br><span class="line"><span class="keyword">elif</span> [ 条件判断式二 ]; <span class="keyword">then</span></span><br><span class="line">	当条件判断式二成立时，可以进行的指令工作内容；</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">	当条件判断式一与二均不成立时，可以进行的指令工作内容；</span><br><span class="line"><span class="keyword">fi</span></span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">case</span>  $变量名称 <span class="keyword">in</span>   &lt;==关键字为 <span class="keyword">case</span> ，还有变量前有钱字号</span><br><span class="line">  <span class="string">&quot;第一个变量内容&quot;</span>)   &lt;==每个变量内容建议用双引号括起来，关键字则为小括号 )</span><br><span class="line">	程序段</span><br><span class="line">	;;            &lt;==每个类别结尾使用两个连续的分号来处理！</span><br><span class="line">  <span class="string">&quot;第二个变量内容&quot;</span>)</span><br><span class="line">	程序段</span><br><span class="line">	;;</span><br><span class="line">  *)                  &lt;==最后一个变量内容都会用 * 来代表所有其他值</span><br><span class="line">	不包含第一个变量内容与第二个变量内容的其他程序运行段</span><br><span class="line">	<span class="built_in">exit</span> 1</span><br><span class="line">	;;</span><br><span class="line"><span class="keyword">esac</span>                  &lt;==最终的 <span class="keyword">case</span> 结尾！『反过来写』思考一下！</span><br></pre></td></tr></table></figure>
<h3 id="不定循环"><a class="markdownIt-Anchor" href="#不定循环"></a> 不定循环</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> [ condition ]  &lt;==中括号内的状态就是判断式</span><br><span class="line"><span class="keyword">do</span>            &lt;==<span class="keyword">do</span> 是循环的开始！</span><br><span class="line">	程序段落</span><br><span class="line"><span class="keyword">done</span>          &lt;==<span class="keyword">done</span> 是循环的结束</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">until</span> [ condition ]</span><br><span class="line"><span class="keyword">do</span></span><br><span class="line">	程序段落</span><br><span class="line"><span class="keyword">done</span></span><br></pre></td></tr></table></figure>
<h3 id="固定循环"><a class="markdownIt-Anchor" href="#固定循环"></a> 固定循环</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> var <span class="keyword">in</span> con1 con2 con3 ...</span><br><span class="line"><span class="keyword">do</span></span><br><span class="line">	程序段</span><br><span class="line"><span class="keyword">done</span></span><br></pre></td></tr></table></figure>
<h2 id="vim编辑"><a class="markdownIt-Anchor" href="#vim编辑"></a> Vim编辑</h2>
<p>通常我们使用vim编辑器来编辑shell script脚本，下面对vim操作的一些快捷键进行叙述。</p>
<ul>
<li>第一部份：一般指令模式可用的按钮说明，光标移动、复制粘贴、搜索取代等</li>
</ul>
<table>
<thead>
<tr>
<th></th>
<th></th>
</tr>
</thead>
<tbody>
<tr>
<td>移动光标的方法</td>
<td></td>
</tr>
<tr>
<td>h 或 向左方向键(←)</td>
<td>光标向左移动一个字符</td>
</tr>
<tr>
<td>j 或 向下方向键(↓)</td>
<td>光标向下移动一个字符</td>
</tr>
<tr>
<td>k 或 向上方向键(↑)</td>
<td>光标向上移动一个字符</td>
</tr>
<tr>
<td>l 或 向右方向键(→)</td>
<td>光标向右移动一个字符</td>
</tr>
<tr>
<td>如果你将右手放在键盘上的话，你会发现 hjkl 是排列在一起的，因此可以使用这四个按钮来移动光标。 如果想要进行多次移动的话，例如向下移动 30 列，可以使用 “30j” 或 “30↓” 的组合按键， 亦即加上想要进行的次数(数字)后，按下动作即可！</td>
<td></td>
</tr>
<tr>
<td><font color="#c00000">[Ctrl] + [f]</font></td>
<td>屏幕『向下』移动一页，相当于 [Page Down]按键 (常用)</td>
</tr>
<tr>
<td><font color="#c00000">[Ctrl] + [b]</font></td>
<td>屏幕『向上』移动一页，相当于 [Page Up] 按键 (常用)</td>
</tr>
<tr>
<td>[Ctrl] + [d]</td>
<td>屏幕『向下』移动半页</td>
</tr>
<tr>
<td>[Ctrl] + [u]</td>
<td>屏幕『向上』移动半页</td>
</tr>
<tr>
<td>+</td>
<td>光标移动到非空白字符的下一列</td>
</tr>
<tr>
<td>-</td>
<td>光标移动到非空白字符的上一列</td>
</tr>
<tr>
<td>n<space></td>
<td>那个 n 表示『数字』，例如 20 。按下数字后再按空白键，光标会向右移动这一列的 n 个字符。例如 20<space> 则光标会向后面移动 20 个字符距离。</td>
</tr>
<tr>
<td><font color="#c00000">0 或功能键[Home]</font></td>
<td>这是数字『 0 』：移动到这一列的最前面字符处 (常用)</td>
</tr>
<tr>
<td><font color="#c00000">$ 或功能键[End]</font></td>
<td>移动到这一列的最后面字符处(常用)</td>
</tr>
<tr>
<td>H</td>
<td>光标移动到这个屏幕的最上方那一列的第一个字符</td>
</tr>
<tr>
<td>M</td>
<td>光标移动到这个屏幕的中央那一列的第一个字符</td>
</tr>
<tr>
<td>L</td>
<td>光标移动到这个屏幕的最下方那一列的第一个字符</td>
</tr>
<tr>
<td><font color="#c00000">G</font></td>
<td>移动到这个文件的最后一列(常用)</td>
</tr>
<tr>
<td>nG</td>
<td>n 为数字。移动到这个文件的第 n 列。例如 20G 则会移动到这个文件的第 20 列(可配合 :set nu)</td>
</tr>
<tr>
<td><font color="#c00000">gg</font></td>
<td>移动到这个文件的第一列，相当于 1G 啊！ (常用)</td>
</tr>
<tr>
<td><font color="#c00000">n<code>&lt;Enter&gt;</code></font></td>
<td>n 为数字。光标向下移动 n 列(常用)</td>
</tr>
<tr>
<td>搜索与取代</td>
<td></td>
</tr>
<tr>
<td><font color="#c00000">/word</font></td>
<td>向光标之下寻找一个名称为 word 的字符串。例如要在文件内搜索 vbird 这个字符串，就输入 /vbird 即可！ (常用)</td>
</tr>
<tr>
<td>?word</td>
<td>向光标之上寻找一个字符串名称为 word 的字符串。</td>
</tr>
<tr>
<td>n</td>
<td>这个 n 是英文按键。代表『重复前一个搜索的动作』。举例来说， 如果刚刚我们运行 /vbird 去向下搜索 vbird 这个字符串，则按下 n 后，会向下继续搜索下一个名称为 vbird 的字符串。如果是运行 ?vbird 的话，那么按下 n 则会向上继续搜索名称为 vbird 的字符串！</td>
</tr>
<tr>
<td>N</td>
<td>这个 N 是英文按键。与 n 刚好相反，为『反向』进行前一个搜索动作。 例如 /vbird 后，按下 N 则表示『向上』搜索 vbird 。</td>
</tr>
<tr>
<td>使用 /word 配合 n 及 N 是非常有帮助的！可以让你重复的找到一些你搜索的关键字！</td>
<td></td>
</tr>
<tr>
<td><font color="#c00000">:n1,n2s/word1/word2/g</font></td>
<td>n1 与 n2 为数字。在第 n1 与 n2 列之间寻找 word1 这个字符串，并将该字符串取代为 word2 ！举例来说，在 100 到 200 列之间搜索 vbird 并取代为 VBIRD 则：  <br>『:100,200s/vbird/VBIRD/g』。(常用)</td>
</tr>
<tr>
<td><font color="#c00000">:1,$s/word1/word2/g</font></td>
<td>从第一列到最后一列寻找 word1 字符串，并将该字符串取代为 word2 ！(常用)</td>
</tr>
<tr>
<td><font color="#c00000">:1,$s/word1/word2/gc</font></td>
<td>从第一列到最后一列寻找 word1 字符串，并将该字符串取代为 word2 ！且在取代前显示提示字符给用户确认 (confirm) 是否需要取代！(常用)</td>
</tr>
<tr>
<td>删除、复制与粘贴</td>
<td></td>
</tr>
<tr>
<td><font color="#c00000">x, X</font></td>
<td>在一列字当中，x 为向后删除一个字符 (相当于 [del] 按键)， X 为向前删除一个字符(相当于 [backspace] 亦即是倒退键) (常用)</td>
</tr>
<tr>
<td>nx</td>
<td>n 为数字，连续向后删除 n 个字符。举例来说，我要连续删除 10 个字符， 『10x』。</td>
</tr>
<tr>
<td><font color="#c00000">dd</font></td>
<td>删除光标所在的那一整列(常用)</td>
</tr>
<tr>
<td><font color="#c00000">ndd</font></td>
<td>n 为数字。删除光标所在的向下 n 列，例如 20dd 则是删除 20 列 (常用)</td>
</tr>
<tr>
<td>d1G</td>
<td>删除光标所在到第一列的所有数据</td>
</tr>
<tr>
<td>dG</td>
<td>删除光标所在到最后一列的所有数据</td>
</tr>
<tr>
<td>d$</td>
<td>删除光标所在处，到该列的最后一个字符</td>
</tr>
<tr>
<td>d0</td>
<td>那个是数字的 0 ，删除光标所在处，到该列的最前面一个字符</td>
</tr>
<tr>
<td><font color="#c00000">yy</font></td>
<td>复制光标所在的那一列(常用)</td>
</tr>
<tr>
<td><font color="#c00000">nyy</font></td>
<td>n 为数字。复制光标所在的向下 n 列，例如 20yy 则是复制 20 列(常用)</td>
</tr>
<tr>
<td>y1G</td>
<td>复制光标所在列到第一列的所有数据</td>
</tr>
<tr>
<td>yG</td>
<td>复制光标所在列到最后一列的所有数据</td>
</tr>
<tr>
<td>y0</td>
<td>复制光标所在的那个字符到该列行首的所有数据</td>
</tr>
<tr>
<td>y$</td>
<td>复制光标所在的那个字符到该列行尾的所有数据</td>
</tr>
<tr>
<td><font color="#c00000">p, P</font></td>
<td>p 为将已复制的数据在光标下一列粘贴，P 则为贴在光标上一列！ 举例来说，我目前光标在第 20 列，且已经复制了 10 列数据。则按下 p 后， 那 10 列数据会贴在原本的 20 列之后，亦即由 21 列开始贴。但如果是按下 P 呢？ 那么原本的第 20 列会被推到变成 30 列。 (常用)</td>
</tr>
<tr>
<td>J</td>
<td>将光标所在列与下一列的数据结合成同一列</td>
</tr>
<tr>
<td>c</td>
<td>重复删除多个数据，例如向下删除 10 列，[ 10cj ]</td>
</tr>
<tr>
<td>u</td>
<td>复原前一个动作。(常用)</td>
</tr>
<tr>
<td><font color="#c00000">[Ctrl]+r</font></td>
<td>重做上一个动作。(常用)</td>
</tr>
<tr>
<td>这个 u 与 [Ctrl]+r 是很常用的指令！一个是复原，另一个则是重做一次～ 利用这两个功能按键，你的编辑，嘿嘿！很快乐的啦！</td>
<td></td>
</tr>
<tr>
<td><font color="#c00000">.</font></td>
<td>不要怀疑！这就是小数点！意思是重复前一个动作的意思。 如果你想要重复删除、重复粘贴等等动作，按下小数点『.』就好了！ (常用)</td>
</tr>
</tbody>
</table>
<ul>
<li>第二部份：一般指令模式切换到编辑模式的可用的按钮说明</li>
</ul>
<table>
<thead>
<tr>
<th></th>
<th></th>
</tr>
</thead>
<tbody>
<tr>
<td>进入插入或取代的编辑模式</td>
<td></td>
</tr>
<tr>
<td>i, I</td>
<td>进入插入模式(Insert mode)：  <br>i 为『从目前光标所在处插入』， I 为『在目前所在列的第一个非空白字符处开始插入』。 (常用)</td>
</tr>
<tr>
<td>a, A</td>
<td>进入插入模式(Insert mode)：  <br>a 为『从目前光标所在的下一个字符处开始插入』， A 为『从光标所在列的最后一个字符处开始插入』。(常用)</td>
</tr>
<tr>
<td>o, O</td>
<td>进入插入模式(Insert mode)：  <br>这是英文本母 o 的大小写。o 为『在目前光标所在的下一列处插入新的一列』； O 为在目前光标所在处的上一列插入新的一列！(常用)</td>
</tr>
<tr>
<td>r, R</td>
<td>进入取代模式(Replace mode)：  <br>r 只会取代光标所在的那一个字符一次；R会一直取代光标所在的文本，直到按下 ESC 为止；(常用)</td>
</tr>
<tr>
<td>上面这些按键中，在 vi 画面的左下角处会出现『–INSERT–』或『–REPLACE–』的字样。 由名称就知道该动作了吧！！特别注意的是，我们上面也提过了，你想要在文件里面输入字符时， 一定要在左下角处看到 INSERT 或 REPLACE 才能输入喔！</td>
<td></td>
</tr>
<tr>
<td>[Esc]</td>
<td>退出编辑模式，回到一般指令模式中(常用)</td>
</tr>
</tbody>
</table>
<ul>
<li>第三部份：一般指令模式切换到指令列模式的可用按钮说明</li>
</ul>
<table>
<thead>
<tr>
<th></th>
<th></th>
</tr>
</thead>
<tbody>
<tr>
<td>指令列模式的保存、离开等指令</td>
<td></td>
</tr>
<tr>
<td>:w</td>
<td>将编辑的数据写入硬盘文件中(常用)</td>
</tr>
<tr>
<td>:w!</td>
<td>若文件属性为『唯读』时，强制写入该文件。不过，到底能不能写入， 还是跟你对该文件的文件权限有关啊！</td>
</tr>
<tr>
<td>:q</td>
<td>离开 vi (常用)</td>
</tr>
<tr>
<td>:q!</td>
<td>若曾修改过文件，又不想保存，使用 ! 为强制离开不保存文件。</td>
</tr>
<tr>
<td>注意一下啊，那个惊叹号 (!) 在 vi 当中，常常具有『强制』的意思～</td>
<td></td>
</tr>
<tr>
<td>:wq</td>
<td>保存后离开，若为 :wq! 则为强制保存后离开 (常用)</td>
</tr>
<tr>
<td>ZZ</td>
<td>这是大写的 Z 喔！若文件没有更动，则不保存离开，若文件已经被更动过，则保存后离开！</td>
</tr>
<tr>
<td>:w [filename]</td>
<td>将编辑的数据保存成另一个文件（类似另存新档）</td>
</tr>
<tr>
<td>:r [filename]</td>
<td>在编辑的数据中，读入另一个文件的数据。亦即将 『filename』 这个文件内容加到光标所在列后面</td>
</tr>
<tr>
<td>:n1,n2 w [filename]</td>
<td>将 n1 到 n2 的内容保存成 filename 这个文件。</td>
</tr>
<tr>
<td>:! command</td>
<td>暂时离开 vi 到指令列模式下运行 command 的显示结果！例如  <br>『:! ls /home』即可在 vi 当中察看 /home 底下以 ls 输出的文件信息！</td>
</tr>
<tr>
<td>vim 环境的变更</td>
<td></td>
</tr>
<tr>
<td>:set nu</td>
<td>显示行号，设置之后，会在每一列的前缀显示该列的行号</td>
</tr>
<tr>
<td>:set nonu</td>
<td>与 set nu 相反，为取消行号！</td>
</tr>
</tbody>
</table>
]]></content>
      <categories>
        <category>Linux操作系统</category>
      </categories>
      <tags>
        <tag>Shell</tag>
        <tag>Linux</tag>
        <tag>vim</tag>
      </tags>
  </entry>
  <entry>
    <title>EEG数据初探</title>
    <url>/2025/03/09/2025-3-9-eeg-data/</url>
    <content><![CDATA[<p>最近在学习EEG数据的处理，发现网上的有关EEG数据的讲解较少，尤其是使用<code>mne-python</code>库来对EEG数据进行处理，现在来对EEG数据和该库的整体进行大致的窥探。</p>
<span id="more"></span>
<h2 id="eeg数据介绍"><a class="markdownIt-Anchor" href="#eeg数据介绍"></a> EEG数据介绍</h2>
<p>从我们数据处理人员的角度来说，尤其是进行深度学习的数据处理。我们要尤其注意数据的维度属性，从这个角度来看，一个简单的EEG数据会有两个维度属性：空间（通道）维度和时间维度：空间维度从物理层面来看就是我们脑袋上所接入的电极数量；而时间维度就是电极以固定频率所采集的离散点。有时候为了固定的任务要求会让受试者参与一定的事件，事件触发信号通常由<strong>刺激通道</strong>（<code>stim channel</code>）来进行记录，刺激通道中标记的特定事件的时间点和类别被称为<code>trigger</code>。</p>
<h2 id="mne-python库"><a class="markdownIt-Anchor" href="#mne-python库"></a> <code>mne-python</code>库</h2>
<p>有了上面的对EEG数据简要介绍，接下来我们就可以对<code>mne-python</code>中的一些功能函数做出更加详细的介绍。</p>
<h3 id="读取eeg数据"><a class="markdownIt-Anchor" href="#读取eeg数据"></a> 读取EEG数据</h3>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># stim_channel就是我们上面所说的时间通道，在自定义的数据集中我们将该通道命名为&#x27;Status&#x27;</span></span><br><span class="line"><span class="comment"># preload=True会让数据会被直接加载到内存中，这样后续操作（如滤波、提取特征等）可以直接使用内存中的数据，速度更快。</span></span><br><span class="line">raw = mne.io.read_raw_edf(<span class="string">&#x27;data.edf&#x27;</span>, stim_channel=<span class="string">&quot;Status&quot;</span>, preload=<span class="literal">True</span>)</span><br></pre></td></tr></table></figure>
<p>输出为：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">Extracting EDF parameters from data.edf...</span><br><span class="line">EDF file detected</span><br><span class="line">Setting channel info structure...</span><br><span class="line">Creating raw.info structure...</span><br><span class="line">Reading 0 ... 786431  =      0.000 ...   767.999 secs...</span><br></pre></td></tr></table></figure>
<p>EEG数据的格式不止有.edf格式，其他常见的还有.bdf格式、.fif格式等等，在<code>mne-python</code>库中都有相应的读取实现（<code>mne.io.read_raw_bdf</code>,<code>mne.io.read_raw_fif</code>）等，看情况选取。</p>
<h3 id="了解raw对象的信息"><a class="markdownIt-Anchor" href="#了解raw对象的信息"></a> 了解<code>Raw</code>对象的信息</h3>
<p>一些比较重要的EEG数据的属性信息我们都可以使用<code>raw.info</code>来进行索引，比如时间频率信息和空间通道信息。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">raw.info[<span class="string">&#x27;sfreq&#x27;</span>] <span class="comment"># 时间频率</span></span><br></pre></td></tr></table></figure>
<p>输出为：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">1024.0</span><br></pre></td></tr></table></figure>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">raw.info[<span class="string">&#x27;ch_names&#x27;</span>] <span class="comment"># 空间通道</span></span><br></pre></td></tr></table></figure>
<p>输出为：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">[&#x27;Fp1&#x27;,</span><br><span class="line"> &#x27;AF7&#x27;,</span><br><span class="line"> &#x27;AF3&#x27;,</span><br><span class="line"> &#x27;F1&#x27;,</span><br><span class="line"> &#x27;F3&#x27;,</span><br><span class="line"> &#x27;F5&#x27;,</span><br><span class="line"> &#x27;F7&#x27;,</span><br><span class="line"> &#x27;FT7&#x27;,</span><br><span class="line"> &#x27;FC5&#x27;,</span><br><span class="line"> &#x27;FC3&#x27;,</span><br><span class="line"> &#x27;FC1&#x27;,</span><br><span class="line"> &#x27;C1&#x27;,</span><br><span class="line"> &#x27;C3&#x27;,</span><br><span class="line"> &#x27;C5&#x27;,</span><br><span class="line"> &#x27;T7&#x27;,</span><br><span class="line"> &#x27;TP7&#x27;,</span><br><span class="line"> &#x27;CP5&#x27;,</span><br><span class="line"> &#x27;CP3&#x27;,</span><br><span class="line"> &#x27;CP1&#x27;,</span><br><span class="line"> &#x27;P1&#x27;,</span><br><span class="line"> &#x27;P3&#x27;,</span><br><span class="line"> &#x27;P5&#x27;,</span><br><span class="line"> &#x27;P7&#x27;,</span><br><span class="line"> &#x27;P9&#x27;,</span><br><span class="line"> &#x27;PO7&#x27;,</span><br><span class="line"> &#x27;PO3&#x27;,</span><br><span class="line"> &#x27;O1&#x27;,</span><br><span class="line"> &#x27;Iz&#x27;,</span><br><span class="line"> &#x27;Oz&#x27;,</span><br><span class="line"> &#x27;POz&#x27;,</span><br><span class="line"> &#x27;Pz&#x27;,</span><br><span class="line"> &#x27;CPz&#x27;,</span><br><span class="line"> &#x27;Fpz&#x27;,</span><br><span class="line"> &#x27;Fp2&#x27;,</span><br><span class="line"> &#x27;AF8&#x27;,</span><br><span class="line"> &#x27;AF4&#x27;,</span><br><span class="line"> &#x27;AFz&#x27;,</span><br><span class="line"> &#x27;Fz&#x27;,</span><br><span class="line"> &#x27;F2&#x27;,</span><br><span class="line"> &#x27;F4&#x27;,</span><br><span class="line"> &#x27;F6&#x27;,</span><br><span class="line"> &#x27;F8&#x27;,</span><br><span class="line"> &#x27;FT8&#x27;,</span><br><span class="line"> &#x27;FC6&#x27;,</span><br><span class="line"> &#x27;FC4&#x27;,</span><br><span class="line"> &#x27;FC2&#x27;,</span><br><span class="line"> &#x27;FCz&#x27;,</span><br><span class="line"> &#x27;Cz&#x27;,</span><br><span class="line"> &#x27;C2&#x27;,</span><br><span class="line"> &#x27;C4&#x27;,</span><br><span class="line"> &#x27;C6&#x27;,</span><br><span class="line"> &#x27;T8&#x27;,</span><br><span class="line"> &#x27;TP8&#x27;,</span><br><span class="line"> &#x27;CP6&#x27;,</span><br><span class="line"> &#x27;CP4&#x27;,</span><br><span class="line"> &#x27;CP2&#x27;,</span><br><span class="line"> &#x27;P2&#x27;,</span><br><span class="line"> &#x27;P4&#x27;,</span><br><span class="line"> &#x27;P6&#x27;,</span><br><span class="line"> &#x27;P8&#x27;,</span><br><span class="line"> &#x27;P10&#x27;,</span><br><span class="line"> &#x27;PO8&#x27;,</span><br><span class="line"> &#x27;PO4&#x27;,</span><br><span class="line"> &#x27;O2&#x27;,</span><br><span class="line"> &#x27;EXG1&#x27;,</span><br><span class="line"> &#x27;EXG2&#x27;,</span><br><span class="line"> &#x27;EXG3&#x27;,</span><br><span class="line"> &#x27;EXG4&#x27;,</span><br><span class="line"> &#x27;EXG5&#x27;,</span><br><span class="line"> &#x27;EXG6&#x27;,</span><br><span class="line"> &#x27;EXG7&#x27;,</span><br><span class="line"> &#x27;EXG8&#x27;,</span><br><span class="line"> &#x27;GSR1&#x27;,</span><br><span class="line"> &#x27;GSR2&#x27;,</span><br><span class="line"> &#x27;Erg1&#x27;,</span><br><span class="line"> &#x27;Erg2&#x27;,</span><br><span class="line"> &#x27;Resp&#x27;,</span><br><span class="line"> &#x27;Plet&#x27;,</span><br><span class="line"> &#x27;Temp&#x27;,</span><br><span class="line"> &#x27;Status&#x27;]</span><br></pre></td></tr></table></figure>
<p>当然，<code>raw.info</code>身为类似字典的类型还有许多其他的属性，我们可以通过以下代码查看：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">raw.info.keys()</span><br></pre></td></tr></table></figure>
<p>输出为：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">dict_keys([&#x27;acq_pars&#x27;, &#x27;acq_stim&#x27;, &#x27;ctf_head_t&#x27;, &#x27;description&#x27;, &#x27;dev_ctf_t&#x27;, &#x27;dig&#x27;, &#x27;experimenter&#x27;, &#x27;utc_offset&#x27;, &#x27;device_info&#x27;, &#x27;file_id&#x27;, &#x27;highpass&#x27;, &#x27;hpi_subsystem&#x27;, &#x27;kit_system_id&#x27;, &#x27;helium_info&#x27;, &#x27;line_freq&#x27;, &#x27;lowpass&#x27;, &#x27;meas_date&#x27;, &#x27;meas_id&#x27;, &#x27;proj_id&#x27;, &#x27;proj_name&#x27;, &#x27;subject_info&#x27;, &#x27;xplotter_layout&#x27;, &#x27;gantry_angle&#x27;, &#x27;bads&#x27;, &#x27;chs&#x27;, &#x27;comps&#x27;, &#x27;events&#x27;, &#x27;hpi_meas&#x27;, &#x27;hpi_results&#x27;, &#x27;projs&#x27;, &#x27;proc_history&#x27;, &#x27;custom_ref_applied&#x27;, &#x27;sfreq&#x27;, &#x27;dev_head_t&#x27;, &#x27;ch_names&#x27;, &#x27;nchan&#x27;])</span><br></pre></td></tr></table></figure>
<p>其中比较重要的有以下属性：</p>
<ul>
<li><strong>highpass</strong>: 高通滤波器的截止频率，表示信号处理时应用的高通滤波参数。</li>
<li><strong>lowpass</strong>: 低通滤波器的截止频率，用于信号处理时的低通滤波。</li>
<li><strong>bads</strong>: 有问题或坏掉的通道列表，这些通道的数据可能被排除在分析之外。</li>
<li><strong>chs</strong>: 通道信息，包含每个通道的类型、位置和其他详细描述。</li>
<li><strong>events</strong>: 事件信息，记录实验过程中发生的关键事件或时间点。</li>
<li><strong>sfreq</strong>: 采样频率，表示每秒钟采集多少次数据点。</li>
<li><strong>ch_names</strong>: 通道名称列表，表示每个通道的标识符（如 EEG 或 MEG 通道的名字）。</li>
<li><strong>nchan</strong>: 通道数量，表示测量中使用的总通道数。</li>
</ul>
<p>详细的情况可以自己运行<code>raw.info['key']</code>进行查看。</p>
<h3 id="eeg原始数据epoch化"><a class="markdownIt-Anchor" href="#eeg原始数据epoch化"></a> EEG原始数据Epoch化</h3>
<h4 id="eeg原始数据可视化"><a class="markdownIt-Anchor" href="#eeg原始数据可视化"></a> EEG原始数据可视化</h4>
<p>EEG原始数据一般来说是包含多个事件的，可视化详情如图1所示：</p>
<p><img data-src="../../codes/eeg-data/raw.png" alt="我的图片" /></p>
<center>
<strong>图1：EEG原始数据附带事件标记截取部分通道和部分时间可视化</strong>
</center>
<p>可以看到，我们截取的这30s的片段就发生了8次事件，该EEG原始数据采集了768s，可见这其中包含了远远不止8次事件这么简单。</p>
<h4 id="eeg原始数据事件"><a class="markdownIt-Anchor" href="#eeg原始数据事件"></a> EEG原始数据事件</h4>
<p>下一步我们要做的事就是定位事件发生的时刻以及事件类型，还记得我们在刚开始的时候所说的：</p>
<blockquote>
<p>事件触发信号通常由<strong>刺激通道</strong>（<code>stim channel</code>）来进行记录。</p>
</blockquote>
<p>因此要定位事件发生的位置及类型，我们不免要和<code>stim channel</code>打交道，我们现将该通道的电位随时间的变化可视化如图2所示。</p>
<p><img data-src="../../codes/eeg-data/stim.png" alt="我的图片" /></p>
<center>
<strong>图2：EEG原始数据附带事件标记截取刺激通道和部分时间可视化</strong>
</center>
<p>显而易见，刺激通道的电位幅度可以查找事件类型，电位幅度产生的时刻就是事件发生的时刻！在<code>mne-python</code>中根据刺激通道查找事件的时刻和类型已经封装好在<code>mne.find_events</code>中，具体代码如下所示：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># initial_event设置为 False，不会插入额外的初始事件，函数只返回真正检测到的事件。</span></span><br><span class="line">events = mne.find_events(raw=raw, stim_channel=<span class="string">&quot;Status&quot;</span>, initial_event=<span class="literal">False</span>)</span><br></pre></td></tr></table></figure>
<p>输出为:</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">161 events found on stim channel Status</span><br><span class="line">Event IDs: [  1   2   3   4   5   6   7   8 111]</span><br></pre></td></tr></table></figure>
<p>其中事件 ID 111表示事件开始。<code>events</code>是一个<span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><msub><mi>n</mi><mrow><mi>e</mi><mi>v</mi><mi>e</mi><mi>n</mi><mi>t</mi><mi>s</mi></mrow></msub><mo>×</mo><mn>3</mn></mrow><annotation encoding="application/x-tex">n_{events}\times 3</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.73333em;vertical-align:-0.15em;"></span><span class="mord"><span class="mord mathnormal">n</span><span class="msupsub"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.2805559999999999em;"><span style="top:-2.5500000000000003em;margin-left:0em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mathnormal mtight">e</span><span class="mord mathnormal mtight" style="margin-right:0.03588em;">v</span><span class="mord mathnormal mtight">e</span><span class="mord mathnormal mtight">n</span><span class="mord mathnormal mtight">t</span><span class="mord mathnormal mtight">s</span></span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.15em;"><span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2222222222222222em;"></span><span class="mbin">×</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span></span><span class="base"><span class="strut" style="height:0.64444em;vertical-align:0em;"></span><span class="mord">3</span></span></span></span>的数组类型，纵向第一列表示事件发生的时刻，第三列表示事件发生的类型，第二列为全零：表示表示事件通道在该事件发生前的值。</p>
<p><code>event[:9]</code>的示例。</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">array([[ 2596,     0,   111],</span><br><span class="line">       [15698,     0,     8],</span><br><span class="line">       [19117,     0,     2],</span><br><span class="line">       [22758,     0,     7],</span><br><span class="line">       [26381,     0,     4],</span><br><span class="line">       [29834,     0,     6],</span><br><span class="line">       [33664,     0,     3],</span><br><span class="line">       [37378,     0,     1],</span><br><span class="line">       [40793,     0,     5]])</span><br></pre></td></tr></table></figure>
<p>EEG原始数据Epoch化的前戏已经准备好了，接下来我们要根据<code>events</code>和<code>raw</code>来对原始EEG数据进行Epoch化。Epoch化简单来说就是对原始的EEG数据在事件发生附近按照固定的时间范围进行截取，由于我们的EEG原始数据往往会有很多事件，所以Epoch化得到的结果可以说是批量化的数据，这里的批次就是EEG数据中的有效事件数量（深度学习模型狂喜😁）。</p>
<p>具体的实现过程也被封装在了<code>mne.Epochs</code>中：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># tmin和tmax表示以事件为基准然后以（tmin s, tmax s）为时间范围截取</span></span><br><span class="line"><span class="comment"># baseline用于进行基线校正。基线校正是指在Epoch化之前，减去一个时间段的平均值以消除噪声</span></span><br><span class="line"><span class="comment"># proj控制是否应用投影运算符（比如通道信号的空间滤波）。</span></span><br><span class="line">epochs = mne.Epochs(raw, events=events, event_id=event_id, tmin=<span class="number">0</span>, tmax=<span class="number">1</span>, baseline=<span class="literal">None</span>, proj=<span class="literal">False</span>, picks=<span class="string">&#x27;eeg&#x27;</span>, preload=<span class="literal">True</span>)</span><br></pre></td></tr></table></figure>
<p>然后我们可以根据<code>epochs</code>得到批量化的数据，使用如下方法：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">batched_data = epochs.get_data()[:, :, :-<span class="number">1</span>]</span><br></pre></td></tr></table></figure>
<p>批量化数据的形状为<span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>b</mi><mi>s</mi><mo>×</mo><msub><mi>n</mi><mrow><mi>c</mi><mi>h</mi><mi>a</mi><mi>n</mi><mi>n</mi><mi>e</mi><mi>l</mi><mi>s</mi></mrow></msub><mo>×</mo><msub><mi>n</mi><mrow><mi>t</mi><mi>i</mi><mi>m</mi><mi>e</mi><mi>s</mi></mrow></msub></mrow><annotation encoding="application/x-tex">bs\times n_{channels} \times n_{times}</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.77777em;vertical-align:-0.08333em;"></span><span class="mord mathnormal">b</span><span class="mord mathnormal">s</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span><span class="mbin">×</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span></span><span class="base"><span class="strut" style="height:0.73333em;vertical-align:-0.15em;"></span><span class="mord"><span class="mord mathnormal">n</span><span class="msupsub"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.33610799999999996em;"><span style="top:-2.5500000000000003em;margin-left:0em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mathnormal mtight">c</span><span class="mord mathnormal mtight">h</span><span class="mord mathnormal mtight">a</span><span class="mord mathnormal mtight">n</span><span class="mord mathnormal mtight">n</span><span class="mord mathnormal mtight">e</span><span class="mord mathnormal mtight" style="margin-right:0.01968em;">l</span><span class="mord mathnormal mtight">s</span></span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.15em;"><span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2222222222222222em;"></span><span class="mbin">×</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span></span><span class="base"><span class="strut" style="height:0.58056em;vertical-align:-0.15em;"></span><span class="mord"><span class="mord mathnormal">n</span><span class="msupsub"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.31166399999999994em;"><span style="top:-2.5500000000000003em;margin-left:0em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mathnormal mtight">t</span><span class="mord mathnormal mtight">i</span><span class="mord mathnormal mtight">m</span><span class="mord mathnormal mtight">e</span><span class="mord mathnormal mtight">s</span></span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.15em;"><span></span></span></span></span></span></span></span></span></span>，Epoch化后的EEG原始数据截取部分通道和部分事件后的可视化如图3所示。</p>
<p><img data-src="../../codes/eeg-data/epoch.png" alt="我的图片" /></p>
<center>
<strong>图3：Epoch化后EEG数据截取部分事件和部分通道后的可视化</strong>
</center>
]]></content>
      <categories>
        <category>Data Analysis</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>EEG</tag>
      </tags>
  </entry>
</search>
