<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Git详解</title>
    <url>/2025/03/07/2025-3-7-git/</url>
    <content><![CDATA[<p>这篇博客主要自底向上地介绍Git命令行，有时甚至会使用一些<code>python</code>代码来对一些Git的功能进行更加详细的分析。我一直认为基础一旦打好，那么一些更高级的用法也可以循序渐进地了解，而且是更加透彻的了解。不是像博主一开始那样以为只要死记硬背<code>git add</code>，<code>git commit</code>,<code>git pull</code>，<code>git clone</code>等常见命令的一些用法就可以了😭（实际上网上大多数教程就是这样教的…），在这上面走了不少弯路，所以从头开始打地基写作此文留以警示⚠️。</p>
<span id="more"></span>

<h2 id="Git仓库（Git-Repository）"><a href="#Git仓库（Git-Repository）" class="headerlink" title="Git仓库（Git Repository）"></a>Git仓库（Git Repository）</h2><p>要学习Git，首先我们要知道我们运行Git命令是对什么对象进行操作的，在Git的相关术语中，我们把这个对象叫做Git仓库（Git Repository），Git仓库实际上就是一个文件夹，在这个文件夹里我们通过.git文件夹（隐藏文件夹）对Git仓库中的文件（非隐藏文件夹）内容的改变进行记录，当然并不是所有非隐藏文件夹中文件的改变都会被记录，被.gitingore文件所标识的文件被改变后.git文件夹不会对其进行记录。根据我上面所说，<strong>Git操作最隐晦的地方就在于对于.git文件夹中内容的修改来保证对于Git仓库非隐藏文件夹中文件内容修改的记录</strong>。</p>
<p>根据上面的描述，我们可以得到一下简略的公式：<br>$$<br>Git\ Repository &#x3D; .git文件夹 + 非隐藏文件夹\tag{1}<br>$$<br>更加详尽的来说以<code>python</code>来构建Git仓库类：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">GitRepository</span> (<span class="title class_ inherited__">object</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;A git repository&quot;&quot;&quot;</span></span><br><span class="line"></span><br><span class="line">    worktree = <span class="literal">None</span></span><br><span class="line">    gitdir = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, path</span>):</span><br><span class="line">        <span class="variable language_">self</span>.worktree = path</span><br><span class="line">        <span class="variable language_">self</span>.gitdir = os.path.join(path, <span class="string">&quot;.git&quot;</span>)</span><br></pre></td></tr></table></figure>

<h2 id="git文件夹内部结构探究"><a href="#git文件夹内部结构探究" class="headerlink" title=".git文件夹内部结构探究"></a>.git文件夹内部结构探究</h2><p>前面我们已经知道了<strong>Git操作最隐晦的地方就在于对于.git文件夹中内容的修改来保证对于Git仓库非隐藏文件夹中文件内容修改的记录</strong>，那接下来我们就要对此进行脱贫攻坚了。由于.git是隐藏文件夹，所以我们要先对.git文件夹中的文件分布有一定的了解，<code>git init</code>初始化后的.git文件夹中的文件分布如图1所示，此时Git仓库内除了.git文件夹之外一片荒芜。</p>
<p><img data-src="/../../codes/git/gitinit.png" alt="我的图片"></p>
<center>
<strong>图1：运行git init之后.git文件夹的文件分布结构</strong>
</center>

<p>然后我们再本地Git仓库中添加可见文件，添加后的结果如图2所示。</p>
<p><img data-src="/../../codes/git/add.png" alt="image"></p>
<center>
<strong>图2：Git仓库添加文件后的文件分布</strong>
</center>

<p>将上述所有文件运行<code>git add</code>之后在进行<code>git commit</code>观察.git文件夹中的文件分布，如图3所示。可以看到相较于最初的.git文件，我们在<code>git add</code>和<code>git commit</code>之后文件夹中多出了<code>index</code>文件，<code>log</code>文件夹，<code>objects</code>文件夹中的8个文件以及<code>refs/heads</code>下的<code>main</code>文件，其他的一些变化我们不做考虑。<strong>有果导因我们可以知道.git文件夹内部的这些变化中有对于Git仓库非隐藏文件夹文件修改的记录。</strong></p>
<p><img data-src="/../../codes/git/gitaddcommit.png" alt="image"></p>
<center>
<strong>图3：运行git add和git commit之后.git文件夹的文件分布结构</strong>
</center>

<h2 id="Git对象（Git-Object）"><a href="#Git对象（Git-Object）" class="headerlink" title="Git对象（Git Object）"></a>Git对象（Git Object）</h2><p>上面的变化究竟是如何实现的呢？要知道Git中的几乎一切都被存储为Git对象！也就是说我们可以通过Git对象进行操作来完成上面的变化。说了这么多，让我们来为Git对象下一个比较官方的定义：<strong>Git对象就是在 Git 仓库中的文件，它们的路径由它们的内容决定。</strong> </p>
<p>Git对象范性可以借由<code>python</code>来实现：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">GitObject</span> (<span class="title class_ inherited__">object</span>):</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, data=<span class="literal">None</span></span>):</span><br><span class="line">        <span class="keyword">if</span> data != <span class="literal">None</span>:</span><br><span class="line">            <span class="variable language_">self</span>.deserialize(data)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="variable language_">self</span>.init()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">serialize</span>(<span class="params">self</span>):</span><br><span class="line">	    <span class="string">&quot;&quot;&quot;将对象转变为zip文件解压缩后的byte格式&quot;&quot;&quot;</span></span><br><span class="line">        <span class="keyword">raise</span> Exception(<span class="string">&quot;Unimplemented!&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">deserialize</span>(<span class="params">self, data</span>):</span><br><span class="line">	    <span class="string">&quot;&quot;&quot;data通常为zip文件解压缩后的byte格式，根据类型解码&quot;&quot;&quot;</span></span><br><span class="line">        <span class="keyword">raise</span> Exception(<span class="string">&quot;Unimplemented!&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">init</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="keyword">pass</span> <span class="comment"># Just do nothing. This is a reasonable default!</span></span><br></pre></td></tr></table></figure>

<p>Git对象又细分为以下四种类型：GitBlob（Binary Large Object）对象， GitCommit对象， GitTree对象， GitTag对象，以下分别进行介绍。</p>
<h3 id="GitBlob对象"><a href="#GitBlob对象" class="headerlink" title="GitBlob对象"></a>GitBlob对象</h3><p>我们先最直观的看看Git中对于Blob内容的展示，如图4所示：</p>
<p><img data-src="/../../codes/git/blob.png" alt="image"></p>
<center>
<strong>图4：运行git cat-file之后对a.txt所对应的类型和类型内容的展示</strong>
</center>

<p>Blobs 是用户数据：你放入 Git 中的每个文件的内容（如a.txt、b.txt、src&#x2F;c.txt、src&#x2F;d.txt中的内容）的字节形式都被存储为一个 blob。这样做使它们很容易操作，因为 blob 没有特定的语法或约束，除了基本的对象存储机制之外，它们只是未指定的数据。</p>
<p>GitBlob对象的<code>python</code>实现如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">GitBlob</span>(<span class="title class_ inherited__">GitObject</span>):</span><br><span class="line">    fmt=<span class="string">b&#x27;blob&#x27;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">serialize</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>.blobdata</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">deserialize</span>(<span class="params">self, data</span>):</span><br><span class="line">        <span class="variable language_">self</span>.blobdata = data</span><br></pre></td></tr></table></figure>

<h3 id="GitTree对象"><a href="#GitTree对象" class="headerlink" title="GitTree对象"></a>GitTree对象</h3><p>同上，我们先最直观的看看Git中对于树内容的展示，如图5所示：</p>
<p><img data-src="/../../codes/git/tree.png" alt="image"></p>
<center>
<strong>图5：运行git cat-file之后对最顶层的树所对应的类型和类型内容的展示</strong>
</center>

<p>非正式地说，树（tree）描述了Git仓库非隐藏文件夹中的内容，它将 blobs（文件对象）与路径关联起来。树是一个由三个元素组成的元组数组，这些元素包括文件模式、相对于工作树的路径，以及一个 SHA-1 值。一个典型的树的内容如下所示：</p>
<table>
<thead>
<tr>
<th>Mode</th>
<th>SHA-1</th>
<th>Path</th>
</tr>
</thead>
<tbody><tr>
<td>100644</td>
<td>06f844865cfc4b68116d2cbf00833b294aae63ec</td>
<td>.DS_Store</td>
</tr>
<tr>
<td>100644</td>
<td>e12523960bf9941182c801077be75dce699ff37c</td>
<td>a.txt</td>
</tr>
<tr>
<td>100644</td>
<td>753e270e92068316ec7fa2b6a40e780a4e1d14d7</td>
<td>b.txt</td>
</tr>
<tr>
<td>040000</td>
<td>30d66379dddee7068ab4fd3a17cc1ef9555278ac</td>
<td>src</td>
</tr>
</tbody></table>
<p>模式（Mode）就是文件的模式，路径（Path）是文件的位置。SHA-1 是指向一个 blob 或另一个树对象。如果是 blob，路径表示的是一个文件；如果是树对象，路径表示的是一个目录。</p>
<p>常见的文件模式有以下几种：</p>
<ul>
<li><strong>100644</strong>: 普通文件，具有标准的读写权限（普通的文本文件或代码文件）。</li>
<li><strong>100755</strong>: 可执行文件，具有执行权限（如脚本或可执行程序）。</li>
<li><strong>040000</strong>: 目录（directory）。</li>
<li><strong>120000</strong>: 符号链接（symbolic link）。</li>
<li><strong>160000</strong>: Git 子模块（submodule）。</li>
</ul>
<p>而实际上当我们解析树的时候，也就是对<code>.git/objects</code>中树的SHA-1路径所代表的文件进行解压缩后提纯到的树对象内容的字节编码，它们的格式通常如下：</p>
<blockquote>
<p>[mode] space [path] 0x00 [sha-1] （byte格式）</p>
</blockquote>
<p>由于树对应的文件夹中的文件不止一个，有时甚至会有在文件夹中进行文件夹嵌套。因此我们要设计出叶子对应单个文件，树对应由不同文件所组成的文件夹。</p>
<p>我们可以将叶子对象和树对象按以下<code>python</code>代码来表示：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">GitTreeLeaf</span> (<span class="title class_ inherited__">object</span>):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, mode, path, sha</span>):</span><br><span class="line">        <span class="variable language_">self</span>.mode = mode <span class="comment"># 模式</span></span><br><span class="line">        <span class="variable language_">self</span>.path = path <span class="comment"># 文件路径</span></span><br><span class="line">        <span class="variable language_">self</span>.sha = sha <span class="comment"># 文件路径索引到的文件内容标准化后所对应的sha-1值</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">GitTree</span>(<span class="title class_ inherited__">GitObject</span>):</span><br><span class="line">    fmt=<span class="string">b&#x27;tree&#x27;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">deserialize</span>(<span class="params">self, data</span>):</span><br><span class="line">	    <span class="comment"># tree_parse是解析所得到的树对象的字节编码数据</span></span><br><span class="line">        <span class="variable language_">self</span>.items = tree_parse(data)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">serialize</span>(<span class="params">self</span>):</span><br><span class="line">	    <span class="comment"># tree_serialize将解析后的结果求逆得到树对象的字节编码数据</span></span><br><span class="line">        <span class="keyword">return</span> tree_serialize(<span class="variable language_">self</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">init</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="variable language_">self</span>.items = <span class="built_in">list</span>()</span><br></pre></td></tr></table></figure>

<p>由于一个树中有时不单单包含叶子更有可能包含子树，具体情形见上面一棵树的典型情况，<code>tree_parse</code>和<code>tree_serialize</code>函数如下所示：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">tree_parse</span>(<span class="params">raw</span>):</span><br><span class="line">    pos = <span class="number">0</span></span><br><span class="line">    <span class="built_in">max</span> = <span class="built_in">len</span>(raw)</span><br><span class="line">    ret = <span class="built_in">list</span>()</span><br><span class="line">    <span class="keyword">while</span> pos &lt; <span class="built_in">max</span>:</span><br><span class="line">        pos, data = tree_parse_one(raw, pos)</span><br><span class="line">        ret.append(data)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> ret</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">tree_parse_one</span>(<span class="params">raw, start=<span class="number">0</span></span>):</span><br><span class="line">	<span class="comment"># [mode] space [path] 0x00 [sha-1] （byte格式）</span></span><br><span class="line">    <span class="comment"># 查找字节形式空格以确定mode</span></span><br><span class="line">    x = raw.find(<span class="string">b&#x27; &#x27;</span>, start)</span><br><span class="line">    <span class="keyword">assert</span> x-start == <span class="number">5</span> <span class="keyword">or</span> x-start==<span class="number">6</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 读取mode</span></span><br><span class="line">    mode = raw[start:x]</span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">len</span>(mode) == <span class="number">5</span>:</span><br><span class="line">        <span class="comment"># 标准到6字节</span></span><br><span class="line">        mode = <span class="string">b&quot;0&quot;</span> + mode</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 查找字节形式空终止符以确定path</span></span><br><span class="line">    y = raw.find(<span class="string">b&#x27;\x00&#x27;</span>, x)</span><br><span class="line">    <span class="comment"># 读取path</span></span><br><span class="line">    path = raw[x+<span class="number">1</span>:y]</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 读取sha…</span></span><br><span class="line">    <span class="comment"># raw[y+1:y+21] 提取的是 20 字节的二进制数据（SHA-1 哈希值）。</span></span><br><span class="line">    raw_sha = <span class="built_in">int</span>.from_bytes(raw[y+<span class="number">1</span>:y+<span class="number">21</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">    <span class="comment"># int.from_bytes(raw[y+1:y+21], &quot;big&quot;)将提取的 20 字节二进制数据转</span></span><br><span class="line">    <span class="comment"># 换为一个整数，然后使用 format(raw_sha, &quot;040x&quot;) 将这个整数转换为 40 </span></span><br><span class="line">    <span class="comment"># 位的十六进制字符串 。Git中通常使用的sha值时这种形式。比如.DS_Store对应</span></span><br><span class="line">    <span class="comment"># 的06f844865cfc4b68116d2cbf00833b294aae63ec</span></span><br><span class="line">    sha = <span class="built_in">format</span>(raw_sha, <span class="string">&quot;040x&quot;</span>)</span><br><span class="line">    <span class="keyword">return</span> y+<span class="number">21</span>, GitTreeLeaf(mode, path.decode(<span class="string">&quot;utf8&quot;</span>), sha)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">tree_serialize</span>(<span class="params">obj</span>):</span><br><span class="line">    obj.items.sort(key=tree_leaf_sort_key)</span><br><span class="line">    ret = <span class="string">b&#x27;&#x27;</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> obj.items:</span><br><span class="line">        ret += i.mode</span><br><span class="line">        ret += <span class="string">b&#x27; &#x27;</span></span><br><span class="line">        ret += i.path.encode(<span class="string">&quot;utf8&quot;</span>)</span><br><span class="line">        ret += <span class="string">b&#x27;\x00&#x27;</span></span><br><span class="line">        sha = <span class="built_in">int</span>(i.sha, <span class="number">16</span>)</span><br><span class="line">        ret += sha.to_bytes(<span class="number">20</span>, byteorder=<span class="string">&quot;big&quot;</span>)</span><br><span class="line">    <span class="keyword">return</span> ret</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">tree_leaf_sort_key</span>(<span class="params">leaf</span>):</span><br><span class="line">    <span class="keyword">if</span> leaf.mode.startswith(<span class="string">b&quot;10&quot;</span>):</span><br><span class="line">        <span class="keyword">return</span> leaf.path <span class="comment"># 文件</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">return</span> leaf.path + <span class="string">&quot;/&quot;</span> <span class="comment"># 文件夹</span></span><br></pre></td></tr></table></figure>

<h3 id="GitCommit对象"><a href="#GitCommit对象" class="headerlink" title="GitCommit对象"></a>GitCommit对象</h3><p>同上，我们先最直观的看看Git中对于树内容的展示，如图6所示：</p>
<p><img data-src="/../../codes/git/commit.png" alt="image"></p>
<center>
<strong>图6：运行git cat-file之后对最顶层的提交所对应的类型和类型内容的展示</strong>
</center>

<p>一个提交（Commit）的典型如下所示：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">tree 50306289de5ae719af85eaad3430d785517a11e7</span><br><span class="line">parent 735a65cef921f8e30ae55fdf19b695eb15ea8d45</span><br><span class="line">author fordelkon &lt;27xxx88816@qq.com&gt; 1741406298 +0800</span><br><span class="line">committer fordelkon &lt;27xxx88816@qq.com&gt; 1741406298 +0800</span><br><span class="line"></span><br><span class="line">life is short, I use Python</span><br></pre></td></tr></table></figure>

<p>从上面的典型来看，一个提交由以下几部分组成：</p>
<ul>
<li>树对象，也就是工作树的内容、文件和目录；</li>
<li>零个、一个或多个父提交；</li>
<li>一个作者身份（姓名和邮箱）以及一个时间戳；</li>
<li>一个提交者身份（姓名和邮箱）以及一个时间戳；</li>
<li>一个提交信息。<br>从提交的组成来看，我们可以明白已经创建好的提交是不可变的。如果我们更改了作者、父提交或任何一个文件，那么我们实际上最终会创建一个新的、不同的提交对象与改变后的结果绑定。所以每一个提交都与它在整个仓库中的位置及其与最初提交的关系紧密相连。换句话说，一个给定的提交 ID （sha-1）不仅标识了某些文件的内容，还将提交与它的整个历史及整个仓库绑定在一起。也就是说，有了提交，我们的Git的历史记录功能就有了保证！</li>
</ul>
<p>一个提交对象的<code>python</code>代码如下所示：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">GitCommit</span>(<span class="title class_ inherited__">GitObject</span>):</span><br><span class="line">    fmt=<span class="string">b&#x27;commit&#x27;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">deserialize</span>(<span class="params">self, data</span>):</span><br><span class="line">	    <span class="comment"># kvlm_parse是解析所得到的提交对象的字节编码数据</span></span><br><span class="line">        <span class="variable language_">self</span>.kvlm = kvlm_parse(data)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">serialize</span>(<span class="params">self</span>):</span><br><span class="line">	    <span class="comment"># kvlm_serialize将解析后的结果求逆得到提交对象的字节编码数据</span></span><br><span class="line">        <span class="keyword">return</span> kvlm_serialize(<span class="variable language_">self</span>.kvlm)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">init</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="variable language_">self</span>.kvlm = <span class="built_in">dict</span>()</span><br></pre></td></tr></table></figure>

<p>由提交的典型来看，我们可以将提交对象的字节编码数据转换成键值对的字典格式，由于有空格和回车。更形象的表示如下:</p>
<blockquote>
<p>[key1] space [value1] \n … [keyn] space [valuen] \n [commit]</p>
</blockquote>
<p>因此，<code>kvlm_parse</code>和<code>kvlm_serialize</code>的python代码如下所示</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">kvlm_parse</span>(<span class="params">raw, start=<span class="number">0</span>, dct=<span class="literal">None</span></span>):</span><br><span class="line">    <span class="comment"># 如果 dct 为空，初始化一个空字典</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> dct:</span><br><span class="line">        dct = <span class="built_in">dict</span>()</span><br><span class="line">        <span class="comment"># 不能在函数参数中直接声明 dct=dict()，否则所有函数调用会共用同一个字典，</span></span><br><span class="line">        <span class="comment"># 导致每次调用时字典不断增长。</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 这个函数是递归的：它读取一个键/值对，然后调用自身来处理下一个位置。</span></span><br><span class="line">    <span class="comment"># 因此我们首先需要知道当前的位置：是在一个关键字，还是已经到了消息部分。</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 查找下一个空格和换行符的位置</span></span><br><span class="line">    spc = raw.find(<span class="string">b&#x27; &#x27;</span>, start)</span><br><span class="line">    nl = raw.find(<span class="string">b&#x27;\n&#x27;</span>, start)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 如果空格出现在换行符之前，说明我们有一个关键字。否则，它是最终的消息部分，</span></span><br><span class="line">    <span class="comment"># 我们会读取直到文件的末尾。</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 基本情况</span></span><br><span class="line">    <span class="comment"># =========</span></span><br><span class="line">    <span class="comment"># 如果换行符先出现（或者根本没有空格，这种情况下 find 返回 -1），</span></span><br><span class="line">    <span class="comment"># 我们假设遇到了一个空行。空行意味着剩余的数据是消息部分。</span></span><br><span class="line">    <span class="comment"># 我们将它存储在字典中，键为 None，然后返回。</span></span><br><span class="line">    <span class="keyword">if</span> (spc &lt; <span class="number">0</span>) <span class="keyword">or</span> (nl &lt; spc):</span><br><span class="line">        <span class="keyword">assert</span> nl == start</span><br><span class="line">        dct[<span class="literal">None</span>] = raw[start+<span class="number">1</span>:]  <span class="comment"># 存储消息部分</span></span><br><span class="line">        <span class="keyword">return</span> dct</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 递归情况</span></span><br><span class="line">    <span class="comment"># ==============</span></span><br><span class="line">    <span class="comment"># 读取一个键值对并递归处理下一个。</span></span><br><span class="line">    key = raw[start:spc]</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 查找值的结尾。以空格开头的行表示值的延续行，</span></span><br><span class="line">    <span class="comment"># 因此我们需要循环，直到找到一个不是以空格开头的换行符。</span></span><br><span class="line">    end = start</span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        end = raw.find(<span class="string">b&#x27;\n&#x27;</span>, end+<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">if</span> raw[end+<span class="number">1</span>] != <span class="built_in">ord</span>(<span class="string">&#x27; &#x27;</span>): <span class="keyword">break</span>  <span class="comment"># 找到一个不以空格开头的换行符</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 获取值，同时删除延续行的前导空格</span></span><br><span class="line">    value = raw[spc+<span class="number">1</span>:end].replace(<span class="string">b&#x27;\n &#x27;</span>, <span class="string">b&#x27;\n&#x27;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 不要覆盖现有的数据内容</span></span><br><span class="line">    <span class="keyword">if</span> key <span class="keyword">in</span> dct:</span><br><span class="line">        <span class="comment"># 如果字典中已有该键，且值为列表，则直接添加新值</span></span><br><span class="line">        <span class="keyword">if</span> <span class="built_in">type</span>(dct[key]) == <span class="built_in">list</span>:</span><br><span class="line">            dct[key].append(value)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="comment"># 如果已有的值不是列表，创建一个列表来存储多个值</span></span><br><span class="line">            dct[key] = [ dct[key], value ]</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="comment"># 如果字典中没有该键，则直接存储键值对</span></span><br><span class="line">        dct[key]=value</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 递归处理剩余的部分</span></span><br><span class="line">    <span class="keyword">return</span> kvlm_parse(raw, start=end+<span class="number">1</span>, dct=dct)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">kvlm_serialize</span>(<span class="params">kvlm</span>):</span><br><span class="line">    ret = <span class="string">b&#x27;&#x27;</span>  <span class="comment"># 初始化一个空的字节串用于存储序列化结果</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 输出字段</span></span><br><span class="line">    <span class="keyword">for</span> k <span class="keyword">in</span> kvlm.keys():</span><br><span class="line">        <span class="comment"># 跳过消息体本身（消息存储在字典的 None 键中）</span></span><br><span class="line">        <span class="keyword">if</span> k == <span class="literal">None</span>: <span class="keyword">continue</span></span><br><span class="line">        val = kvlm[k]</span><br><span class="line">        <span class="comment"># 将单个值规范化为列表，以便统一处理</span></span><br><span class="line">        <span class="keyword">if</span> <span class="built_in">type</span>(val) != <span class="built_in">list</span>:</span><br><span class="line">            val = [ val ]</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 遍历值列表，处理每个值</span></span><br><span class="line">        <span class="keyword">for</span> v <span class="keyword">in</span> val:</span><br><span class="line">            <span class="comment"># 拼接键和值，并确保换行符后面有一个空格（用于表示值的延续行）</span></span><br><span class="line">            ret += k + <span class="string">b&#x27; &#x27;</span> + (v.replace(<span class="string">b&#x27;\n&#x27;</span>, <span class="string">b&#x27;\n &#x27;</span>)) + <span class="string">b&#x27;\n&#x27;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 最后拼接消息体，消息体之前需要一个空行分隔</span></span><br><span class="line">    ret += <span class="string">b&#x27;\n&#x27;</span> + kvlm[<span class="literal">None</span>]</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> ret  <span class="comment"># 返回序列化后的字节串</span></span><br></pre></td></tr></table></figure>
<h3 id="GitTag对象"><a href="#GitTag对象" class="headerlink" title="GitTag对象"></a>GitTag对象</h3><p>标签实际上就是引用（refs）。它们位于 .git&#x2F;refs&#x2F;tags&#x2F; 目录下。值得注意的是，标签有两种类型：轻量标签和标签对象。</p>
<h4 id="轻量标签（“Lightweight”-tags）"><a href="#轻量标签（“Lightweight”-tags）" class="headerlink" title="轻量标签（“Lightweight” tags）"></a>轻量标签（“Lightweight” tags）</h4><p>仅仅是指向提交、树对象或 blob 对象的普通引用。</p>
<h4 id="标签对象（Tag-objects）"><a href="#标签对象（Tag-objects）" class="headerlink" title="标签对象（Tag objects）"></a>标签对象（Tag objects）</h4><p>是指向标签类型对象的普通引用。与轻量标签不同，标签对象包含作者、日期、可选的 PGP 签名以及可选的注释。它们的格式与提交对象相同。</p>
<p>因此，GitTag的<code>python</code>代码如下所示：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">GitTag</span>(<span class="title class_ inherited__">GitCommit</span>):</span><br><span class="line">    fmt = <span class="string">b&#x27;tag&#x27;</span></span><br></pre></td></tr></table></figure>

<p>我们已经了解了上面四种Git对象，那么也就知道了Git对象的内容决定路径的性质可以保证对于非隐藏文件夹中文件内容修改的记录。也就是说，Git仓库非隐藏文件夹中的文件一旦被修改，那么相应的Git对象的路径也会发生改变，将修改后的不同的Git对象的文件标准化后压缩按照改变的路径存储在<code>.git/objects</code>文件夹下，这样可以保证对该改变的记录。那么文件的内容和文件的路径有着明确的映射关系吗？Git给出的答案是对标准化后的文件内容进行 <a href="https://en.wikipedia.org/wiki/SHA-1">SHA-1</a><a href="https://en.wikipedia.org/wiki/Cryptographic_hash_function">哈希</a>后得到文件的路径。如何进行标准化也是我们值得注意的，Git中实现的方案是<strong>字节形式类型头+字节形式空格+字节形式大小+字节形式空分离符+对象内容的字节编码。</strong> 具体方案如下所示：</p>
<blockquote>
<p>[fmt] space [size] 0x00 [data] (byte格式)</p>
</blockquote>
<p>根据这一标准化的形式我们可以对对象进行读和写操作，所有对象的核心载体是对象内容的字节编码。具体<code>python</code>代码如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">object_read</span>(<span class="params">repo, sha</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;Read object sha from Git repository repo.  Return a</span></span><br><span class="line"><span class="string">    GitObject whose exact type depends on the object.&quot;&quot;&quot;</span></span><br><span class="line">	<span class="comment"># 由sha1值得到对应路径</span></span><br><span class="line">    path = repo_file(repo, <span class="string">&quot;objects&quot;</span>, sha[<span class="number">0</span>:<span class="number">2</span>], sha[<span class="number">2</span>:])</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> os.path.isfile(path):</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">with</span> <span class="built_in">open</span> (path, <span class="string">&quot;rb&quot;</span>) <span class="keyword">as</span> f:</span><br><span class="line">        raw = zlib.decompress(f.read())</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 读取对象类型</span></span><br><span class="line">        x = raw.find(<span class="string">b&#x27; &#x27;</span>) <span class="comment"># 0x20 字节形式空格</span></span><br><span class="line">        fmt = raw[<span class="number">0</span>:x] <span class="comment"># 字节形式类型头</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 读取有效的对象大小</span></span><br><span class="line">        y = raw.find(<span class="string">b&#x27;\x00&#x27;</span>, x) <span class="comment"># 0x00 字节形式空分离符</span></span><br><span class="line">        size = <span class="built_in">int</span>(raw[x:y].decode(<span class="string">&quot;ascii&quot;</span>))</span><br><span class="line">        <span class="keyword">if</span> size != <span class="built_in">len</span>(raw)-y-<span class="number">1</span>:</span><br><span class="line">            <span class="keyword">raise</span> Exception(<span class="string">f&quot;Malformed object <span class="subst">&#123;sha&#125;</span>: bad length&quot;</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 根据对象类型选择生成器</span></span><br><span class="line">        <span class="keyword">match</span> fmt:</span><br><span class="line">            <span class="keyword">case</span> <span class="string">b&#x27;commit&#x27;</span> : c=GitCommit</span><br><span class="line">            <span class="keyword">case</span> <span class="string">b&#x27;tree&#x27;</span>   : c=GitTree</span><br><span class="line">            <span class="keyword">case</span> <span class="string">b&#x27;tag&#x27;</span>    : c=GitTag</span><br><span class="line">            <span class="keyword">case</span> <span class="string">b&#x27;blob&#x27;</span>   : c=GitBlob</span><br><span class="line">            <span class="keyword">case</span> _:</span><br><span class="line">                <span class="keyword">raise</span> Exception(<span class="string">f&quot;Unknown type <span class="subst">&#123;fmt.decode(<span class="string">&quot;ascii&quot;</span>)&#125;</span> for object <span class="subst">&#123;sha&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 调用生成器返回对象</span></span><br><span class="line">        <span class="keyword">return</span> c(raw[y+<span class="number">1</span>:]) <span class="comment"># raw[y+1:]对象内容的字节编码</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">object_write</span>(<span class="params">obj, repo=<span class="literal">None</span></span>):</span><br><span class="line">    <span class="comment"># 对象内容的字节编码</span></span><br><span class="line">    data = obj.serialize()</span><br><span class="line">    <span class="comment"># 添加一些前缀成为标准格式</span></span><br><span class="line">    result = obj.fmt + <span class="string">b&#x27; &#x27;</span> + <span class="built_in">str</span>(<span class="built_in">len</span>(data)).encode() + <span class="string">b&#x27;\x00&#x27;</span> + data</span><br><span class="line">    <span class="comment"># 计算sha1</span></span><br><span class="line">    sha = hashlib.sha1(result).hexdigest()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> repo:</span><br><span class="line">        <span class="comment"># 由sha1值得到对应路径</span></span><br><span class="line">        path=repo_file(repo, <span class="string">&quot;objects&quot;</span>, sha[<span class="number">0</span>:<span class="number">2</span>], sha[<span class="number">2</span>:], mkdir=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> os.path.exists(path):</span><br><span class="line">            <span class="keyword">with</span> <span class="built_in">open</span>(path, <span class="string">&#x27;wb&#x27;</span>) <span class="keyword">as</span> f:</span><br><span class="line">                <span class="comment"># Compress and write</span></span><br><span class="line">                f.write(zlib.compress(result))</span><br><span class="line">    <span class="keyword">return</span> sha</span><br></pre></td></tr></table></figure>

<p>好了，前戏已经准备了这么多，我们可以凭借Git仓库和Git对象来实现最最最重要的Git两连击吗（<code>git add</code>和<code>git commit</code>）？答案当然是……否定的。是的，我们已经拥有了这么多的对象，也有了读取和写入对象的功能，但是一旦我们改变文件（不妨就改变a.txt文件的内容），由前面我们说的主要的三种对象来看，GitBlob元数据会改变，随之而来的GitTree元数据会改变，进而GitCommit元数据也会改变。这个过程不是一挥而就的，所以Git种才会分为<code>git add</code>和<code>git commit</code>两步走。在<code>git add</code>的过程中我们将修改后的文件属性变化提交到暂存区（Staging area），也就是我们图3中的<code>.git/index</code>文件中。然后使用<code>git commit</code>将暂存区中的所有记录的文件统合生成新的GitTree，进而生成新的GitCommit。很明显，我们还需要一个表示暂存区的类型。</p>
<h2 id="暂存区（Staging-area）"><a href="#暂存区（Staging-area）" class="headerlink" title="暂存区（Staging area）"></a>暂存区（Staging area）</h2><p>暂存区在.git文件夹中的展现形式就是<code>.git/index</code>文件，在一次提交之后，<code>.git/index</code>文件可以看作是该提交的一种副本：它保存了与对应的树相同的路径&#x2F;Blob（对象）的关联关系。但它还保存了Git仓库中关于文件的额外信息，比如它们的创建&#x2F;修改时间，因此 <code>git status</code> 通常不需要实际比较文件：它只需要检查文件的修改时间是否与索引文件中存储的时间相同，只有当它们不同时，才会执行实际的比较。类比树对象的表示，<code>.git/index</code>文件中包含许多元素，所以我们的第一个任务是对单个元素进行表示，之后是对整体<code>.git/index</code>文件进行表示。</p>
<p>单个元素和整体<code>.git/index</code>文件的<code>python</code>表示如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">GitIndexEntry</span> (<span class="title class_ inherited__">object</span>):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, ctime=<span class="literal">None</span>, mtime=<span class="literal">None</span>, dev=<span class="literal">None</span>, ino=<span class="literal">None</span>,</span></span><br><span class="line"><span class="params">                 mode_type=<span class="literal">None</span>, mode_perms=<span class="literal">None</span>, uid=<span class="literal">None</span>, gid=<span class="literal">None</span>,</span></span><br><span class="line"><span class="params">                 fsize=<span class="literal">None</span>, sha=<span class="literal">None</span>, flag_assume_valid=<span class="literal">None</span>,</span></span><br><span class="line"><span class="params">                 flag_stage=<span class="literal">None</span>, name=<span class="literal">None</span></span>):</span><br><span class="line">        <span class="comment"># 文件元数据最后更改的时间。格式是一个二元组</span></span><br><span class="line">        <span class="comment"># (以秒为单位的时间戳, 纳秒)</span></span><br><span class="line">        <span class="variable language_">self</span>.ctime = ctime</span><br><span class="line">        <span class="comment"># 文件数据最后更改的时间。格式是一个二元组</span></span><br><span class="line">        <span class="comment"># (以秒为单位的时间戳, 纳秒)</span></span><br><span class="line">        <span class="variable language_">self</span>.mtime = mtime</span><br><span class="line">        <span class="comment"># 包含该文件的设备ID</span></span><br><span class="line">        <span class="variable language_">self</span>.dev = dev</span><br><span class="line">        <span class="comment"># 文件的inode编号</span></span><br><span class="line">        <span class="variable language_">self</span>.ino = ino</span><br><span class="line">        <span class="comment"># 对象类型，可以是 b1000 (普通文件), b1010 (符号链接),</span></span><br><span class="line">        <span class="comment"># b1110 (gitlink)</span></span><br><span class="line">        <span class="variable language_">self</span>.mode_type = mode_type</span><br><span class="line">        <span class="comment"># 对象的权限，表示为一个整数</span></span><br><span class="line">        <span class="variable language_">self</span>.mode_perms = mode_perms</span><br><span class="line">        <span class="comment"># 文件所有者的用户ID</span></span><br><span class="line">        <span class="variable language_">self</span>.uid = uid</span><br><span class="line">        <span class="comment"># 文件所有者的组ID</span></span><br><span class="line">        <span class="variable language_">self</span>.gid = gid</span><br><span class="line">        <span class="comment"># 对象的大小，以字节为单位</span></span><br><span class="line">        <span class="variable language_">self</span>.fsize = fsize</span><br><span class="line">        <span class="comment"># 对象的SHA哈希值</span></span><br><span class="line">        <span class="variable language_">self</span>.sha = sha</span><br><span class="line">        <span class="comment"># 标志：假设该对象有效</span></span><br><span class="line">        <span class="variable language_">self</span>.flag_assume_valid = flag_assume_valid</span><br><span class="line">        <span class="comment"># 文件的阶段标志</span></span><br><span class="line">        <span class="variable language_">self</span>.flag_stage = flag_stage</span><br><span class="line">        <span class="comment"># 对象的名称（包括完整路径）</span></span><br><span class="line">        <span class="variable language_">self</span>.name = name</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">GitIndex</span> (<span class="title class_ inherited__">object</span>):</span><br><span class="line">    version = <span class="literal">None</span></span><br><span class="line">    entries = []</span><br><span class="line">    <span class="comment"># ext = None</span></span><br><span class="line">    <span class="comment"># sha = None</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, version=<span class="number">2</span>, entries=<span class="literal">None</span></span>):</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> entries:</span><br><span class="line">            entries = <span class="built_in">list</span>()</span><br><span class="line"></span><br><span class="line">        <span class="variable language_">self</span>.version = version</span><br><span class="line">        <span class="variable language_">self</span>.entries = entries</span><br></pre></td></tr></table></figure>

<p><code>.git/index</code>文件是一个二进制文件，可能是出于性能考虑。尽管如此，它的格式相对简单。文件开始部分是一个头部信息，包含了 DIRC 魔法字节、版本号以及该索引文件中的条目总数。更形象的格式如下：</p>
<blockquote>
<p>[DIRC]  [version]  [count]  [entries]</p>
</blockquote>
<p>所以我们可以定义<code>index_read</code>和<code>index_write</code>函数如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">index_read</span>(<span class="params">repo</span>):</span><br><span class="line">    <span class="comment"># 获取仓库索引文件的路径</span></span><br><span class="line">    index_file = repo_file(repo, <span class="string">&quot;index&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 如果索引文件不存在（例如新仓库），则返回一个空的 GitIndex 实例</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> os.path.exists(index_file):</span><br><span class="line">        <span class="keyword">return</span> GitIndex()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 打开索引文件，以二进制方式读取</span></span><br><span class="line">    <span class="keyword">with</span> <span class="built_in">open</span>(index_file, <span class="string">&#x27;rb&#x27;</span>) <span class="keyword">as</span> f:</span><br><span class="line">        raw = f.read()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 索引文件的头部包含 12 个字节</span></span><br><span class="line">    header = raw[:<span class="number">12</span>]</span><br><span class="line">    signature = header[:<span class="number">4</span>]</span><br><span class="line">    <span class="comment"># 检查索引文件的魔法字节，必须为 &quot;DIRC&quot;，代表 &quot;DirCache&quot;</span></span><br><span class="line">    <span class="keyword">assert</span> signature == <span class="string">b&quot;DIRC&quot;</span></span><br><span class="line">    <span class="comment"># 从头部解析版本号（4-8字节），此处只支持版本 2</span></span><br><span class="line">    version = <span class="built_in">int</span>.from_bytes(header[<span class="number">4</span>:<span class="number">8</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">    <span class="keyword">assert</span> version == <span class="number">2</span>, <span class="string">&quot;wyag 只支持索引文件版本 2&quot;</span></span><br><span class="line">    <span class="comment"># 从头部解析索引文件中的条目数量（8-12字节）</span></span><br><span class="line">    count = <span class="built_in">int</span>.from_bytes(header[<span class="number">8</span>:<span class="number">12</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 用于存储读取到的索引条目</span></span><br><span class="line">    entries = <span class="built_in">list</span>()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 索引文件内容部分从第 12 个字节开始</span></span><br><span class="line">    content = raw[<span class="number">12</span>:]</span><br><span class="line">    idx = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 循环解析每个条目，根据文件头部给出的条目数进行循环</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, count):</span><br><span class="line">        <span class="comment"># 解析创建时间（秒数和纳秒数）</span></span><br><span class="line">        ctime_s =  <span class="built_in">int</span>.from_bytes(content[idx: idx+<span class="number">4</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        ctime_ns = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">4</span>: idx+<span class="number">8</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        <span class="comment"># 解析修改时间（秒数和纳秒数）</span></span><br><span class="line">        mtime_s = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">8</span>: idx+<span class="number">12</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        mtime_ns = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">12</span>: idx+<span class="number">16</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        <span class="comment"># 解析设备 ID</span></span><br><span class="line">        dev = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">16</span>: idx+<span class="number">20</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        <span class="comment"># 解析 inode 编号</span></span><br><span class="line">        ino = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">20</span>: idx+<span class="number">24</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        <span class="comment"># 忽略的字段</span></span><br><span class="line">        unused = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">24</span>: idx+<span class="number">26</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        <span class="keyword">assert</span> <span class="number">0</span> == unused</span><br><span class="line">        <span class="comment"># 解析文件模式（包括类型和权限）</span></span><br><span class="line">        mode = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">26</span>: idx+<span class="number">28</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        mode_type = mode &gt;&gt; <span class="number">12</span></span><br><span class="line">        <span class="keyword">assert</span> mode_type <span class="keyword">in</span> [<span class="number">0b1000</span>, <span class="number">0b1010</span>, <span class="number">0b1110</span>]  <span class="comment"># 文件类型必须是常规文件、符号链接或 gitlink</span></span><br><span class="line">        mode_perms = mode &amp; <span class="number">0b0000000111111111</span></span><br><span class="line">        <span class="comment"># 解析用户 ID 和组 ID</span></span><br><span class="line">        uid = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">28</span>: idx+<span class="number">32</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        gid = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">32</span>: idx+<span class="number">36</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        <span class="comment"># 解析文件大小</span></span><br><span class="line">        fsize = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">36</span>: idx+<span class="number">40</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        <span class="comment"># 解析 SHA（对象 ID），并格式化为 40 字符的小写十六进制字符串</span></span><br><span class="line">        sha = <span class="built_in">format</span>(<span class="built_in">int</span>.from_bytes(content[idx+<span class="number">40</span>: idx+<span class="number">60</span>], <span class="string">&quot;big&quot;</span>), <span class="string">&quot;040x&quot;</span>)</span><br><span class="line">        <span class="comment"># 解析标志位</span></span><br><span class="line">        flags = <span class="built_in">int</span>.from_bytes(content[idx+<span class="number">60</span>: idx+<span class="number">62</span>], <span class="string">&quot;big&quot;</span>)</span><br><span class="line">        <span class="comment"># 解析假定有效标志位</span></span><br><span class="line">        flag_assume_valid = (flags &amp; <span class="number">0b1000000000000000</span>) != <span class="number">0</span></span><br><span class="line">        <span class="comment"># 解析扩展标志位（被忽略）</span></span><br><span class="line">        flag_extended = (flags &amp; <span class="number">0b0100000000000000</span>) != <span class="number">0</span></span><br><span class="line">        <span class="keyword">assert</span> <span class="keyword">not</span> flag_extended</span><br><span class="line">        <span class="comment"># 解析阶段标志位</span></span><br><span class="line">        flag_stage =  flags &amp; <span class="number">0b0011000000000000</span></span><br><span class="line">        <span class="comment"># 解析文件名长度（12 位）</span></span><br><span class="line">        name_length = flags &amp; <span class="number">0b0000111111111111</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 读取了 62 个字节，继续处理文件名</span></span><br><span class="line">        idx += <span class="number">62</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 如果文件名长度小于 0xFFF，则文件名之后有一个空字节终止符</span></span><br><span class="line">        <span class="keyword">if</span> name_length &lt; <span class="number">0xFFF</span>:</span><br><span class="line">            <span class="keyword">assert</span> content[idx + name_length] == <span class="number">0x00</span></span><br><span class="line">            raw_name = content[idx:idx+name_length]</span><br><span class="line">            idx += name_length + <span class="number">1</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="comment"># 处理文件名长度大于等于 0xFFF 的情况，需要查找空字节终止符</span></span><br><span class="line">            <span class="built_in">print</span>(<span class="string">f&quot;注意: 文件名长度为 0x<span class="subst">&#123;name_length:X&#125;</span> 字节长。&quot;</span>)</span><br><span class="line">            null_idx = content.find(<span class="string">b&#x27;\x00&#x27;</span>, idx + <span class="number">0xFFF</span>)</span><br><span class="line">            raw_name = content[idx: null_idx]</span><br><span class="line">            idx = null_idx + <span class="number">1</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 将文件名解析为 UTF-8 字符串</span></span><br><span class="line">        name = raw_name.decode(<span class="string">&quot;utf8&quot;</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 索引文件中的数据以 8 字节对齐，因此需要根据指针对齐调整 idx</span></span><br><span class="line">        idx = <span class="number">8</span> * ceil(idx / <span class="number">8</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 将解析出的索引条目添加到列表中</span></span><br><span class="line">        entries.append(GitIndexEntry(ctime=(ctime_s, ctime_ns),</span><br><span class="line">                                     mtime=(mtime_s,  mtime_ns),</span><br><span class="line">                                     dev=dev,</span><br><span class="line">                                     ino=ino,</span><br><span class="line">                                     mode_type=mode_type,</span><br><span class="line">                                     mode_perms=mode_perms,</span><br><span class="line">                                     uid=uid,</span><br><span class="line">                                     gid=gid,</span><br><span class="line">                                     fsize=fsize,</span><br><span class="line">                                     sha=sha,</span><br><span class="line">                                flag_assume_valid=flag_assume_valid,</span><br><span class="line">                                     flag_stage=flag_stage,</span><br><span class="line">                                     name=name))</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 返回包含所有索引条目的 GitIndex 实例</span></span><br><span class="line">    <span class="keyword">return</span> GitIndex(version=version, entries=entries)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">index_write</span>(<span class="params">repo, index</span>):</span><br><span class="line">    <span class="keyword">with</span> <span class="built_in">open</span>(repo_file(repo, <span class="string">&quot;index&quot;</span>), <span class="string">&quot;wb&quot;</span>) <span class="keyword">as</span> f:</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 写入头部信息</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 写入魔术字节 &quot;DIRC&quot; 以标识该文件为 Git 索引文件。</span></span><br><span class="line">        f.write(<span class="string">b&quot;DIRC&quot;</span>)</span><br><span class="line">        <span class="comment"># 写入版本号（固定为 2）。</span></span><br><span class="line">        f.write(index.version.to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line">        <span class="comment"># 写入索引条目数量。</span></span><br><span class="line">        f.write(<span class="built_in">len</span>(index.entries).to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 写入条目数据</span></span><br><span class="line"></span><br><span class="line">        idx = <span class="number">0</span></span><br><span class="line">        <span class="keyword">for</span> e <span class="keyword">in</span> index.entries:</span><br><span class="line">            <span class="comment"># 写入创建时间（秒）和创建时间的纳秒部分。</span></span><br><span class="line">            f.write(e.ctime[<span class="number">0</span>].to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line">            f.write(e.ctime[<span class="number">1</span>].to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line">            <span class="comment"># 写入修改时间（秒）和修改时间的纳秒部分。</span></span><br><span class="line">            f.write(e.mtime[<span class="number">0</span>].to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line">            f.write(e.mtime[<span class="number">1</span>].to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line">            <span class="comment"># 写入设备号和 inode 号。</span></span><br><span class="line">            f.write(e.dev.to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line">            f.write(e.ino.to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line"></span><br><span class="line">            <span class="comment"># 写入文件权限模式（类型和权限合并）。</span></span><br><span class="line">            mode = (e.mode_type &lt;&lt; <span class="number">12</span>) | e.mode_perms</span><br><span class="line">            f.write(mode.to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line"></span><br><span class="line">            <span class="comment"># 写入用户 ID 和组 ID。</span></span><br><span class="line">            f.write(e.uid.to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line">            f.write(e.gid.to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line"></span><br><span class="line">            <span class="comment"># 写入文件大小。</span></span><br><span class="line">            f.write(e.fsize.to_bytes(<span class="number">4</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line">            <span class="comment"># 写入 SHA-1 对象 ID。</span></span><br><span class="line">            f.write(<span class="built_in">int</span>(e.sha, <span class="number">16</span>).to_bytes(<span class="number">20</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line"></span><br><span class="line">            <span class="comment"># 处理标志位 `flag_assume_valid`。</span></span><br><span class="line">            flag_assume_valid = <span class="number">0x1</span> &lt;&lt; <span class="number">15</span> <span class="keyword">if</span> e.flag_assume_valid <span class="keyword">else</span> <span class="number">0</span></span><br><span class="line"></span><br><span class="line">            <span class="comment"># 将文件名编码为 UTF-8，并计算长度。</span></span><br><span class="line">            name_bytes = e.name.encode(<span class="string">&quot;utf8&quot;</span>)</span><br><span class="line">            bytes_len = <span class="built_in">len</span>(name_bytes)</span><br><span class="line">            <span class="keyword">if</span> bytes_len &gt;= <span class="number">0xFFF</span>:</span><br><span class="line">                name_length = <span class="number">0xFFF</span>  <span class="comment"># 长度过长时，标记为 0xFFF。</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                name_length = bytes_len</span><br><span class="line"></span><br><span class="line">            <span class="comment"># 将 `flag_assume_valid`、`flag_stage` 和名称长度合并到 2 个字节中。</span></span><br><span class="line">            f.write((flag_assume_valid | e.flag_stage | name_length).to_bytes(<span class="number">2</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line"></span><br><span class="line">            <span class="comment"># 写入文件名，并附加一个 0x00 作为结尾。</span></span><br><span class="line">            f.write(name_bytes)</span><br><span class="line">            f.write((<span class="number">0</span>).to_bytes(<span class="number">1</span>, <span class="string">&quot;big&quot;</span>))</span><br><span class="line"></span><br><span class="line">            <span class="comment"># 计算当前条目的字节数。</span></span><br><span class="line">            idx += <span class="number">62</span> + <span class="built_in">len</span>(name_bytes) + <span class="number">1</span></span><br><span class="line"></span><br><span class="line">            <span class="comment"># 若条目不是 8 字节对齐，则进行填充。</span></span><br><span class="line">            <span class="keyword">if</span> idx % <span class="number">8</span> != <span class="number">0</span>:</span><br><span class="line">                pad = <span class="number">8</span> - (idx % <span class="number">8</span>)</span><br><span class="line">                f.write((<span class="number">0</span>).to_bytes(pad, <span class="string">&quot;big&quot;</span>))</span><br><span class="line">                idx += pad</span><br></pre></td></tr></table></figure>

<p>按照我们前面所说的，<code>git add</code>会根据所修改的文件路径对<code>.git/index</code>文件中的内容进行修改。具体实现方式是从<code>.git/index</code>文件中移除指定路径的条目（不物理删除文件），然后对每个路径，读取文件内容，计算文件的哈希值并获取文件的元数据信息，然后将文件的相关信息添加到<code>.git/index</code>文件中，包括 ctime、mtime、文件权限等，最后将新的索引条目写回索引文件。具体的<code>add</code>函数实现如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">add</span>(<span class="params">repo, paths, delete=<span class="literal">True</span>, skip_missing=<span class="literal">False</span></span>):</span><br><span class="line">    <span class="comment"># 首先调用 rm 函数从索引中移除这些路径的条目，避免重复（不物理删除文件）。</span></span><br><span class="line">    rm(repo, paths, delete=<span class="literal">False</span>, skip_missing=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 获取工作树的根目录路径（末尾加上路径分隔符）</span></span><br><span class="line">    worktree = repo.worktree + os.sep</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 将路径转换为 (绝对路径，相对路径) 的对，并确保它们在工作树内且为文件</span></span><br><span class="line">    clean_paths = <span class="built_in">set</span>()</span><br><span class="line">    <span class="keyword">for</span> path <span class="keyword">in</span> paths:</span><br><span class="line">        abspath = os.path.abspath(path)  <span class="comment"># 获取绝对路径</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> (abspath.startswith(worktree) <span class="keyword">and</span> os.path.isfile(abspath)):  <span class="comment"># 确保路径在工作树内且为文件</span></span><br><span class="line">            <span class="keyword">raise</span> Exception(<span class="string">f&quot;Not a file, or outside the worktree: <span class="subst">&#123;paths&#125;</span>&quot;</span>)  <span class="comment"># 如果路径无效或不在工作树内，抛出异常</span></span><br><span class="line">        relpath = os.path.relpath(abspath, repo.worktree)  <span class="comment"># 获取相对路径</span></span><br><span class="line">        clean_paths.add((abspath, relpath))  <span class="comment"># 添加到 clean_paths 集合中</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 查找并读取索引。由于 `rm` 已经修改了索引，这里再次读取。</span></span><br><span class="line">    <span class="comment"># @FIXME：可以通过传递命令来移动索引，而不是反复读写索引。</span></span><br><span class="line">    index = index_read(repo)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 遍历每个路径对，读取文件，生成哈希并更新索引条目</span></span><br><span class="line">    <span class="keyword">for</span> (abspath, relpath) <span class="keyword">in</span> clean_paths:</span><br><span class="line">        <span class="keyword">with</span> <span class="built_in">open</span>(abspath, <span class="string">&quot;rb&quot;</span>) <span class="keyword">as</span> fd:</span><br><span class="line">            sha = object_hash(fd, <span class="string">b&quot;blob&quot;</span>, repo)  <span class="comment"># 计算文件的 SHA 哈希值</span></span><br><span class="line"></span><br><span class="line">            stat = os.stat(abspath)  <span class="comment"># 获取文件状态</span></span><br><span class="line"></span><br><span class="line">            <span class="comment"># 获取文件的创建时间和修改时间（秒和纳秒）</span></span><br><span class="line">            ctime_s = <span class="built_in">int</span>(stat.st_ctime)</span><br><span class="line">            ctime_ns = stat.st_ctime_ns % <span class="number">10</span>**<span class="number">9</span></span><br><span class="line">            mtime_s = <span class="built_in">int</span>(stat.st_mtime)</span><br><span class="line">            mtime_ns = stat.st_mtime_ns % <span class="number">10</span>**<span class="number">9</span></span><br><span class="line"></span><br><span class="line">            <span class="comment"># 创建 Git 索引条目对象</span></span><br><span class="line">            entry = GitIndexEntry(ctime=(ctime_s, ctime_ns), mtime=(mtime_s, mtime_ns), dev=stat.st_dev, ino=stat.st_ino,</span><br><span class="line">                                  mode_type=<span class="number">0b1000</span>, mode_perms=<span class="number">0o644</span>, uid=stat.st_uid, gid=stat.st_gid,</span><br><span class="line">                                  fsize=stat.st_size, sha=sha, flag_assume_valid=<span class="literal">False</span>,</span><br><span class="line">                                  flag_stage=<span class="literal">False</span>, name=relpath)</span><br><span class="line"></span><br><span class="line">            <span class="comment"># 将新条目添加到索引条目列表</span></span><br><span class="line">            index.entries.append(entry)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 将更新后的索引写回文件</span></span><br><span class="line">    index_write(repo, index)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">rm</span>(<span class="params">repo, paths, delete=<span class="literal">True</span>, skip_missing=<span class="literal">False</span></span>):</span><br><span class="line">    <span class="comment"># 查找并读取索引文件</span></span><br><span class="line">    index = index_read(repo)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 获取工作树的根目录路径（末尾加上路径分隔符）</span></span><br><span class="line">    worktree = repo.worktree + os.sep</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 将输入路径转换为绝对路径，并确保它们位于工作树内</span></span><br><span class="line">    abspaths = <span class="built_in">set</span>()</span><br><span class="line">    <span class="keyword">for</span> path <span class="keyword">in</span> paths:</span><br><span class="line">        abspath = os.path.abspath(path)  <span class="comment"># 获取绝对路径</span></span><br><span class="line">        <span class="keyword">if</span> abspath.startswith(worktree):  <span class="comment"># 确保路径在工作树中</span></span><br><span class="line">            abspaths.add(abspath)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">raise</span> Exception(<span class="string">f&quot;Cannot remove paths outside of worktree: <span class="subst">&#123;paths&#125;</span>&quot;</span>)  <span class="comment"># 如果路径不在工作树内，抛出异常</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 将要保留的索引条目列表，用于更新索引文件</span></span><br><span class="line">    kept_entries = <span class="built_in">list</span>()</span><br><span class="line">    <span class="comment"># 将要删除的路径列表，用于物理删除文件</span></span><br><span class="line">    remove = <span class="built_in">list</span>()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 遍历索引条目，删除匹配的路径，保留其余条目</span></span><br><span class="line">    <span class="keyword">for</span> e <span class="keyword">in</span> index.entries:</span><br><span class="line">        full_path = os.path.join(repo.worktree, e.name)  <span class="comment"># 获取索引条目的完整路径</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> full_path <span class="keyword">in</span> abspaths:  <span class="comment"># 如果路径在待删除列表中</span></span><br><span class="line">            remove.append(full_path)  <span class="comment"># 加入待删除列表</span></span><br><span class="line">            abspaths.remove(full_path)  <span class="comment"># 从待删除路径中移除</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            kept_entries.append(e)  <span class="comment"># 保留该条目</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 如果有未在索引中找到的路径，且 skip_missing=False，则抛出异常</span></span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">len</span>(abspaths) &gt; <span class="number">0</span> <span class="keyword">and</span> <span class="keyword">not</span> skip_missing:</span><br><span class="line">        <span class="keyword">raise</span> Exception(<span class="string">f&quot;Cannot remove paths not in the index: <span class="subst">&#123;abspaths&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 物理删除文件系统中的路径</span></span><br><span class="line">    <span class="keyword">if</span> delete:</span><br><span class="line">        <span class="keyword">for</span> path <span class="keyword">in</span> remove:</span><br><span class="line">            os.unlink(path)  <span class="comment"># 删除文件</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 更新索引条目，并将其写回索引文件</span></span><br><span class="line">    index.entries = kept_entries</span><br><span class="line">    index_write(repo, index)</span><br></pre></td></tr></table></figure>

<p>然后再使用<code>git add</code>之后的<code>.git/index</code>文件生成树对象，最后再借由树对象生成提交对象！！！具体<code>python</code>代码实现如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">tree_from_index</span>(<span class="params">repo, index</span>):</span><br><span class="line">    <span class="comment"># 初始化一个字典，用于存储目录及其内容，根目录 &quot;&quot; 存储一个空列表。</span></span><br><span class="line">    contents = <span class="built_in">dict</span>()</span><br><span class="line">    contents[<span class="string">&quot;&quot;</span>] = <span class="built_in">list</span>()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 遍历索引中的每个条目，构建目录结构。</span></span><br><span class="line">    <span class="keyword">for</span> entry <span class="keyword">in</span> index.entries:</span><br><span class="line">        dirname = os.path.dirname(entry.name)  <span class="comment"># 获取当前条目的目录名</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 遍历目录路径，直到根目录，确保每个目录都被创建</span></span><br><span class="line">        key = dirname</span><br><span class="line">        <span class="keyword">while</span> key != <span class="string">&quot;&quot;</span>:</span><br><span class="line">            <span class="keyword">if</span> key <span class="keyword">not</span> <span class="keyword">in</span> contents:  <span class="comment"># 如果该目录尚未在 contents 中，创建一个空列表</span></span><br><span class="line">                contents[key] = <span class="built_in">list</span>()</span><br><span class="line">            key = os.path.dirname(key)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 将条目添加到对应目录的内容列表中</span></span><br><span class="line">        contents[dirname].append(entry)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 获取所有目录的路径并按路径长度从长到短排序</span></span><br><span class="line">    sorted_paths = <span class="built_in">sorted</span>(contents.keys(), key=<span class="built_in">len</span>, reverse=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 初始化树的 SHA-1 哈希（根树的哈希值将会保存在这里）</span></span><br><span class="line">    sha = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 遍历排序后的路径（目录），构建树对象</span></span><br><span class="line">    <span class="keyword">for</span> path <span class="keyword">in</span> sorted_paths:</span><br><span class="line">        tree = GitTree()  <span class="comment"># 创建一个新的空树对象</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 遍历该目录下的条目，生成树叶并添加到树中</span></span><br><span class="line">        <span class="keyword">for</span> entry <span class="keyword">in</span> contents[path]:</span><br><span class="line">            <span class="keyword">if</span> <span class="built_in">isinstance</span>(entry, GitIndexEntry):  <span class="comment"># 普通的文件条目</span></span><br><span class="line">                <span class="comment"># 转换文件的权限模式，Git 使用十六进制表示，树对象需要的是八进制 ASCII 字符串</span></span><br><span class="line">                leaf_mode = <span class="string">f&quot;<span class="subst">&#123;entry.mode_type:02o&#125;</span><span class="subst">&#123;entry.mode_perms:04o&#125;</span>&quot;</span>.encode(<span class="string">&quot;ascii&quot;</span>)</span><br><span class="line">                leaf = GitTreeLeaf(mode=leaf_mode, path=os.path.basename(entry.name), sha=entry.sha)</span><br><span class="line">            <span class="keyword">else</span>:  <span class="comment"># 子目录条目（树）</span></span><br><span class="line">                leaf = GitTreeLeaf(mode=<span class="string">b&quot;040000&quot;</span>, path=entry[<span class="number">0</span>], sha=entry[<span class="number">1</span>])</span><br><span class="line"></span><br><span class="line">            tree.items.append(leaf)  <span class="comment"># 将叶子添加到树对象中</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 将生成的树对象写入对象存储，并获取其 SHA-1 哈希</span></span><br><span class="line">        sha = object_write(tree, repo)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 将树对象的哈希值添加到其父目录的内容列表中，作为一对 (文件名, SHA)</span></span><br><span class="line">        parent = os.path.dirname(path)  <span class="comment"># 获取父目录路径</span></span><br><span class="line">        base = os.path.basename(path)   <span class="comment"># 获取当前目录的名称（例如 &quot;main.go&quot;）</span></span><br><span class="line">        contents[parent].append((base, sha))  <span class="comment"># 将父目录与新生成的树的哈希值关联</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> sha  <span class="comment"># 返回根树的 SHA-1 哈希</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">commit_create</span>(<span class="params">repo, tree, parent, author, timestamp, message</span>):</span><br><span class="line">    commit = GitCommit()  <span class="comment"># 创建一个新的提交对象</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 设置提交对象的树字段为当前提交的树对象的 SHA-1 值</span></span><br><span class="line">    commit.kvlm[<span class="string">b&quot;tree&quot;</span>] = tree.encode(<span class="string">&quot;ascii&quot;</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 如果有父提交，设置父提交字段</span></span><br><span class="line">    <span class="keyword">if</span> parent:</span><br><span class="line">        commit.kvlm[<span class="string">b&quot;parent&quot;</span>] = parent.encode(<span class="string">&quot;ascii&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 清理提交信息，并在末尾加上换行符</span></span><br><span class="line">    message = message.strip() + <span class="string">&quot;\n&quot;</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 格式化时区偏移（例如 &quot;+0200&quot; 或 &quot;-0700&quot;）</span></span><br><span class="line">    offset = <span class="built_in">int</span>(timestamp.astimezone().utcoffset().total_seconds())  <span class="comment"># 获取时区偏移（秒）</span></span><br><span class="line">    hours = offset // <span class="number">3600</span>  <span class="comment"># 计算小时</span></span><br><span class="line">    minutes = (offset % <span class="number">3600</span>) // <span class="number">60</span>  <span class="comment"># 计算分钟</span></span><br><span class="line">    tz = <span class="string">&quot;&#123;&#125;&#123;:02&#125;&#123;:02&#125;&quot;</span>.<span class="built_in">format</span>(<span class="string">&quot;+&quot;</span> <span class="keyword">if</span> offset &gt; <span class="number">0</span> <span class="keyword">else</span> <span class="string">&quot;-&quot;</span>, hours, minutes)  <span class="comment"># 格式化时区字符串</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 将作者信息格式化为 &quot;Author Name &lt;email&gt; timestamp timezone&quot;</span></span><br><span class="line">    author = author + timestamp.strftime(<span class="string">&quot; %s &quot;</span>) + tz</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 设置提交对象的作者和提交者字段为相同的值</span></span><br><span class="line">    commit.kvlm[<span class="string">b&quot;author&quot;</span>] = author.encode(<span class="string">&quot;utf8&quot;</span>)</span><br><span class="line">    commit.kvlm[<span class="string">b&quot;committer&quot;</span>] = author.encode(<span class="string">&quot;utf8&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 设置提交信息字段</span></span><br><span class="line">    commit.kvlm[<span class="literal">None</span>] = message.encode(<span class="string">&quot;utf8&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 将提交对象写入对象存储，并返回其 SHA-1 哈希</span></span><br><span class="line">    <span class="keyword">return</span> object_write(commit, repo)</span><br></pre></td></tr></table></figure>

<p>现在，😮‍💨，所有Git基础的内容的底层原理以及其代码形式差不多就讲解完了。还有一些比较高级的Git内容比如我们常见的命名简化，分支以及.gitingore文件等等我们留待后文。</p>
<h2 id="参考网站"><a href="#参考网站" class="headerlink" title="参考网站"></a>参考网站</h2><ul>
<li><a href="https://wyag.thb.lt/">Write yourself a Git!</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/96631135">这才是真正的Git——Git内部原理揭秘！</a></li>
</ul>
]]></content>
      <categories>
        <category>Linux操作系统</category>
      </categories>
      <tags>
        <tag>Git</tag>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>EEG数据初探</title>
    <url>/2025/03/09/2025-3-9-eeg-data/</url>
    <content><![CDATA[<p>最近在学习EEG数据的处理，发现网上的有关EEG数据的讲解较少，尤其是使用<code>mne-python</code>库来对EEG数据进行处理，现在来对EEG数据和该库的整体进行大致的窥探。</p>
<span id="more"></span>

<h2 id="EEG数据介绍"><a href="#EEG数据介绍" class="headerlink" title="EEG数据介绍"></a>EEG数据介绍</h2><p>从我们数据处理人员的角度来说，尤其是进行深度学习的数据处理。我们要尤其注意数据的维度属性，从这个角度来看，一个简单的EEG数据会有两个维度属性：空间（通道）维度和时间维度：空间维度从物理层面来看就是我们脑袋上所接入的电极数量；而时间维度就是电极以固定频率所采集的离散点。有时候为了固定的任务要求会让受试者参与一定的事件，事件触发信号通常由<strong>刺激通道</strong>（<code>stim channel</code>）来进行记录，刺激通道中标记的特定事件的时间点和类别被称为<code>trigger</code>。</p>
<h2 id="mne-python库"><a href="#mne-python库" class="headerlink" title="mne-python库"></a><code>mne-python</code>库</h2><p>有了上面的对EEG数据简要介绍，接下来我们就可以对<code>mne-python</code>中的一些功能函数做出更加详细的介绍。</p>
<h3 id="读取EEG数据"><a href="#读取EEG数据" class="headerlink" title="读取EEG数据"></a>读取EEG数据</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># stim_channel就是我们上面所说的时间通道，在自定义的数据集中我们将该通道命名为&#x27;Status&#x27;</span></span><br><span class="line"><span class="comment"># preload=True会让数据会被直接加载到内存中，这样后续操作（如滤波、提取特征等）可以直接使用内存中的数据，速度更快。</span></span><br><span class="line">raw = mne.io.read_raw_edf(<span class="string">&#x27;data.edf&#x27;</span>, stim_channel=<span class="string">&quot;Status&quot;</span>, preload=<span class="literal">True</span>)</span><br></pre></td></tr></table></figure>
<p>输出为：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">Extracting EDF parameters from data.edf...</span><br><span class="line">EDF file detected</span><br><span class="line">Setting channel info structure...</span><br><span class="line">Creating raw.info structure...</span><br><span class="line">Reading 0 ... 786431  =      0.000 ...   767.999 secs...</span><br></pre></td></tr></table></figure>

<p>EEG数据的格式不止有.edf格式，其他常见的还有.bdf格式、.fif格式等等，在<code>mne-python</code>库中都有相应的读取实现（<code>mne.io.read_raw_bdf</code>,<code>mne.io.read_raw_fif</code>）等，看情况选取。</p>
<h3 id="了解Raw对象的信息"><a href="#了解Raw对象的信息" class="headerlink" title="了解Raw对象的信息"></a>了解<code>Raw</code>对象的信息</h3><p>一些比较重要的EEG数据的属性信息我们都可以使用<code>raw.info</code>来进行索引，比如时间频率信息和空间通道信息。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">raw.info[<span class="string">&#x27;sfreq&#x27;</span>] <span class="comment"># 时间频率</span></span><br></pre></td></tr></table></figure>
<p>输出为：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">1024.0</span><br></pre></td></tr></table></figure>

<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">raw.info[<span class="string">&#x27;ch_names&#x27;</span>] <span class="comment"># 空间通道</span></span><br></pre></td></tr></table></figure>
<p>输出为：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">[&#x27;Fp1&#x27;,</span><br><span class="line"> &#x27;AF7&#x27;,</span><br><span class="line"> &#x27;AF3&#x27;,</span><br><span class="line"> &#x27;F1&#x27;,</span><br><span class="line"> &#x27;F3&#x27;,</span><br><span class="line"> &#x27;F5&#x27;,</span><br><span class="line"> &#x27;F7&#x27;,</span><br><span class="line"> &#x27;FT7&#x27;,</span><br><span class="line"> &#x27;FC5&#x27;,</span><br><span class="line"> &#x27;FC3&#x27;,</span><br><span class="line"> &#x27;FC1&#x27;,</span><br><span class="line"> &#x27;C1&#x27;,</span><br><span class="line"> &#x27;C3&#x27;,</span><br><span class="line"> &#x27;C5&#x27;,</span><br><span class="line"> &#x27;T7&#x27;,</span><br><span class="line"> &#x27;TP7&#x27;,</span><br><span class="line"> &#x27;CP5&#x27;,</span><br><span class="line"> &#x27;CP3&#x27;,</span><br><span class="line"> &#x27;CP1&#x27;,</span><br><span class="line"> &#x27;P1&#x27;,</span><br><span class="line"> &#x27;P3&#x27;,</span><br><span class="line"> &#x27;P5&#x27;,</span><br><span class="line"> &#x27;P7&#x27;,</span><br><span class="line"> &#x27;P9&#x27;,</span><br><span class="line"> &#x27;PO7&#x27;,</span><br><span class="line"> &#x27;PO3&#x27;,</span><br><span class="line"> &#x27;O1&#x27;,</span><br><span class="line"> &#x27;Iz&#x27;,</span><br><span class="line"> &#x27;Oz&#x27;,</span><br><span class="line"> &#x27;POz&#x27;,</span><br><span class="line"> &#x27;Pz&#x27;,</span><br><span class="line"> &#x27;CPz&#x27;,</span><br><span class="line"> &#x27;Fpz&#x27;,</span><br><span class="line"> &#x27;Fp2&#x27;,</span><br><span class="line"> &#x27;AF8&#x27;,</span><br><span class="line"> &#x27;AF4&#x27;,</span><br><span class="line"> &#x27;AFz&#x27;,</span><br><span class="line"> &#x27;Fz&#x27;,</span><br><span class="line"> &#x27;F2&#x27;,</span><br><span class="line"> &#x27;F4&#x27;,</span><br><span class="line"> &#x27;F6&#x27;,</span><br><span class="line"> &#x27;F8&#x27;,</span><br><span class="line"> &#x27;FT8&#x27;,</span><br><span class="line"> &#x27;FC6&#x27;,</span><br><span class="line"> &#x27;FC4&#x27;,</span><br><span class="line"> &#x27;FC2&#x27;,</span><br><span class="line"> &#x27;FCz&#x27;,</span><br><span class="line"> &#x27;Cz&#x27;,</span><br><span class="line"> &#x27;C2&#x27;,</span><br><span class="line"> &#x27;C4&#x27;,</span><br><span class="line"> &#x27;C6&#x27;,</span><br><span class="line"> &#x27;T8&#x27;,</span><br><span class="line"> &#x27;TP8&#x27;,</span><br><span class="line"> &#x27;CP6&#x27;,</span><br><span class="line"> &#x27;CP4&#x27;,</span><br><span class="line"> &#x27;CP2&#x27;,</span><br><span class="line"> &#x27;P2&#x27;,</span><br><span class="line"> &#x27;P4&#x27;,</span><br><span class="line"> &#x27;P6&#x27;,</span><br><span class="line"> &#x27;P8&#x27;,</span><br><span class="line"> &#x27;P10&#x27;,</span><br><span class="line"> &#x27;PO8&#x27;,</span><br><span class="line"> &#x27;PO4&#x27;,</span><br><span class="line"> &#x27;O2&#x27;,</span><br><span class="line"> &#x27;EXG1&#x27;,</span><br><span class="line"> &#x27;EXG2&#x27;,</span><br><span class="line"> &#x27;EXG3&#x27;,</span><br><span class="line"> &#x27;EXG4&#x27;,</span><br><span class="line"> &#x27;EXG5&#x27;,</span><br><span class="line"> &#x27;EXG6&#x27;,</span><br><span class="line"> &#x27;EXG7&#x27;,</span><br><span class="line"> &#x27;EXG8&#x27;,</span><br><span class="line"> &#x27;GSR1&#x27;,</span><br><span class="line"> &#x27;GSR2&#x27;,</span><br><span class="line"> &#x27;Erg1&#x27;,</span><br><span class="line"> &#x27;Erg2&#x27;,</span><br><span class="line"> &#x27;Resp&#x27;,</span><br><span class="line"> &#x27;Plet&#x27;,</span><br><span class="line"> &#x27;Temp&#x27;,</span><br><span class="line"> &#x27;Status&#x27;]</span><br></pre></td></tr></table></figure>

<p>当然，<code>raw.info</code>身为类似字典的类型还有许多其他的属性，我们可以通过以下代码查看：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">raw.info.keys()</span><br></pre></td></tr></table></figure>
<p>输出为：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">dict_keys([&#x27;acq_pars&#x27;, &#x27;acq_stim&#x27;, &#x27;ctf_head_t&#x27;, &#x27;description&#x27;, &#x27;dev_ctf_t&#x27;, &#x27;dig&#x27;, &#x27;experimenter&#x27;, &#x27;utc_offset&#x27;, &#x27;device_info&#x27;, &#x27;file_id&#x27;, &#x27;highpass&#x27;, &#x27;hpi_subsystem&#x27;, &#x27;kit_system_id&#x27;, &#x27;helium_info&#x27;, &#x27;line_freq&#x27;, &#x27;lowpass&#x27;, &#x27;meas_date&#x27;, &#x27;meas_id&#x27;, &#x27;proj_id&#x27;, &#x27;proj_name&#x27;, &#x27;subject_info&#x27;, &#x27;xplotter_layout&#x27;, &#x27;gantry_angle&#x27;, &#x27;bads&#x27;, &#x27;chs&#x27;, &#x27;comps&#x27;, &#x27;events&#x27;, &#x27;hpi_meas&#x27;, &#x27;hpi_results&#x27;, &#x27;projs&#x27;, &#x27;proc_history&#x27;, &#x27;custom_ref_applied&#x27;, &#x27;sfreq&#x27;, &#x27;dev_head_t&#x27;, &#x27;ch_names&#x27;, &#x27;nchan&#x27;])</span><br></pre></td></tr></table></figure>

<p>其中比较重要的有以下属性：</p>
<ul>
<li><strong>highpass</strong>: 高通滤波器的截止频率，表示信号处理时应用的高通滤波参数。</li>
<li><strong>lowpass</strong>: 低通滤波器的截止频率，用于信号处理时的低通滤波。</li>
<li><strong>bads</strong>: 有问题或坏掉的通道列表，这些通道的数据可能被排除在分析之外。</li>
<li><strong>chs</strong>: 通道信息，包含每个通道的类型、位置和其他详细描述。</li>
<li><strong>events</strong>: 事件信息，记录实验过程中发生的关键事件或时间点。</li>
<li><strong>sfreq</strong>: 采样频率，表示每秒钟采集多少次数据点。</li>
<li><strong>ch_names</strong>: 通道名称列表，表示每个通道的标识符（如 EEG 或 MEG 通道的名字）。</li>
<li><strong>nchan</strong>: 通道数量，表示测量中使用的总通道数。</li>
</ul>
<p>详细的情况可以自己运行<code>raw.info[&#39;key&#39;]</code>进行查看。</p>
<h3 id="EEG原始数据Epoch化"><a href="#EEG原始数据Epoch化" class="headerlink" title="EEG原始数据Epoch化"></a>EEG原始数据Epoch化</h3><h4 id="EEG原始数据可视化"><a href="#EEG原始数据可视化" class="headerlink" title="EEG原始数据可视化"></a>EEG原始数据可视化</h4><p>EEG原始数据一般来说是包含多个事件的，可视化详情如图1所示：</p>
<p><img data-src="/../../codes/eeg-data/raw.png" alt="我的图片"></p>
<center>
<strong>图1：EEG原始数据附带事件标记截取部分通道和部分时间可视化</strong>
</center>

<p>可以看到，我们截取的这30s的片段就发生了8次事件，该EEG原始数据采集了768s，可见这其中包含了远远不止8次事件这么简单。</p>
<h4 id="EEG原始数据事件"><a href="#EEG原始数据事件" class="headerlink" title="EEG原始数据事件"></a>EEG原始数据事件</h4><p>下一步我们要做的事就是定位事件发生的时刻以及事件类型，还记得我们在刚开始的时候所说的：</p>
<blockquote>
<p>事件触发信号通常由<strong>刺激通道</strong>（<code>stim channel</code>）来进行记录。</p>
</blockquote>
<p>因此要定位事件发生的位置及类型，我们不免要和<code>stim channel</code>打交道，我们现将该通道的电位随时间的变化可视化如图2所示。</p>
<p><img data-src="/../../codes/eeg-data/stim.png" alt="我的图片"></p>
<center>
<strong>图2：EEG原始数据附带事件标记截取刺激通道和部分时间可视化</strong>
</center>

<p>显而易见，刺激通道的电位幅度可以查找事件类型，电位幅度产生的时刻就是事件发生的时刻！在<code>mne-python</code>中根据刺激通道查找事件的时刻和类型已经封装好在<code>mne.find_events</code>中，具体代码如下所示：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># initial_event设置为 False，不会插入额外的初始事件，函数只返回真正检测到的事件。</span></span><br><span class="line">events = mne.find_events(raw=raw, stim_channel=<span class="string">&quot;Status&quot;</span>, initial_event=<span class="literal">False</span>)</span><br></pre></td></tr></table></figure>
<p>输出为:</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">161 events found on stim channel Status</span><br><span class="line">Event IDs: [  1   2   3   4   5   6   7   8 111]</span><br></pre></td></tr></table></figure>

<p>其中事件 ID 111表示事件开始。<code>events</code>是一个$n_events\times 3$的数组类型，纵向第一列表示事件发生的时刻，第三列表示事件发生的类型，第二列为全零：表示表示事件通道在该事件发生前的值。</p>
<p><code>event[:9]</code>的示例。</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">array([[ 2596,     0,   111],</span><br><span class="line">       [15698,     0,     8],</span><br><span class="line">       [19117,     0,     2],</span><br><span class="line">       [22758,     0,     7],</span><br><span class="line">       [26381,     0,     4],</span><br><span class="line">       [29834,     0,     6],</span><br><span class="line">       [33664,     0,     3],</span><br><span class="line">       [37378,     0,     1],</span><br><span class="line">       [40793,     0,     5]])</span><br></pre></td></tr></table></figure>

<p>EEG原始数据Epoch化的前戏已经准备好了，接下来我们要根据<code>events</code>和<code>raw</code>来对原始EEG数据进行Epoch化。Epoch化简单来说就是对原始的EEG数据在事件发生附近按照固定的时间范围进行截取，由于我们的EEG原始数据往往会有很多事件，所以Epoch化得到的结果可以说是批量化的数据，这里的批次就是EEG数据中的有效事件数量（深度学习模型狂喜😁）。</p>
<p>具体的实现过程也被封装在了<code>mne.Epochs</code>中：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># tmin和tmax表示以事件为基准然后以（tmin s, tmax s）为时间范围截取</span></span><br><span class="line"><span class="comment"># baseline用于进行基线校正。基线校正是指在Epoch化之前，减去一个时间段的平均值以消除噪声</span></span><br><span class="line"><span class="comment"># proj控制是否应用投影运算符（比如通道信号的空间滤波）。</span></span><br><span class="line">epochs = mne.Epochs(raw, events=events, event_id=event_id, tmin=<span class="number">0</span>, tmax=<span class="number">1</span>, baseline=<span class="literal">None</span>, proj=<span class="literal">False</span>, picks=<span class="string">&#x27;eeg&#x27;</span>, preload=<span class="literal">True</span>)</span><br></pre></td></tr></table></figure>

<p>然后我们可以根据<code>epochs</code>得到批量化的数据，使用如下方法：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">batched_data = epochs.get_data()[:, :, :-<span class="number">1</span>]</span><br></pre></td></tr></table></figure>

<p>批量化数据的形状为$bs\times n_channels \times n_times$，Epoch化后的EEG原始数据截取部分通道和部分事件后的可视化如图3所示。</p>
<p><img data-src="/../../codes/eeg-data/epoch.png" alt="我的图片"></p>
<center>
<strong>图3：Epoch化后EEG数据截取部分事件和部分通道后的可视化</strong>
</center>
]]></content>
      <categories>
        <category>Data Analysis</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>EEG</tag>
      </tags>
  </entry>
</search>
